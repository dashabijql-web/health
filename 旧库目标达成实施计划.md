# Health 旧库基线目标达成实施计划

制定日期：2026-05-08  
执行数据源：旧库 `health`  
对应评估文档：`D:/Health/旧库项目重新评估结果.md`  
目标场景：模拟器、演示环境、高数据量历史库、旧库全页面非空和性能稳定。

## 1. 执行入口

新开 Codex 会话如果目标是“按旧库把页面空组件、屎山代码、代码健壮度、功能完善性、页面美观度继续推进到目标态”，建议使用以下指令：

```text
以 D:/Health/AGENTS.md、D:/Health/旧库项目重新评估结果.md、D:/Health/旧库目标达成实施计划.md 为主执行文档，在 old 数据源下持续推进 HealthShow 与 HealthData，直到满足 D:/Health/旧库目标达成实施计划.md 的“最终完成定义”为止。执行时必须确认前端顶栏或请求头为 old，不能把 health_new 空库问题混入旧库结论。
```

不要从 `docs/archive/历史归档-治理清单.md` 或 `docs/archive/历史归档-CODEX_MULTI_DAY_TASKBOOK.md` 机械重跑已完成任务。那两份文档仍可作为历史总表和背景资料，但旧库专项执行以本文件为准。

## 2. 最终完成定义

只有以下条件同时满足，才允许说“旧库场景下，页面空数据、屎山代码、健壮度、功能完善性和页面美观度已基本达到目标”。

### 2.1 数据源和数据密度

- 前端默认或当前会话明确为 `old`。
- `audit:data` 显示 `data_source: old`。
- 旧库关键页面核心组件非空，且空态必须有白名单说明。
- 旧库 `department / employee / device / device_user` 数据可用。
- 旧库健康记录、预警记录分表可被统一查询，不允许新接口误查空主表。

### 2.2 代码质量

- 后端核心服务不再出现职责重新堆回 `DataProcessService`。
- 大盘查询栈拆分清晰，慢查询和缓存策略可解释。
- 指标页重复逻辑有统一 engine、mixin、composable 或明确等价抽象。
- AI 服务至少拆清：会话、SQL 守卫、查询执行、图表生成、报告解释、降级响应。
- 前端大页面继续保持 view-model/runtime/scss 分层，不新增巨型单文件。

### 2.3 健壮性

- `mvn -q test` 通过。
- `npm run build` 通过。
- `npm run audit:api` 通过。
- `npm run audit:data` 通过。
- `npm run audit:auth` 通过。
- `npm run audit:e2e` 通过。
- `npm run audit:pipeline` 通过。
- `npm run audit:pipeline-warning` 通过。
- 慢接口有阈值、报告和整改记录。
- 旧库和新库切换不会互相污染结论。

### 2.4 功能完善性

- 预警处理、预警记录、消息通知、SOS 至少完成一轮旧库操作闭环验收。
- 入井准入能解释达标、禁入、待复核人员来源，且处理路径可验收。
- 报表中心能导出旧库代表性数据，字段和口径清楚。
- AI 报告和 AI 聊天在模型不可用时有可接受降级体验。
- 设备、用户、角色、部门、工种管理页完成基础 CRUD 或明确只读边界。

### 2.5 页面美观度

- 大盘、实时监控、安全指挥中心、工作台在旧库有数据下完成桌面视觉验收。
- 关键页面完成移动端基本可用验收。
- 至少建立截图级视觉基线，能发现布局遮挡、严重溢出、关键图表空白。
- 后台管理页完成轻量视觉统一，不再明显像默认 Element Plus 表格拼装。
- 新库空态和旧库异常空态文案区分清楚。

## 3. 当前旧库基线

当前已通过：

- `mvn -q test -f D:/Health/HealthData/pom.xml`
- `npm run audit:api`：`53 checks / 0 failed / 0 warning`
- `npm run audit:data`：`21 checks / 0 failed / 0 warning`
- `npm run audit:nav`：`6 navGroups / 23 visibleNavLeaves / 5 mobileNavItems`
- `npm run audit:auth`：`7 checks / 0 failed`
- `npm run audit:e2e`：`34 routes / 0 issue`
- `npm run audit:write`：`5 checks / 0 failed / 0 warning`
- `npm run audit:pipeline`：`11 stages / 0 failed / 0 warning`
- `npm run audit:pipeline-warning`：`12 stages / 0 failed / 0 warning`
- `npm run build`

当前旧库核心数据：

- `department=20`
- `employee=1000`
- `device=1000`
- `device_user=1000`
- `health_record_202602=108000`
- `health_record_202603=2429036`
- `health_record_202604=5550312`
- `health_record_202605=3557541`
- `warning_record_202602=8010`
- `warning_record_202603=15830`
- `warning_record_202604=46001`
- `warning_record_202605=30758`

当前旧库主要风险：

- `health_record`、`warning_record` 主表为空，实际数据在分表。
- `realtime_data`、`user_online_status` 为空，但接口在线统计为 1000，需要明确语义。
- `health-record.total` 约 5 秒级，旧库性能还需专项治理。
- 大盘前端样式和图表配置仍大。
- 视觉回归仍不是截图级。

## 4. 执行原则

### 4.1 先确认数据源

每轮执行前必须确认：

```powershell
cd D:/Health/HealthShow
npm run audit:data
```

报告里必须出现：

```text
data_source: old
expect_non_empty: true
failed: 0
```

如果页面空，先查数据源：

- 顶栏是否为 `老库`。
- Cookie 是否为 `Health-Data-Source=old`。
- 请求头是否带 `X-Health-Data-Source: old`。
- 响应头是否回写 `X-Health-Data-Source: old`。

### 4.2 不把新库空态当旧库问题

新库 `health_new` 的员工、部门、设备为空，是另一条任务线。旧库执行期间，除非任务明确要求双库联动，否则不把新库空态纳入旧库缺陷。

### 4.3 不牺牲真实手表路径

旧库专项治理不能破坏：

- 真实手表默认写新库。
- 模拟器默认写旧库。
- `HEALTH_SIMULATOR_IMEI_REGEX=^3594567800\d{5}$`。
- `HealthDataSourceRequestFilter` 的请求早期切库逻辑。

### 4.4 每个阶段都要有验证

任何修复都必须至少跑与改动相关的最小验证。涉及数据源、接口、页面结构、流水、预警时，必须跑对应审计脚本。

## 5. Phase O1：旧库数据契约和分表语义固化

目标：防止后续代码误查空主表，防止旧库高数据量页面再次出现“组件空”。

### 任务 1：补充旧库数据字典

输出：

- `health_record` 主表用途说明。
- `health_record_20*` 分表用途说明。
- `warning_record` 主表用途说明。
- `warning_record_20*` 分表用途说明。
- 关键视图和存储过程说明。
- 温度、血压、血氧、压力、步数、睡眠字段缩放/缺失规则。

验收：

- 文档明确哪些接口可以查主表，哪些必须查分表。
- 新增健康字段时有同步清单。

### 任务 2：统一分表查询入口

候选方向：

- 后端提供 `HealthRecordPartitionQueryService`。
- 后端提供 `WarningRecordPartitionQueryService`。
- Mapper 层只保留底层 SQL，跨月和当前月逻辑上移到 service。

验收：

- 旧库下健康记录和预警记录查询不因主表为空而空。
- 新库空态仍能正常返回空列表，不报错。

### 任务 3：明确实时状态来源

需要回答：

- `realtime_data=0` 时，为什么接口仍返回在线 1000。
- 在线状态是基于设备表、最近健康记录、Redis、内存状态还是状态表。
- 在线过期时间是多少。
- 模拟器停止后多久应显示离线。

验收：

- 文档和代码命名一致。
- `device.online.count` 统计来源可解释。

## 6. Phase O2：旧库全页面数据密度门禁扩展

目标：从“21 个关键检查非空”扩展到“所有关键页面关键组件有数据或有白名单空态”。

### 任务 1：扩展 `audit:data`

新增覆盖：

- `safety-command` 核心 KPI、地图/区域、部门排行、告警摘要。
- `workbench` 日历、待办、准入、预警摘要。
- `real-time` 在线列表、实时告警、设备状态、体征卡片。
- `report-center` 图表、报表列表、导出入口。
- `ai-chat` 历史会话、示例问题、查询结果、降级提示。
- `alert-notifications` 待处理列表、处理动作入口。
- `alert-sos` SOS 列表、状态统计。
- `alert-records` 历史记录。
- `device-list`、`user-list`、`role`、`department`、`job-type`。

验收：

- `audit:data` 检查数至少覆盖 50 项。
- 旧库下所有关键检查通过。
- 新库下允许空的检查必须显式标注白名单。

### 任务 2：建立空态白名单

白名单字段：

- 页面。
- 组件。
- 数据源：old/new/all。
- 是否允许为空。
- 为空时显示文案。
- 为空时是否算失败。

验收：

- 旧库下核心组件空直接失败。
- 新库下预期空显示“新库暂无业务数据/请初始化员工设备”。

### 任务 3：前端空态统一组件

输出：

- 统一 `EmptyState` 或等价组件。
- 区分 `loading`、`empty`、`error`、`no-permission`、`new-db-empty`。

验收：

- 页面不再用零值冒充加载成功。
- 接口失败和真实空列表有不同提示。

## 7. Phase O3：旧库高数据量性能治理

目标：旧库千万级数据下，核心页面不依赖 5 秒级慢查询。

### 任务 1：建立性能预算

建议预算：

| 接口类型 | 目标 |
| --- | ---: |
| 顶部 KPI / overview | 800ms 内 |
| 页面首屏核心接口 | 1200ms 内 |
| 历史记录分页 | 1500ms 内 |
| 跨月重统计 | 2500ms 内，且需要缓存 |
| 导出类接口 | 可异步或明确进度，不阻塞首屏 |

验收：

- `audit:data` 或新增 `audit:perf` 输出接口耗时。
- 超预算接口以 warning 或 failed 标出。

### 任务 2：治理当前慢接口

优先接口：

- `health-record.total`：约 5.2s。
- `sleep.detailList`：约 2.1s。
- `trend-warning.list`：约 2.1s。
- `heart-rate.totalCount`：约 1.9s。
- `dashboard.calendar`：约 1.9s。
- `dashboard.person-counts`：约 1.3s。

候选动作：

- 确认 SQL Server 索引。
- 避免全分表扫描。
- 当前月和历史月分开缓存。
- total count 缓存或预聚合。
- 分页接口避免每次强制 count 全量。
- 高频 overview 使用短 TTL。

验收：

- 旧库重复跑三次，耗时稳定下降。
- 不牺牲新库空态正确性。

### 任务 3：补慢 SQL 可观测性

输出：

- 慢 SQL 指标按接口、表、数据源分组。
- `health.sql.slow.total` 能区分 old/new。
- 文档说明如何从 actuator 定位慢接口。

验收：

- 可以从 `http://localhost:8080/health/actuator/metrics` 找到旧库慢查询证据。

## 8. Phase O4：后端结构和健壮性继续压缩

目标：旧库高数据量下后端继续可维护，不把逻辑重新堆回大类。

### 任务 1：Dashboard 查询栈治理

范围：

- `DashboardServiceImpl`
- `service/dashboard/*`
- `mapper/Dashboard*`
- 大盘相关 DTO

要求：

- Controller 只做入参和响应。
- Service 按 KPI、趋势、部门、预警、准入拆职责。
- 缓存策略集中定义。
- 旧库分表逻辑不散落。

验收：

- `DashboardServiceImpl` 继续下降或职责更清晰。
- API smoke 通过。
- data-density 通过。

### 任务 2：指标页查询 engine 化

范围：

- 心率
- 压力
- 血压
- 血氧
- 睡眠

要求：

- 保留各指标差异。
- 抽出通用分页、趋势、分布、排行、部门统计。
- 不把所有差异硬塞进一个巨型 if/else。

验收：

- 至少两个指标页完成同一抽象。
- 旧库数据密度仍通过。

### 任务 3：AI 服务拆分

范围：

- `AiChatService`
- SQL guard
- Schema provider
- Chart/export/session 相关前端模块

要求：

- SQL 生成和 SQL 执行分层。
- 查询结果解释和图表推荐分层。
- 模型不可用时返回可读降级信息。

验收：

- AI 页面不会因为模型异常导致整页不可用。
- API smoke 或新增 AI smoke 覆盖降级。

### 任务 4：补旧库边界测试

新增测试：

- 主表空、分表有数据。
- 当前月分表存在且有新数据。
- 跨月查询。
- 指标字段部分 `NULL`。
- 温度缩放。
- old/new 数据源 header。

验收：

- 后端单测覆盖旧库特殊语义。

## 9. Phase O5：前端结构、空态和交互稳态

目标：页面有数据时好看，没数据或接口异常时不误导。

### 任务 1：大盘前端继续拆分

范围：

- `dashboard.scss`
- `dashboard-chart-options.js`
- `dashboard/*` runtime/view-model/chart modules

要求：

- 设计 token 抽出。
- 图表 option 按模块拆分。
- 卡片布局和业务文案分离。

验收：

- 大盘构建通过。
- e2e dashboard 通过。
- 视觉无明显退化。

### 任务 2：指标页前端共用组件

输出：

- 通用趋势图组件。
- 通用分布卡片。
- 通用排行/部门统计。
- 通用筛选和导出入口。

验收：

- 改一个通用问题不需要四个页面重复改。

### 任务 3：统一 loading/empty/error

要求：

- 不用 `0` 代表接口没返回。
- 不用“暂无数据”掩盖接口异常。
- 旧库核心接口空时给出排障提示。

验收：

- `audit:data` 能识别组件异常空。

## 10. Phase O6：旧库功能闭环专项验收

目标：页面能看不等于功能完成，旧库要把能操作的业务动作跑通。

### 任务 1：预警处理闭环

验收路径：

1. 旧库打开消息通知中心。
2. 选择一条待处理预警。
3. 执行处理或批量处理。
4. 确认列表状态变化。
5. 确认预警记录同步变化。
6. 确认大盘待处理数字变化。

### 任务 2：SOS 闭环

验收路径：

1. 构造或选择 SOS 数据。
2. 查看 SOS 列表。
3. 处理 SOS。
4. 查看记录。
5. 确认大盘/通知变化。

### 任务 3：入井准入闭环

验收路径：

1. 查看准入名单。
2. 查看禁入人员原因。
3. 执行复核或处理动作。
4. 确认统计变化。

### 任务 4：报表导出闭环

验收路径：

1. 选择旧库时间范围。
2. 生成报表。
3. 导出 Excel/PDF。
4. 校验字段、行数、统计口径。
5. 大数据量导出不阻塞页面。

### 任务 5：后台管理 CRUD

范围：

- 设备。
- 用户。
- 角色。
- 部门。
- 工种。

验收：

- 如果页面支持新增/编辑/禁用/删除，必须跑通。
- 如果只允许只读，页面文案和权限要明确。

## 11. Phase O7：页面美观度产品化

目标：旧库有数据时不仅能看，还要适合演示和长期使用。

### 任务 1：建立截图基线

优先页面：

- `safety-command`
- `dashboard`
- `workbench`
- `real-time`
- `risk-warning`
- `employee-archive`
- `report-center`
- `ai-chat`

验收：

- 至少桌面 1920 宽截图。
- 关键移动端截图。
- 截图产物纳入 `tests/e2e/artifacts` 或独立 visual artifacts。

### 任务 2：大屏信息密度调优

要求：

- 1920 宽下首屏不严重拥挤。
- 1366 宽下核心信息可读。
- 右侧栏不遮挡。
- 图表 legend、label 不重叠。

### 任务 3：后台管理页轻量美化

目标：

- 保持业务效率，不做过度大屏化。
- 统一筛选区、表格、状态标签、操作按钮。
- 空态和错误态统一。

### 任务 4：设计 token 固化

输出：

- 色彩变量。
- 间距变量。
- 字号层级。
- 卡片、边框、阴影、图表背景规则。

验收：

- 新页面不再随意复制颜色值。

## 12. Phase O8：旧库回归门禁和交接

目标：治理成果能被后续 Codex 和人类开发者继续执行。

### 任务 1：固化旧库回归命令

推荐旧库完整回归：

```powershell
cd D:/Health/HealthShow
npm run audit:api
npm run audit:data
npm run audit:auth
npm run audit:e2e
npm run audit:write
npm run audit:pipeline
npm run audit:pipeline-warning
npm run build
```

后端：

```powershell
$env:JAVA_HOME='C:/Program Files/Java/jdk-17'
D:/apache-maven-3.8.1/bin/mvn -q test -f D:/Health/HealthData/pom.xml
```

### 任务 2：把旧库报告入口写入交接

需要同步：

- `AGENTS.md`
- `docs/archive/历史归档-HEALTH_HANDOFF.md`
- 必要时 `HealthData/DUAL_DB_CUTOVER.md`

### 任务 3：形成旧库完成报告

最终报告必须包含：

- 本计划每个 Phase 的状态。
- 最新自动化结果。
- 慢接口是否达标。
- 空态白名单。
- 功能闭环验收证据。
- 页面截图基线。
- 未解决风险和原因。

## 13. 推荐执行顺序

按风险和收益排序：

1. `O1`：旧库数据契约和分表语义固化。
2. `O2`：旧库全页面数据密度门禁扩展。
3. `O3`：旧库高数据量性能治理。
4. `O6`：预警、准入、报表、后台 CRUD 功能闭环。
5. `O5`：前端空态和结构稳态。
6. `O4`：后端结构继续压缩。
7. `O7`：页面美观度产品化。
8. `O8`：回归门禁和交接。

如果只能先做一轮，优先完成：

- O1 任务 1、2、3。
- O2 任务 1、2。
- O3 任务 1、2。
- O6 任务 1、3、4。

理由：这些最直接解决“旧库有数据但页面可能空、慢、口径不清、功能只展示不闭环”的问题。

## 14. 停止条件

执行过程中出现以下情况必须暂停并重新评估：

- `audit:data` 报告不是 `data_source: old`。
- 旧库核心数据突然变成空。
- 模拟器误写新库。
- 真实手表路径被旧库改动影响。
- 后端 pipeline 写入后无法清理。
- 某个性能优化导致统计口径变化。
- 视觉重构导致 e2e 或移动端明显退化。

## 15. 完成后的预期状态

本计划执行完后，旧库场景应达到：

- 页面不再因为误查新库或误查空主表而大面积空。
- 旧库所有关键组件都有数据密度门禁。
- 千万级历史数据下核心接口有性能预算和优化记录。
- 分表、缩放、缺失指标、在线状态来源有明确契约。
- 预警、准入、报表、AI、后台管理至少有可验收闭环。
- 大屏和关键页面有截图基线。
- 代码不再靠口口相传维护，后续 Codex 能按文档继续执行。

达到该状态后，可以对旧库场景说：

> 旧库演示和模拟器压测路径下，屎山代码、健壮度、功能完善性和页面空数据问题已经基本解决到可控状态；剩余工作主要是产品级细节、真实手表新库上线和长期性能容量管理。


