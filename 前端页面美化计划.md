# 前端页面美化计划

## 文档定位

本文档用于记录 `D:/Health` 当前前端页面的针对性美化方案，目标不是推倒重做，而是在保留现有业务结构、数据密度和深色科技风识别度的前提下，统一视觉底座、提升信息层级、优化移动端表达，并形成可分阶段落地的执行清单。

本文档基于以下当前事实整理：

- 当前前端仓库：`D:/Health/HealthShow`
- 路由与导航事实源：`HealthShow/src/router/app-routes.mjs`、`HealthShow/src/layout/menu/navigation.mjs`
- 全局样式入口：`HealthShow/src/styles/index.scss`
- 健康监测共享样式：`HealthShow/src/styles/health-monitor.scss`、`HealthShow/src/styles/_hm-vars.scss`
- 重点页面样式：`dashboard.scss`、`realtime.scss`、`safety-command.scss`、`workbench.scss`、`report-center.scss`、`notifications.scss`、`ai-chat.scss`
- 现有快照与审计：`dashboard_snapshot.md`、`real-time-snapshot.md`、`tests/visual/artifacts/`、`tests/api/artifacts/`

## 一、现状判断

### 1. 当前已有可保留的资产

- 全站已经形成了明确的深色科技风基调，主背景、亮青强调色、风险等级语义比较稳定。
- 核心监控页已经不是空白页或通用后台页，`dashboard`、`real-time`、`safety-command`、`risk-warning` 都有较强的指挥舱倾向。
- 页面结构、路由分组、移动端底栏、视觉审计和结构门禁已经具备基础，不需要从零搭建规范。
- 业务内容和数据密度足够高，页面美化的重点应放在“信息组织”和“视觉统一”，而不是额外堆内容。

### 2. 当前主要问题

- 视觉 token 分散：
  - 多个页面分别维护自己的 `$bg`、`$panel`、`$accent`、`$text`、`$dim`
  - 全局与局部对字体、阴影、边框、圆角的定义不统一
- 字体体系混乱：
  - 全局仍以 `Helvetica Neue / Microsoft YaHei / Arial` 为主
  - 数字字体、标题字体、正文字体没有系统拆分
- 页面分层不清：
  - 某些页面是明显的 cockpit 风格
  - 某些页面更像业务工作台
  - 但两类页面当前共享同一种“蓝色卡片堆叠”表达，导致气质混杂
- 导航完成度不够：
  - 移动端底栏仍在使用 emoji 图标
  - 侧栏、顶栏、底栏的 active/hover 语义还没有统一
- 卡片和面板过多：
  - KPI、面板、徽章、边框、发光效果普遍偏重
  - 局部存在“信息很多但主次不够明显”的问题
- 页面之间成熟度不一致：
  - `dashboard / real-time / safety-command / risk-warning` 相对成熟
  - `workbench` 的结构更偏功能完成态，审美完成度明显偏弱
  - `report-center / notifications / ai-chat` 已有方向，但还没完全收口为统一产品语言

## 二、总目标

### 1. 视觉总目标

- 保留当前深色监控产品定位，不改成通用白底 SaaS 模板。
- 统一品牌感，让所有页面都属于同一产品，而不是多套页面风格拼接。
- 让页面在“首屏 3 秒内”能更快看清：
  - 当前页是什么
  - 最重要的数据是什么
  - 下一步能做什么

### 2. 体验总目标

- 核心监控页强调“指挥舱感、态势感、状态感”。
- 业务工作页强调“检索效率、操作效率、长时间使用舒适度”。
- 移动端不只是“能打开”，而是具有完整的导航、标题、筛选、列表和状态表达。

### 3. 技术总目标

- 统一样式 token，减少页面重复维护。
- 抽出共享页面壳和状态组件，避免后续继续各写一套。
- 所有视觉升级必须兼容现有 `audit:structure`、`build`、`audit:visual` 等门禁，不破坏业务链路。

## 三、设计原则

### 1. 不做的事情

- 不把全站改成浅色后台模板。
- 不引入与现有业务无关的大规模炫技动画。
- 不为了“高级感”牺牲信息密度和操作效率。
- 不把所有页面强行做成同一种 cockpit 大屏。

### 2. 必须坚持的原则

- 统一 token，禁止继续在新页面里硬编码整套颜色和边框体系。
- 统一字体层级，数字、标题、正文、说明文字必须有固定规范。
- 统一状态表达，`loading / empty / error / disabled / handled / pending` 必须有全站一致口径。
- 统一导航语义，桌面侧栏、顶部栏、移动底栏都要属于同一设计系统。
- 动效只服务于层级、反馈和状态，不做无意义持续闪烁。

## 四、页面分层策略

当前页面应明确分为两条视觉线，而不是一套样式硬套全站。

### 1. Cockpit 线

适用页面：

- `safety-command`
- `dashboard`
- `real-time`
- `risk-warning`

特征：

- 深色对比更强
- 头部 KPI 更聚焦
- 图表、态势、告警状态优先
- 允许少量呼吸灯、流动线、状态脉冲
- 面板之间更强调“系统态势”

### 2. Workbench 线

适用页面：

- `workbench`
- `employee-archive`
- `employee-profile`
- `mine-entry`
- `report-center`
- `notifications`
- `records`
- `ai-chat`
- `admin` 系列页面

特征：

- 仍保留深色品牌底色
- 减少大面积发光和高压迫边框
- 强调筛选、列表、表单、明细、导出、操作流
- 更适合长时间阅读和批量处理

## 五、分阶段执行计划

## Phase P0：统一视觉底座

目标：先收基础，不直接重做页面。

### P0-1 样式 token 统一

涉及优先文件：

- `HealthShow/src/styles/index.scss`
- `HealthShow/src/styles/health-monitor.scss`
- `HealthShow/src/styles/_hm-vars.scss`
- `HealthShow/src/styles/sidebar.scss`

执行内容：

- 整理全局颜色 token：
  - `--bg-root`
  - `--bg-surface`
  - `--bg-surface-strong`
  - `--border-soft`
  - `--border-strong`
  - `--accent-primary`
  - `--accent-success`
  - `--accent-warning`
  - `--accent-danger`
  - `--text-primary`
  - `--text-secondary`
  - `--text-muted`
- 整理全局结构 token：
  - radius
  - shadow
  - panel header height
  - spacing scale
  - transition duration
- 统一 severity 颜色，不再让不同页面自行解释风险颜色。

### P0-2 字体系统统一

执行内容：

- 为标题、正文、辅助文案、数据数字建立统一规范。
- 数字类内容统一使用一套等宽字体方案。
- 大标题和数据标题统一大小级别，不再逐页随意设置。
- 移除移动端和普通页中低质感的默认系统字体组合。

建议结果：

- 标题字体：统一无衬线主字体
- 数值字体：统一等宽字体
- 文案字体：统一正文无衬线字体

### P0-3 导航壳层统一

涉及优先文件：

- `HealthShow/src/layout/index.vue`
- `HealthShow/src/layout/menu/navigation.mjs`
- `HealthShow/src/styles/sidebar.scss`

执行内容：

- 桌面侧栏重做 active / hover / selected / grouped section 的视觉规则。
- 顶栏、面包屑、用户区与内容区域的边界关系统一。
- 移动端底栏替换 emoji 图标，改为正式图标体系。
- 统一移动端底栏选中态、未选中态和点击反馈。

### P0-4 共享壳组件与页面状态模板

建议沉淀基础组件或共享样式层：

- `PageHeroHeader`
- `MetricStrip`
- `PanelShell`
- `FilterToolbar`
- `StatusChip`
- `PageEmptyState`
- `PageSkeletonState`
- `InlineErrorState`

目标：

- 后续页面改造时不再重复写“蓝色边框卡片 + 标题栏 + 空状态”。

## Phase P1：核心大屏页精修

目标：先把最能代表产品形象的页面做成统一样板。

### P1-1 Dashboard

涉及页面：

- `HealthShow/src/views/health-monitor/dashboard/*`

问题判断：

- 首屏信息量大，但头部 KPI、左侧评估区、中部主体、底部模块的主次关系还能再强化。
- 发光、边框、彩色数值都比较多，容易让注意力平均分散。

改造重点：

- 头部只保留最关键 4 到 6 个 KPI。
- 次级指标下沉到面板内部。
- 面板标题、数值字号、标签位置统一。
- 缩减不必要的边框与 glow，增强“首屏阅读路径”。
- 为移动端定义独立排布，而不是仅靠压缩。

### P1-2 Real-time

涉及页面：

- `HealthShow/src/views/health-monitor/real-time/*`

问题判断：

- 实时列表价值高，但头部 ticker、统计块、筛选区、表格区之间的优先级仍可更清晰。

改造重点：

- 头部 ticker 降噪，只让真正重要的动态信息滚动。
- 筛选栏和状态统计合并成更清晰的工具条。
- 实时表格强化可扫读性：
  - 行高
  - 主字段颜色
  - 状态 chip
  - 更新时间信息密度
- 移动端改为卡片化列表，不强行保完整表格。

### P1-3 Safety Command

涉及页面：

- `HealthShow/src/views/safety-command/*`

问题判断：

- 页面已经具备较强的大屏气质，但发光、动画和信息块之间还可以更克制。

改造重点：

- 压缩视觉噪音，强化指挥状态和核心风险态势。
- 让“安全状态”和“应急动作”比图形装饰更突出。
- 调整 panel 的 hover、动画、数据数字层级，让页面更专业，不像 demo。

### P1-4 Risk Warning

涉及页面：

- `HealthShow/src/views/health-monitor/risk-warning/*`
- `HealthShow/src/views/alert-management/common/*`

问题判断：

- 风险态势、预警列表、趋势图、处理状态已经有基础，但不同模块之间风格还不完全一致。

改造重点：

- 统一风险等级颜色与 handled/pending 语义。
- 优化列表密度与趋势图标题区。
- 强化“可处理、可追踪、可汇总”的产品感。

## Phase P2：业务工作页体系化升级

目标：把“功能完成态页面”升级成“产品完成态页面”。

### P2-1 Workbench

涉及页面：

- `HealthShow/src/views/health-monitor/workbench/*`

问题判断：

- 当前最像功能页，结构清楚但整体偏朴素，与核心 cockpit 页的完成度差距最大。

改造重点：

- 升级为“运营日历工作台”。
- 顶部改为月份总览 + 关键统计 + 快捷动作。
- 日历区域强化选中态、今日态、预警态、无数据态。
- 某一天的详情建议改为更完整的明细区或抽屉，不再只是简单块级补充。

### P2-2 Report Center

涉及页面：

- `HealthShow/src/views/health-monitor/report-center/*`

改造重点：

- 作为 `Workbench` 视觉线模板页之一。
- 强化报表概览卡、导出区、状态提示区之间的层次。
- 统一导出按钮禁用态、无数据态、导出中状态。

### P2-3 Notifications / Records

涉及页面：

- `HealthShow/src/views/alert-management/notifications/*`
- `HealthShow/src/views/alert-management/records/*`

改造重点：

- 统一通知流和处理流的样式语言。
- 把 SLA、处理状态、时间信息做成稳定的信息块。
- 强化筛选、列表、详情动作的一致性。

### P2-4 AI Chat

涉及页面：

- `HealthShow/src/views/ai-chat/*`

问题判断：

- 当前已具备基础科技风，但还是偏“聊天容器”，业务产品感还不够强。

改造重点：

- 顶部增加更明确的“AI 能力说明”和快捷提问入口。
- 把“消息区、结果区、SQL 调试区、导出区”区分得更清楚。
- 强化 AI 结果的结构化展示，而不是只有气泡文本。
- 移动端重点优化输入区、历史消息和结果块的堆叠顺序。

## Phase P3：人员页与后台页收口

目标：统一长表格、检索、表单类页面的可用性。

### 涉及页面

- `employee-archive`
- `employee-profile`
- `mine-entry`
- `device-list`
- `user-list`
- `role`
- `department`
- `job-type`

### 改造重点

- 统一列表页的筛选栏、表格标题区、分页区、空状态。
- 统一详情页的分组标题、属性块、图表块、操作按钮排布。
- 统一后台表单页的字段间距、标签层级、按钮区和错误提示。
- 这些页面应减少 cockpit 式强发光，转向稳健、耐用、长时间阅读友好的表达。

## 六、优先级排序

### P0：底座

- token
- 字体
- 导航
- 共享壳组件
- 页面状态模板

### P1：核心展示页

- `dashboard`
- `real-time`
- `safety-command`
- `risk-warning`

### P2：核心业务工作页

- `workbench`
- `report-center`
- `notifications`
- `records`
- `ai-chat`

### P3：人员与后台页

- `employee-*`
- `mine-entry`
- `admin`

## 七、实施方式

### 1. 推荐推进顺序

- 第一轮：只改全局视觉底座和导航壳层，不碰具体业务页结构
- 第二轮：只精修 4 个核心大屏页
- 第三轮：统一业务工作页
- 第四轮：补齐后台与移动端细节

### 2. 不推荐的推进方式

- 全站同时改
- 每个页面单独发挥
- 先大量换色再回头统一组件

原因：

- 容易产生新的风格漂移
- 很难通过现有门禁快速验证
- 页面之间的视觉债务会继续累积

## 八、验收标准

### 1. 视觉统一

- 全站只有一套品牌主字体和一套数字等宽字体策略。
- 移动端底栏不再使用 emoji 图标。
- 颜色、圆角、阴影、边框、标题条、标签的来源统一。

### 2. 页面体验

- 核心 8 页的 `header / KPI / panel / filter / empty / loading / error` 语言一致。
- Cockpit 页与 Workbench 页有明确区分，但仍属于同一品牌体系。
- 移动端页面不只是“缩小桌面版”，而是有独立的布局取舍。

### 3. 工程验证

每一阶段至少执行：

- `npm run audit:structure`
- `npm run build`
- `npm run audit:visual`

必要时补充：

- `npm run audit:e2e`
- `npm run test:fast`

### 4. 业务不退化

- 不破坏现有数据密度门禁
- 不破坏现有路由分组和结构门禁
- 不影响双库切换、写入语义和核心展示页数据闭环

## 九、建议的第一轮落地内容

如果按最稳妥的方式启动，美化改造第一轮建议只做以下事项：

- 收口全局 token 和字体体系
- 重做侧栏、顶栏、移动端底栏的统一视觉
- 替换移动端 emoji 图标
- 抽出 `PanelShell / MetricStrip / PageHeroHeader / EmptyState`
- 先把 `dashboard` 作为新视觉样板页试跑

第一轮完成后，再决定是否按 `dashboard -> real-time -> safety-command -> risk-warning -> workbench` 的顺序继续推进。

## 十、最终结论

本项目当前前端不需要推倒重来，最合适的路线是：

- 保留深色科技风
- 分成 `Cockpit` 与 `Workbench` 两条页面线
- 先统一视觉底座，再逐页升级
- 先做核心代表页，再做业务页和后台页

这样改造风险最低，也最容易在现有测试门禁下稳定落地。
