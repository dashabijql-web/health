# 安全指挥中心与统一管控重开发实施计划

> 状态：Phase 0 至 Phase 5 已完成；双库接口、结构、视觉、认证、端到端和生产构建门禁已通过。
>
> 编制日期：2026-07-13
>
> 适用范围：`/safety-command/index` 安全指挥中心、`/health-monitor/dashboard` 统一管控，以及两页共用的事件、实时状态、人员、设备和处置能力。

## 实施进度

2026-07-13 已完成第一轮 P0：

- 安全指挥中心的预警查询收敛到当天，并使用后端返回的待处理总数、已处理总数和高危总数，不再把 50 条分页结果当成全量。
- 呼叫、广播、撤离和人员通知在真实能力未接入时改为明确提示“未配置/未发送”，不再显示假成功。
- 两页移动端头部改为标题、控制区和独立 KPI 网格，`390 / 414` 视口不再出现标题、KPI、时间重叠。
- 新增 `HealthShow/tests/safety-command-p0-contract.mjs`，保护以上 P0 约束。
- P0 验收：安全指挥中心和统一管控五档视觉审计通过，P0 契约测试通过，生产构建通过。

下一阶段进入共享事件详情、跨页上下文、责任人/SLA 和真实动作接口，不把页面本地数组作为最终事件状态。

2026-07-13 已完成 Phase 1 第一段：

- 新增 `/command-center/incidents`、`/command-center/incidents/{warningId}` 与 `resolve` 接口，两页均改为消费统一事件数据。
- 事件定位使用 `warningId + occurredAt`；这是分月预警表下的必要组合，不能把可能重复的裸 `id` 当成全局事件编号。
- `realtime` 使用精确 5 分钟窗口，`today` 使用自然日窗口，`range/shift` 必须显式传入开始和结束时间，避免把历史未处理预警冒充当前事件。
- 事件处理从前端自报 `handleBy` 改为后端读取当前登录用户，并在处理后重新查询服务端状态；安全指挥中心不再通过删除本地数组伪造闭环。
- 统一管控可携带 `warningId/occurredAt` 跳至安全指挥中心打开同一事件详情。定位、责任人和 SLA 尚无接入数据时接口明确返回 `UNAVAILABLE/UNASSIGNED/NOT_CONFIGURED`，页面不得把它们显示为已获取。
- 已通过后端单测、前端契约测试、结构门禁、生产构建、实际接口读/详情回读和双页五档视觉审计。

2026-07-13 已完成 Phase 2 至 Phase 5 实施：

- 安全指挥中心首屏不再使用雷达式装饰态势；它改为部门/区域风险关系矩阵和当前优先事件，直接显示人员、位置状态、责任人、SLA 与处置入口。
- 全局“呼叫/广播/撤离”入口已移除。外部动作只能从带 `warningId + occurredAt` 的事件抽屉发起，未配置设备时写审计并返回 `NOT_CONFIGURED`。
- 统一管控直接复用 `IncidentCommandDrawer`，高危待办和健康异常复核都能进入同一事件详情；从抽屉进入安全指挥中心或返回时保留事件、人员、区域上下文。
- 班前准入改为可点击的通过、禁止入井和健康异常复核队列；复检超时、设备数据中断因后端尚未提供事实数据而明确显示“未接入”，不伪造人员或 SLA。
- 设备例外按离线、预警、低电进入设备列表；原路径由 `VITE_SAFETY_COMMAND_V2` 和 `VITE_UNIFIED_CONTROL_V2` 灰度开关保持可回滚，默认启用 V2。
- 灰度开关的 V1 入口已修复为可构建的历史页面。新前端契约测试覆盖共享抽屉、时间定位、未接入状态、准入筛选跳转和回滚开关。
- 最终验证：`audit:api` 53/53、`audit:auth` 7/7、`audit:e2e`、`audit:structure`、两页五档 `audit:visual`、`npm run build` 及后端事件/认证定向单测均通过。

2026-07-13 已完成统一管控两块信息面板收口：

- 原“近30日检测人数”改为“监测覆盖与数据质量”，展示在线作业、全项覆盖、待处理预警和数据更新时间；每项均有明确明细入口或数据窗口说明。
- 原“健康快照”改为“健康异常快照”，保留六项核心体征均值，同时显示未处理异常人数；均值服务趋势识别，异常人数服务现场处置。
- 覆盖率在人员统计没有总人数时回退使用实时监测总人数；血压通用预警只归入高压卡，舒张压预警单独归入低压卡，避免重复计数。
- 新增 dashboard 面板契约测试，保护新标题、异常人数、统一事件数据来源和旧面板下线。
- 本轮验证：`node --test tests/dashboard-summary.mjs tests/command-center-workflow-contract.mjs`、`npm run audit:structure`、`npm run build`、`VISUAL_ROUTES=dashboard npm run audit:visual` 均通过；dashboard 桌面 `1440 / 1707 / 1920`、移动 `390 / 414` 五档通过。

2026-07-14 已完成设备状态面板紧凑化：

- 完整长图复核发现，单纯收紧设备卡只会把空白从设备面板内部挪到双列外部；原布局左列比右列提前结束约 `217px`。
- 顶部双列重新配对为“覆盖/健康”和“准入/闭环”，实测总高分别约 `685px / 690px`；设备概览与设备例外拆成独立横向带，值班决策改为全宽内容带。
- 值班决策内部改为“任务优先级、四类待办、AI 摘要/重点人员”三行，消除队列与辅助卡互相拉高产生的空盒。
- 新增 `npm run audit:visual:dashboard-full`，固定生成桌面 `1440 / 1707 / 1920` 完整长图和面板尺寸数据；顶部双列高度差超过 `24px`、页面总高异常超过 `5000px`、健康快照不是两列或设备卡未铺满面板时直接失败。
- 验证：dashboard 契约测试、`audit:structure`、`npm run build`、五档视觉审计和完整长图审计均通过。

## 1. 目标

把两个“信息很多的大屏”重构成一套面向煤矿值班、调度、健康管理和应急处置的工作系统：

1. 安全指挥中心只负责当前时刻和当前班次的井下安全态势、紧急事件和指挥动作。
2. 统一管控只负责人员健康、班前准入、设备状态、风险待办、责任闭环和趋势分析。
3. 两页使用同一套事件编号、人员、位置、等级、责任人、时限和状态，不再各自计算同一类数字。
4. 呼叫、广播、撤离、派遣、处理等动作必须有真实后端结果；未接入外部设备时必须明确显示“未配置”或“模拟演示”，禁止显示虚假的成功提示。
5. 桌面端服务值班室大屏，移动端服务现场管理和快速处置，两个端都必须可用。

## 2. 当前基线

### 2.1 页面职责现状

| 页面 | 当前入口 | 当前主要内容 | 当前问题 |
| --- | --- | --- | --- |
| 安全指挥中心 | `/safety-command/index` | 井下人数、SOS、跌倒、手表、态势节点、现场处置队列、重点人员、预警趋势 | 与统一管控重复展示健康和预警；应急按钮中有演示逻辑；事件时间范围不清晰 |
| 统一管控 | `/health-monitor/dashboard` | 检测人数、健康快照、班前准入、闭环指挥线、实时预警、值班决策、AI、趋势 | 更像人员健康管控，不足以承担完整的“统一管控”；异常人员和处置责任不够直接 |

### 2.2 已验证问题

历史视觉审计结果（已由后续修复覆盖）：

- 两页桌面 `1440 / 1707 / 1920` 均通过布局检查。
- 两页移动 `390 / 414` 曾失败，原因是顶部标题、KPI 和时间文本重叠；Phase 0 已完成移动头部修复。
- 统一管控审计产物：`HealthShow/tests/visual/artifacts/2026-07-13T06-25-20-415Z/`。
- 安全指挥中心审计产物：`HealthShow/tests/visual/artifacts/2026-07-13T06-49-49-751Z/`。

### 2.3 当前实现风险

1. `safety-command-interactions.js` 中的呼叫、广播、撤离主要是弹窗、提示或本地状态变化，并未形成真实指令下发链路。
2. `SafetyCommandDialogs.vue` 的“紧急联系人”直接显示“暂无联系人”；广播确认只显示成功消息；人员呼叫和通知也只是提示文本。
3. 安全指挥中心使用 `getRiskWarningList({ handled: false, page: 1, size: 50 })`，却把返回列表长度当成待处理数量，存在 50 条上限导致的总数失真。
4. 安全指挥中心把未处理预警、实时在线人数、设备状态和 7 日趋势混在同一页面，时间窗口不统一。
5. 两页分别计算预警、闭环率、在线状态和重点人员，事件处理后存在短时间内数字不一致的可能。
6. 当前页面数字能表达“有多少”，但不能在首屏直接回答“谁、在哪、为什么、谁负责、多久处理、下一步是什么”。

## 3. 目标职责边界

### 3.1 安全指挥中心：实时应急工作台

安全指挥中心首屏只保留影响当前调度决策的内容：

- 当前班次和当前时刻的井下在线人数、分区人数、失联人数。
- SOS、跌倒、长时间静止、生命体征危险、设备失联等实时事件。
- 事件位置、人员、事件持续时间、危险等级和处置时限。
- 处置队列、责任人、派遣状态、通信状态和事件时间线。
- 呼叫、广播、撤离、派遣、确认安全等真实动作。
- 事件影响区域和人员清单。

以下内容移出首屏或降级为辅助信息：全矿平均体征、长期趋势、部门健康排名、AI 健康报告、普通设备统计。

### 3.2 统一管控：健康与准入运营台

统一管控首屏只保留班前和日常健康管理所需内容：

- 班前检测总量、通过、禁止入井、待复核和复检超时。
- 高危人员、异常体征、连续异常、异常趋势和责任部门。
- 设备在线、低电、数据中断、佩戴异常和设备干预待办。
- 预警待办、SLA、责任人、处理状态和处理结果。
- 部门、班组、工种和时间范围的健康统计。
- AI 只作为解释和辅助建议，不作为入井或撤离的唯一依据。

安全指挥中心可以跳转统一管控查看人员健康详情；统一管控可以跳转安全指挥中心查看实时位置和应急状态。

## 4. 统一事件模型

两页必须消费同一套事件模型。建议后端新增或收敛为以下结构，字段名称以现有 Java DTO 命名规范为准：

```text
Incident
  id                  事件唯一编号
  source              WATCH / HEALTH / DEVICE / MANUAL / SYSTEM
  type                SOS / FALL / STILL / VITAL / DEVICE_OFFLINE / ADMISSION / OTHER
  severity            CRITICAL / HIGH / MEDIUM / LOW
  status              NEW / ACKED / DISPATCHED / PROCESSING / RESOLVED / FALSE_ALARM / CLOSED
  occurredAt          发生时间
  lastSeenAt          最近更新时间
  durationSeconds     持续时间
  person              人员编号、姓名、部门、班组、工种
  location            区域、工作面、巷道、定位精度和最后位置时间
  vitalSnapshot       发生时体征快照
  owner               当前责任人和责任部门
  sla                 截止时间、是否超时、剩余时间
  actions             可执行动作及权限
  audit               操作人、操作时间、备注和结果
```

事件状态必须由后端维护。前端只负责展示和发起动作，不能通过删除本地数组来伪造“已处理”。

### 4.1 事件状态流转

```text
NEW -> ACKED -> DISPATCHED -> PROCESSING -> RESOLVED -> CLOSED
  |       |          |             |
  +-------+----------+-------------+--> FALSE_ALARM
```

每次流转都必须写入事件审计记录，并返回新的事件状态、责任人、时间和可用动作。

### 4.2 时间窗口规则

页面必须明确标注并统一传递：

- `realtime`：最近 5 分钟或当前在线状态。
- `shift`：当前班次。
- `today`：自然日。
- `range`：用户选择的日期范围。

不得用“所有历史未处理事件”冒充“当前现场事件”。历史遗留事件必须在页面上标注来源日期和是否影响当前班次。

## 5. 目标页面结构

### 5.1 安全指挥中心首屏

建议固定为四个区域：

1. 顶部态势条：当前班次、井下人数、危险事件数、失联数、通信状态、数据更新时间。
2. 中央态势区：按区域展示人员和事件，支持点击区域查看人员清单，不使用无法解释的装饰性雷达图替代地图或区域关系。
3. 右侧处置队列：按严重程度和 SLA 排序，显示人员、位置、持续时间、责任人、当前状态和主操作。
4. 底部事件时间线：展示事件发生、确认、派遣、广播、处理和关闭的完整记录。

紧急按钮只允许出现在事件上下文中，并显示动作结果：

- 呼叫：联系人、号码或通信设备、发送中、成功、失败。
- 广播：目标区域、广播内容、发送结果和回执。
- 撤离：目标区域、影响人数、二次确认、指令编号和回执。
- 派遣：队伍、负责人、预计到达时间和接受状态。

### 5.2 统一管控首屏

建议固定为四个区域：

1. 顶部健康运营条：今日检测、禁止入井、待复核、高危待办、设备异常、数据更新时间。
2. 左侧准入与健康：班前准入率、禁止入井名单、复检队列和异常体征例外项。
3. 中央闭环区：高危预警、全量待办、设备干预和 SLA，按“最需要动作”排序。
4. 下方趋势与分析：按部门、班组、工种查看异常趋势，AI 作为可展开的解释层。

首屏每个告警数字必须能展开到人员和事件，不能只跳转到一个没有筛选条件的列表页。

## 6. 后端与接口实施方案

### 6.1 先做统一查询服务

在现有 `RiskWarningService`、`RealtimeService`、设备和班前准入服务之上，新增面向页面的查询编排层，避免两个 Controller 各自拼数据。建议命名为 `CommandCenterQueryService` 或拆为：

- `LiveSituationQueryService`
- `IncidentQueueQueryService`
- `AdmissionRiskQueryService`
- `DeviceHealthQueryService`
- `IncidentTimelineQueryService`

返回 typed DTO，不向前端暴露多套 `Map<String,Object>` 行结构。

### 6.2 建议接口

以下为目标接口，不要求一次性全部落地；实施时以现有 Controller 和权限体系为准：

```text
GET  /command-center/live-overview
GET  /command-center/incidents?scope=realtime|shift|today&status=...
GET  /command-center/incidents/{id}
POST /command-center/incidents/{id}/ack
POST /command-center/incidents/{id}/dispatch
POST /command-center/incidents/{id}/resolve
POST /command-center/incidents/{id}/false-alarm
GET  /command-center/incidents/{id}/timeline
POST /command-center/communications/call
POST /command-center/communications/broadcast
POST /command-center/evacuations
GET  /command-center/admission-overview
GET  /command-center/health-exceptions
GET  /command-center/device-exceptions
```

### 6.3 真实动作和模拟动作

每个高风险动作都必须返回统一结果：

```text
actionId
status: ACCEPTED | SENT | ACKNOWLEDGED | FAILED | NOT_CONFIGURED | SIMULATED
message
target
operator
createdAt
failureReason
```

外部广播、电话、定位或撤离设备尚未接入时，前端不得显示“已发送”或“已撤离”。开发环境可以使用 `SIMULATED`，并在界面明确标记“模拟指令”。

所有动作需要：权限校验、幂等键、二次确认、操作审计、失败重试策略和超时状态。

## 7. 前端实施阶段

### Phase 0：基线冻结与数据盘点

交付物：页面字段清单、接口口径表、角色权限表、事件状态图、现有截图基线。

任务：

- 对照 `safety-command-runtime.js`、`use-safety-command-page-data.js`、统一管控 dashboard runtime，列出每个数字的来源、时间窗口和刷新频率。
- 明确“井下人数”“在线人数”“设备在线”“手表在线”的定义，禁止同名不同义。
- 清点现有数据库和 API 是否支持区域、位置、班次、责任人、SLA 和事件时间线。
- 确认哪些动作有真实外部设备，哪些只能做模拟状态。
- 暂不新增视觉装饰，不以截图好看作为阶段完成标准。

完成标准：任何一个顶部 KPI 都能回答“接口来源、查询时间范围、刷新周期、点击后的明细入口”。

### Phase 1：统一事件和实时状态后端

交付物：统一事件 DTO、查询接口、状态流转接口、动作回执、审计表或等价存储。

任务：

- 修复预警总量不能使用分页列表长度的问题，返回 `items + total + pendingTotal + handledTotal`。
- 为未处理事件增加时间窗口和当前班次过滤。
- 统一严重等级和事件类型字典。
- 统一事件处理接口，安全指挥中心和统一管控共用。
- 增加事件操作审计和动作结果。
- 为查询结果增加 `dataAsOf` 或 `updatedAt`，前端显示数据时间。

完成标准：同一事件从两页进入详情时，事件编号、状态、人员、位置、责任人和更新时间一致；处理一次后两个页面最终一致。

### Phase 2：重做安全指挥中心

交付物：实时态势首屏、真实处置队列、事件详情时间线、动作回执和移动端快速处置。

任务：

- 去除或降级长期平均体征和重复趋势卡片。
- 将处置队列改为按严重等级、持续时间和 SLA 排序。
- 用明确的区域/人员关系替代无法解释的装饰性节点。
- 补充当前班次、事件来源、位置更新时间和通信状态。
- 接通事件处理、人员详情、部门责任、派遣和事件时间线。
- 呼叫、广播、撤离按钮全部改为后端动作状态驱动。
- 没有真实设备能力时显示 `未配置` 或 `模拟指令`，不显示假成功。

完成标准：值班员从发现高危事件到确认人员、选择责任人、执行动作、看到回执，最多经过一个事件详情页和一个动作确认步骤。

### Phase 3：重做统一管控

交付物：健康与准入工作台、异常人员清单、设备例外队列、SLA 闭环和跨页跳转。

任务：

- 把班前准入从汇总圆环改成“通过、禁止、待复核、超时”四类可操作队列。
- 高危待办直接显示人员、部门、指标、异常值、最近检测时间和责任人。
- 设备干预按离线、低电、无数据、佩戴异常分类。
- 趋势图只用于识别变化，点击后必须进入已带筛选条件的人员或事件列表。
- AI 摘要显示生成时间、数据范围和“仅供辅助判断”提示。
- 对接统一事件详情，避免使用页面本地数组改变处理状态。

完成标准：值班员不需要先打开多个菜单，就能从高危数字直接进入人员、原因、责任人和处理动作。

### Phase 4：移动端与跨页体验

交付物：移动端任务优先布局、统一底栏、跨页事件上下文保持。

任务：

- 移动端顶部改为两行结构：页面标题/当前状态一行，核心数字横向滚动或折叠一行。
- 禁止标题、KPI、时间和刷新控件共享固定宽度导致重叠。
- 安全指挥中心移动首屏优先显示“最高危事件 + 处理按钮 + 位置 + 联系/派遣”。
- 统一管控移动首屏优先显示“禁止入井/待复核/高危待办”。
- 从一页跳到另一页时保留 `incidentId`、人员、区域和返回入口。
- 所有操作按钮在窄屏下保持可点击尺寸和明确文字。

完成标准：`390px` 和 `414px` 不存在文本重叠、横向溢出、底栏遮挡和不可点击控件。

### Phase 5：灰度切换和旧页面收口

交付物：V2 页面、灰度开关、回滚方案、旧页面下线清单。

任务：

- 保留原路径，使用权限或环境开关切换 V1/V2。
- 先在旧库模拟数据下验证，再在新库空态和真实设备数据下验证。
- 对实际动作接入采用白名单人员和模拟目标进行演练。
- V2 连续通过两轮完整回归后，再下线旧组件和旧重复计算。
- 更新 `AGENTS.md`、`HealthData/DUAL_DB_CUTOVER.md` 或相关事实源，记录新接口和双库行为。

## 8. 测试和验收门禁

### 8.1 数据和接口

- 同一事件在两页显示相同的 `id/status/person/location/updatedAt`。
- `pendingTotal` 不受单页 50 条分页上限影响。
- 当前现场事件不会混入无时间范围的历史未处理事件。
- old/new 数据源切换后，所有事件和人员数据跟随请求头正确切换。
- 停止模拟器并切到新库时，页面不把老库模拟数据显示为真实现场数据。
- 处理、派遣、广播、撤离动作均能验证成功、失败、未配置和模拟状态。

### 8.2 功能流程

- 高危事件：发现 -> 查看人员 -> 查看位置 -> 确认 -> 派遣 -> 处理 -> 关闭。
- SOS 事件：发现 -> 联系人/通信目标 -> 呼叫或广播 -> 回执 -> 时间线记录。
- 禁止入井：发现人员 -> 查看异常指标 -> 复核 -> 通过/禁止 -> 记录操作人。
- 设备异常：离线/低电/无数据 -> 设备详情 -> 责任人 -> 处理结果。
- 两页互跳后返回，筛选条件和事件上下文不丢失。

### 8.3 视觉和响应式

必须执行：

```bash
node scripts/with-env.mjs VISUAL_ROUTES=safety-command,dashboard -- npm run audit:visual
npm run audit:structure
npm run audit:api
npm run audit:auth
npm run audit:e2e
npm run build
```

必要时分别运行单页审计并检查最新 `layout-summary.md` 和 PNG。验收视口至少包括：

- 桌面：`1440 / 1707 / 1920`
- 移动：`390 / 414`

禁止以“桌面截图通过”代替移动端验收。

## 9. 优先级

### P0：上线前必须解决

- 两页移动端文字重叠和核心操作不可用。
- 呼叫、广播、撤离不能再显示假成功。
- 预警总量、闭环率、当前事件时间窗口统一。
- 同一事件的处理状态在两页一致。
- 高危事件必须展示人员、位置、持续时间、责任人和 SLA。
- 新旧库和模拟器数据源不能混淆。

### P1：第一轮重开发完成

- 安全指挥中心实时态势和处置时间线。
- 统一管控班前准入与健康异常队列。
- 责任人、派遣、通知和动作回执。
- 事件详情跨页复用。
- 事件和动作审计。

### P2：第二轮增强

- 区域地图或更准确的人员定位可视化。
- 通信设备、广播系统、定位系统的真实适配。
- AI 风险解释、班组画像和趋势预测。
- 值班日报、交接班摘要和历史复盘。

## 10. 资源和风险

### 需要提前确认的外部能力

- 是否有真实的电话、对讲、广播、定位和撤离系统接口。
- 是否能提供区域、工作面、巷道和人员定位数据。
- 谁是紧急广播和撤离动作的最终授权角色。
- 事件关闭是否需要安全负责人复核。

### 主要风险

- 没有真实设备接口时，页面只能做状态编排，不能宣称已经完成现场指令。
- 旧库模拟数据可能造成“井下人数、SOS、预警”看起来互相矛盾，必须在页面标识数据源和模拟状态。
- 双库切换会影响事件总量和人员清单，必须在 API 响应和页面状态中明确数据源。
- 事件状态改为后端统一维护后，会影响现有预警通知、记录、报表和 AI 报告，需要同步回归。

## 11. 最终验收标准

只有同时满足以下条件，才认为两个页面重开发完成：

1. 安全指挥中心能够完成一条真实或明确标注为模拟的应急处置链，并给出动作回执和审计记录。
2. 统一管控能够完成一条班前准入或健康异常闭环，并显示责任人和处理时限。
3. 两页看到的同一事件数据一致，处理一次后状态一致。
4. 页面不再把历史未处理事件误标为当前井下事故。
5. 两个页面的职责和菜单名称让一线职工、调度员和值班领导能够快速理解。
6. 桌面和移动五档视口均通过布局审计，无重叠、溢出和底栏遮挡。
7. `audit:api`、`audit:auth`、`audit:e2e`、`audit:structure`、`audit:visual` 和 `build` 全部通过。
8. 旧页面和旧重复计算逻辑有明确下线时间，不以两个版本长期并存作为最终状态。

## 12. 建议执行顺序

```text
数据口径与事件模型
        |
        v
统一查询与状态流转接口
        |
        +--> 安全指挥中心：实时应急与动作回执
        |
        +--> 统一管控：准入健康与运营闭环
        |
        v
共享事件详情、跨页上下文、移动端布局
        |
        v
双库/模拟器/真实动作验收
        |
        v
灰度切换并下线旧页面
```

第一轮实施不应从改颜色、换图表或增加 AI 卡片开始。必须先完成 P0 的事件口径、真实动作状态、移动端骨架和跨页闭环，否则视觉上的“更完整”只会放大现场误判风险。
