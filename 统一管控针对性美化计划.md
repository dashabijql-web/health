# 统一管控针对性美化计划

## 文档定位

本文只针对 `HealthShow` 的统一管控页，即：

- 页面路由：`/health-monitor/dashboard`
- 页面入口：`/home/j/code/health/HealthShow/src/views/health-monitor/dashboard/index.vue`

目标不是再写一份泛化“前端美化方向”，而是把统一管控页当前真实代码、视觉债务、文件责任和验收命令收成一份可以直接执行的专项计划。

本计划沿用 `design-taste-frontend` 可迁移约束，但只保留适合本仓库 `Vue 3 + SCSS + Element Plus + ECharts` 的部分：

- 单主强调色
- 去紫色和霓虹化表达
- 首屏阅读路径必须清晰
- 动效只做轻量状态反馈，不做演示型特效
- 移动端优先保证头部、KPI、主面板和底部导航不冲突

## 一、当前代码事实

统一管控页当前已经完成了第一轮壳层收口，但还没有完成第二轮精修。

### 1. 已经具备的基础

- `index.vue` 已接入共享壳：
  - `PageHeroHeader`
  - `MetricStrip`
- 最近一轮专项视觉审计已通过：
  - `dashboard` 在 `desktop-1440 / desktop-1920 / mobile-390 / mobile-414` 全通过
  - 结果见 `HealthShow/tests/visual/artifacts/2026-05-19T09-41-49-244Z/layout-summary.md`
- `dashboard.scss` 已补齐环境图表高度，移动端 breadcrumb 冲突也已修掉

### 2. 仍然明显未收口的部分

虽然视觉门禁已过，但页面内部仍然存在明显的“新壳层 + 旧 cockpit 表达”混搭：

- `PageHeroHeader + MetricStrip` 已是新语法
- 主体区仍大量依赖旧 `dm-panel / dm-ph / dm-*` 体系
- 多个区块仍在模板内使用内联颜色和内联 `cursor/style`
- 面板层级虽然能看懂，但“左看态势、中看决策、右看行动”的路径还不够稳定
- 右侧栏仍混入 `HealthTips` 这类偏轻内容，和统一管控页的值班指挥气质不完全匹配

### 3. 本页当前关键文件

- 页面装配：
  - `HealthShow/src/views/health-monitor/dashboard/index.vue`
- 页面样式：
  - `HealthShow/src/views/health-monitor/dashboard/dashboard.scss`
- 页面计算和视图模型：
  - `HealthShow/src/views/health-monitor/dashboard/dashboard-computed.js`
  - `HealthShow/src/views/health-monitor/dashboard/dashboard-view-model.js`
  - `HealthShow/src/views/health-monitor/dashboard/dashboard-summary.js`
- 图表配置：
  - `HealthShow/src/views/health-monitor/dashboard/dashboard-chart-options.js`
  - `HealthShow/src/views/health-monitor/dashboard/dashboard-chart-methods.js`
- 关键子组件：
  - `components/DashboardDispatchPanel.vue`
  - `components/DashboardDevicePanel.vue`
  - `components/DashboardRightSidebar.vue`
  - `components/DashboardWarningStream.vue`
  - `components/DashboardDialogs.vue`

## 二、专项目标

统一管控页这一轮不追求“更炫”，只追求四个结果：

1. 首屏路径稳定
2. cockpit 气质统一
3. 决策区优先级更高
4. 移动端信息密度降噪

翻译成页面语言，就是：

- 用户打开后 3 秒内能看清“当前态势、需要处理什么、下一步点哪里”
- 页面不再同时出现 hero 新语言、旧后台标题条、装饰型卡片三套话语体系
- 中间决策带是绝对主角，左侧态势和右侧行动是支撑，不再彼此抢焦点
- 手机端保留标题、关键 KPI、预警流和关键行动，不再把桌面心智原样压缩

## 三、本页主要设计债务

### 1. 头部已新，主体未新

当前最明显的问题不是头部本身，而是 hero 下方主体区还保留较多旧监控页质感：

- `dm-ph` 标题条仍然普遍偏旧
- 说明文字、彩条、强调方式没有统一成同一代 cockpit 语法
- 面板之间的“视觉重量”仍然比较平均，导致主次不够鲜明

### 2. 中栏是核心，但还不够像核心

中栏当前实际承担了最重要的信息：

- 值班决策面板
- 实时预警流
- 趋势图
- 时段分布

但视觉上仍然存在两类问题：

- 决策面板和普通统计面板的差异不够大
- 趋势与预警流的组合方式仍偏“拼块”，不够像统一工作台

### 3. 右栏内容结构合理，但内容气质偏杂

右栏有价值的内容包括：

- 异常人员排行
- 指标预警率分析
- 班前健康准入
- AI 全矿健康分析

但现在还混有：

- `HealthTips`

这会削弱页面作为统一管控 cockpit 的专业感和值班态。

### 4. 颜色与状态语义还可以再收紧

当前页面总体方向已经从旧紫色风格拉回来了，但仍需要进一步收口：

- 主强调继续固定为冷青蓝
- `danger / warning / success` 只服务状态语义
- 不再让单个模块再定义自己的“主题色世界观”

## 四、范围边界

本计划只处理统一管控页的表现层和装配层，不扩散到以下范围：

- 不重做接口契约
- 不改双库逻辑
- 不改 `dashboard-runtime.js` 的业务取数语义，除非视觉方案逼出非常小的派生字段调整
- 不把 `real-time`、`safety-command`、`risk-warning` 一起拖进同一轮

## 五、分阶段执行计划

## Phase D1：头部与首屏 KPI 二次精修

### 目标

让 hero 和 KPI 不只是“能用”，而是真正成为统一管控页的首屏指挥带。

### 文件

- `dashboard/index.vue`
- `dashboard.scss`
- `src/components/health-shell/PageHeroHeader.vue`
- `src/components/health-shell/MetricStrip.vue`

### 动作

- 缩短 hero 描述句，避免它和 KPI 抢层级
- 把 `meta` 信息明确固定成：
  - 当前运行状态
  - 当前时间
  - 刷新状态
- 把顶部 KPI 收成“态势摘要”，不再像二级导航
- KPI note 文案统一为短句，不再混长短口径
- 移动端下只保留最关键的 4 到 6 个态势指标

### 完成标准

- 首屏打开后，标题、时间、刷新、KPI 的阅读顺序不需要来回扫视
- 顶部区域不再出现“说明过长、KPI 过密、动作过散”的问题

## Phase D2：三栏权重重排

### 目标

把“左态势、中决策、右行动”变成真正稳定的阅读路径。

### 文件

- `dashboard/index.vue`
- `dashboard.scss`

### 动作

- 左栏只承接基础态势：
  - 体征健康评估
  - 部门综合看板
- 中栏明确为唯一主列：
  - 检测人数概览
  - 值班决策面板
  - 健康监测中心
  - 设备状态
  - 环境健康关联
- 右栏只保留行动和辅助判断：
  - 排行
  - 预警率
  - 班前准入
  - AI 分析
- 通过面板尺寸、留白和标题样式拉开主次，不再靠加更多颜色

### 完成标准

- 桌面端第一眼能看出“中栏是主战场”
- 左右栏不会再和中栏争夺主视觉

## Phase D3：中栏核心区统一成一个决策工作台

### 目标

把当前“决策面板 + 预警流 + 趋势图 + 时段分布”从拼块改成统一 cockpit 语言。

### 文件

- `dashboard/index.vue`
- `dashboard.scss`
- `components/DashboardDispatchPanel.vue`
- `components/DashboardWarningStream.vue`

### 动作

- 提升 `DashboardDispatchPanel` 的视觉权重：
  - 标题、摘要、动作按钮更像主控区
  - 次级描述收短
- `DashboardWarningStream` 从“滚动列表”升级为“实时事件带”：
  - 处理态、待处理态、危险态的层级更明确
  - 列表标题和总数表达统一
  - 去掉模板内零散的内联颜色
- 趋势图和时段分布标题统一语法：
  - 主标题
  - 辅助说明
  - 右上角轻操作
- 健康监测中心内部不再像三个独立卡片，而是同一工作台下的不同子区

### 完成标准

- 中栏读起来像一个连续的决策面，而不是四个互不相干的模块
- 危险事件、趋势变化、可执行动作三者的关系更直观

## Phase D4：右栏去杂质，强化行动语义

### 目标

把右栏从“信息拼盘”收成“行动辅助栏”。

### 文件

- `components/DashboardRightSidebar.vue`
- `dashboard.scss`
- 视情况替换 `src/components/HealthTips.vue` 的统一管控场景使用

### 动作

- 移除统一管控页中的 `HealthTips`，替换为更专业的区块，二选一：
  - `值班提示`
  - `处置建议`
- 排行、预警率、班前准入、AI 分析四块统一标题和操作语法
- `AI 全矿健康分析` 入口更像行动按钮，不再像普通折叠卡片
- 班前准入块强化“是否需要介入”的判读感

### 完成标准

- 右栏不再出现“内容有用但风格不统一”的问题
- 值班人员能直接从右栏找到下一步入口

## Phase D5：颜色、图表和状态口径总收口

### 目标

把 dashboard 页面内所有状态表达拉回同一规则。

### 文件

- `dashboard.scss`
- `dashboard-chart-options.js`
- `dashboard-warning-level.js`
- `dashboard-summary.js`

### 动作

- 统一状态色：
  - `primary`：冷青蓝
  - `danger`：红
  - `warning`：橙黄
  - `success`：绿
- 去掉页面内部零散自定义的强调色漂移
- 图表 legend、tooltip、bar/line 强调色口径统一
- badge、tag、bar、summary 数字的颜色映射由同一套规则控制

### 完成标准

- 页面内同一语义不会出现两套颜色
- 压力相关不再单独形成紫色视觉分支

## Phase D6：移动端专门降噪

### 目标

桌面布局不直接压缩到手机，而是保留统一管控页最有价值的 3 到 4 段内容。

### 文件

- `dashboard.scss`
- `dashboard/index.vue`
- `components/DashboardWarningStream.vue`
- `components/DashboardDevicePanel.vue`

### 动作

- 移动端首屏只保留：
  - hero
  - 核心 KPI
  - 值班决策面板
  - 实时预警流
- 部门综合看板、环境关联、部分设备状态往下移，不抢首屏
- 预警列表卡片进一步加大可点击区域
- 保证与底部导航安全区持续兼容

### 完成标准

- 手机端打开时不需要先滚动才能理解页面
- 首屏只呈现“看态势、看预警、去处理”三件事

## 六、文件责任图

### 1. 页面装配层

- `dashboard/index.vue`

职责：

- 只负责三栏结构、共享壳装配和模块顺位
- 不继续在模板中堆叠大量视觉内联样式

### 2. 页面样式层

- `dashboard.scss`

职责：

- 统一 dashboard 的 layout、panel、section、responsive 规则
- 收掉旧式霓虹、重复彩条、过密标题条

### 3. 面板子组件层

- `DashboardDispatchPanel.vue`
- `DashboardWarningStream.vue`
- `DashboardRightSidebar.vue`
- `DashboardDevicePanel.vue`

职责：

- 子组件只处理各自信息块的密度和交互
- 不各自再定义一套主题语言

### 4. 图表与状态层

- `dashboard-chart-options.js`
- `dashboard-warning-level.js`
- `dashboard-summary.js`

职责：

- 统一颜色、tooltip、legend、状态等级口径
- 避免视图模板再散落硬编码颜色逻辑

## 七、执行顺序建议

按性价比和回归风险，建议顺序固定为：

1. `D1 头部与 KPI`
2. `D2 三栏权重`
3. `D3 中栏工作台`
4. `D4 右栏行动语义`
5. `D5 颜色与图表口径`
6. `D6 移动端降噪`

原因：

- 先做头部和结构，后做局部面板，返工最少
- 先稳定桌面主次，再压缩移动端，更不容易反复改

## 八、验收命令

每个阶段结束后，至少跑以下命令：

```bash
cd /home/j/code/health/HealthShow
npm run build
npm run audit:structure
node --test tests/dashboard-dialogs-level.mjs tests/dashboard-runtime-imports.mjs tests/dashboard-runtime-handled.mjs tests/dashboard-summary.mjs tests/dashboard-warning-level.mjs tests/dashboard-warn-curve.mjs
node scripts/with-env.mjs VISUAL_ROUTES=dashboard -- npm run audit:visual
```

重点验收结果：

- `build` 通过
- `audit:structure` 不回退
- dashboard 专项 node 测试通过
- `audit:visual` 中 `dashboard` 四个 viewport 全通过

## 九、本轮不做的事

这份计划明确不包含以下内容：

- 不改后端 dashboard 接口
- 不重写 dashboard 整页 runtime
- 不把统一管控页直接迁成另一套全新组件体系
- 不为“更高级”引入与当前仓库不一致的新 UI 库
- 不把 `HealthTips` 的全局存在一次性全删，只处理它在统一管控页的使用场景

## 十、最终目标态

统一管控页完成这一轮后，应达到下面这个目标态：

- 桌面端像一个真正的值班 cockpit，而不是数据卡片拼贴页
- 移动端像一个可以快速判断态势并进入处理流程的轻指挥页
- 页面内所有状态表达都属于同一套视觉语言
- 新共享壳和旧 dashboard 主体不再割裂
