# 前端页面美化接力计划

## 当前状态

- `dashboard` 统一管控页已完成本轮专项美化，且 `audit:visual` 已通过。
- 当前仓库已经是单仓（monorepo）工作流，根目录是唯一 git 入口。
- 下一个 Codex 不要再从 `dashboard` 从头开始，除非出现明确回归。

## 已完成，不再重复

- `dashboard` 的 cockpit 线视觉收口。
- `dashboard` 的桌面 / 移动端视觉审计清零。
- 单仓切换、根仓 `AGENTS.md` 护栏和 monorepo 远端首推。

## 现在真正还没收口的计划

当前剩余工作不是“再做一次 dashboard”，而是把其他核心页面收进同一条视觉语言里。

优先级按下面顺序继续：

1. `risk-warning`
2. `real-time`
3. `safety-command`
4. `alert-management/notifications`
5. `alert-management/records`
6. `report-center`
7. `ai-chat`
8. `workbench`

## 下一位 Codex 的执行目标

### 1. 先补 Cockpit 线一致性

目标页面：

- `risk-warning`
- `real-time`
- `safety-command`

要做的事：

- 统一页头、说明文案、状态标签和移动端折叠策略。
- 统一主色、面板层级、标题区与动作区的语法。
- 避免再次做出一套和 `dashboard` 不同的 cockpit 语言。

优先文件：

- `HealthShow/src/views/health-monitor/risk-warning/*`
- `HealthShow/src/views/health-monitor/real-time/*`
- `HealthShow/src/views/safety-command/*`
- `HealthShow/src/components/health-shell/*`
- `HealthShow/src/layout/*`

### 2. 再补 Workbench / 列表线一致性

目标页面：

- `alert-management/notifications`
- `alert-management/records`
- `report-center`
- `ai-chat`
- `workbench`

要做的事：

- 统一筛选区、导出区、空态、禁用态、批量动作区。
- 统一列表页顶部信息结构，不再各写各的。
- 让 AI / 报表 / 预警列表共享同一条 workbench 语法。

优先文件：

- `HealthShow/src/views/alert-management/notifications/*`
- `HealthShow/src/views/alert-management/records/*`
- `HealthShow/src/views/health-monitor/report-center/*`
- `HealthShow/src/views/ai-chat/*`
- `HealthShow/src/views/health-monitor/workbench/*`

## 不要做的事

- 不要重做 `dashboard`。
- 不要把美化任务扩散成后端接口重构。
- 不要为了局部页面好看再拆一套新的视觉系统。
- 不要改双库语义去迁就页面视觉。

## 每个页面的验收规则

每做完一页，至少跑这三项：

```bash
npm run build
npm run audit:visual
npm run audit:page-structure
```

如果只改了某个页面簇，优先跑定向视觉审计：

```bash
VISUAL_ROUTES=<route-slug> npm run audit:visual
```

建议顺序：

1. 先把 `risk-warning` 跑通。
2. 再收 `real-time` 和 `safety-command`。
3. 然后收 `alert-management/*`、`report-center`、`ai-chat`。
4. 最后看 `workbench` 是否还需要第二刀。

## 交接口径

下一位 Codex 只需要理解一句话：

> `dashboard` 已经是稳定样板页，后续任务是把其他核心页面按同一条视觉语言收口，而不是重新发明新的 dashboard。

