# HealthShow 健康管理平台 — 全面优化整改计划

**审计日期：** 2026-03-15
**审计范围：** 全部页面 + 全部可交互元素
**审计方式：** Playwright 自动化浏览器探索（96次操作）
**文档状态：** 待审核

---

## 一、问题汇总速览

| 优先级 | 数量 | 说明 |
|--------|------|------|
| 🔴 Critical（功能完全失效） | 4 | 必须立即修复 |
| 🟠 High（严重影响使用） | 6 | 本周内修复 |
| 🟡 Medium（影响工作流） | 6 | 下个迭代修复 |
| 🟢 Low（细节/体验优化） | 8 | 排期修复 |

---

## 二、Critical 级别问题（功能完全失效）

### C-1：预警"确认处理"接口返回 500 错误

**位置：** 统一管控 Dashboard → 右侧预警列表 → 点击行 → 详情弹窗 → "确认处理"按钮

**现象：** 点击"确认处理"后，后端返回 HTTP 500，弹窗不关闭，预警状态永远停留在"待处理"。这是整个系统**核心工作流**的致命缺陷，导致：
- 安全指挥中心 今日处理率 = 0%
- 预警记录页 总处置率 = 0%
- 所有预警永远无法被标记为已处理

**排查路径：**
```bash
# 1. 查后端日志，找 500 的 stacktrace
grep -n "ERROR\|Exception" D:/HealthData/logs/backend.log | tail -50

# 2. 检查前端调用
# src/api/risk-warning.js → 找处理接口
# src/views/alert-management/records/index.vue → handleProcess 方法
```

**修复方案：**
1. 查后端 `RiskWarningController` 的 handle/confirm 接口是否存在
2. 检查 `WarningRecordMapper` 的 UPDATE 语句是否正确映射字段
3. 检查传入的 `warningId` 类型（Long vs String vs Integer）
4. 修复后需完整重启后端（Mapper SQL 修改）

**关联影响：** H-2（安全指挥中心处理率）、H-3（预警记录KPI）修复后自动改善

---

### C-2：心率页"查看健康画像"跳转参数错误

**位置：** `src/views/health-monitor/heart-rate/index.vue` → 员工详情弹窗 → "查看健康画像"按钮

**现象：** 跳转到 `/health-monitor/employee-profile?name=林凯`，但健康画像页读取的是 `empCode` 参数，导致页面显示空状态："请从职工列表点击姓名进入健康画像"，所有指标显示 "--"。

**修复方案：**
```javascript
// 当前错误代码（heart-rate/index.vue）
router.push({ path: '/health-monitor/employee-profile', query: { name: row.name } })

// 修复后
router.push({ path: '/health-monitor/employee-profile', query: { empCode: row.empCode } })
```

**需同步检查：** 血氧、血压、睡眠等页面是否有相同的跳转逻辑，统一修复。

---

### C-3：血氧页"检测率"始终显示 0%

**位置：** `src/views/health-monitor/blood-oxygen/index.vue` → KPI 卡片首位

**现象：** 1000名员工有完整数据，但"检测率"显示 0%。其他三个指标（平均血氧、正常率、异常数）均正常显示。

**排查路径：**
```javascript
// 检查前端字段映射
// blood-oxygen/index.vue 中 loadKpi() 方法
// 确认 API 返回字段名 vs 前端读取字段名
// 常见问题：后端返回 detectionRate，前端读 detectRate

// 检查后端 SQL
// BloodOxygenMapper.java → getOverviewStats 或类似方法
// 检测率通常 = 有数据的员工数 / 总员工数
```

**修复方案：** 对齐前后端字段名，或修复后端检测率计算逻辑（可能除以了错误的分母）。

---

### C-4：睡眠页 `d.map is not a function` 崩溃

**位置：** `src/views/health-monitor/sleep/index.vue` → `loadQualityDist()` → `initScore(d)`

**现象：** 页面加载时 `getSleepQualityDistribution()` API 返回对象而非数组，`d.map(x => x.label)` 报 TypeError 崩溃，整个睡眠分析页面无法使用。

**修复方案（前端防御性处理）：**
```javascript
// sleep/index.vue 中 initScore() 函数开头添加
function initScore(d) {
  if (!Array.isArray(d)) {
    console.warn('[sleep] initScore received non-array:', d)
    d = []
  }
  // ... 原有逻辑
}
```

**同步修复：** 睡眠阶段饼图显示 100% "梦境"（REM）问题——页面不可用时应显示"暂无数据"空态，而非错误的默认图表。

---

## 三、High 级别问题

### H-1：Dashboard 滚动定时器控制台刷屏

**位置：** `src/views/health-monitor/dashboard/index.vue` → 右侧预警列表滚动逻辑

**现象：** 当预警列表可滚动高度 ≤ 83px 时，滚动 interval 每 100ms 执行一次，不断打印 `[scrollInterval] reached bottom`，导致控制台每秒 10+ 条日志，浪费 CPU。

**修复方案：**
```javascript
// 添加最小可滚动高度检查
const startScroll = () => {
  if (scrollEl.scrollHeight - scrollEl.clientHeight < 50) {
    // 内容不足以滚动，不启动定时器
    return
  }
  // 原有 interval 逻辑
}

// 到底后增加防抖，延迟 2 秒后再翻页，避免频繁切换
```

---

### H-2：安全指挥中心今日处理率 0%

**位置：** `src/views/safety-command/components/KpiCardRow.vue`

**现象：** 处理率统计依赖已处理的预警数量，由于处理接口全部报 500（见 C-1），处理数永为 0。**修复 C-1 后此问题自动解决。**

---

### H-3：预警记录页 KPI 统计卡均显示 0

**位置：** `src/views/alert-management/records/index.vue` → `loadOverview()` 方法

**现象：** 今日预警=0、待处理=0、危急预警=0、处理率=0%，但表格中有 7672 条真实记录，时间戳均为近期。KPI 统计与表格数据严重不符，完全丧失可信度。

**排查路径：**
```javascript
// 1. Network 面板检查 /risk-warning/overview 接口是否被调用
// 2. 检查接口返回的 JSON 结构与前端字段映射是否一致
// 3. 检查是否遗漏了日期范围参数（导致后端查询返回空）
```

---

### H-4：ECharts 废弃 API 警告（全图表页）

**位置：** 所有含图表的页面

**现象：** 控制台持续输出 `DEPRECATED: textStyle hierarchy` 和 `DEPRECATED: name property in rich`，这是 ECharts v5 对 v4 配置项的警告。当前不影响显示，但未来版本升级后将直接报错。

**修复范围：**
- `src/components/HeartRateWave.vue`
- `src/views/health-monitor/heart-rate/index.vue`
- `src/views/health-monitor/blood-oxygen/index.vue`
- `src/views/health-monitor/sleep/index.vue`
- `src/views/safety-command/` 下的图表组件

**修复方式：** 将 `series[].label.normal.textStyle.color` 改为 `series[].label.color`；将 `rich` 中的 `name` 属性按 v5 规范重命名。

---

### H-5：Element Plus 表格 `Invalid prop` 警告（全表格页）

**位置：** 所有含 `el-table` 的页面

**现象：** 每个表格行渲染时触发 `Invalid prop: validation failed for prop "width"` 或 `align` 的警告，实时监控 1000 行员工时每次刷新触发 1000+ 条警告，严重污染控制台，影响调试效率。

**修复方案：** 审查所有 `el-table-column` 的 `width` 属性，确保传入字符串（如 `"80"`）而非数字（如 `80`）；检查 `align` 值是否为 Element Plus 允许的 `'left' | 'center' | 'right'`。

---

### H-6：工作台日历大量日期无数据

**位置：** 工作台日历页面

**现象：** 3月仅 1-5 日、12-15 日有数据，其余日期全显示 "--"，可能是模拟器未连续运行导致的数据空洞。

**改进方案（两步走）：**
1. **短期：** 在日历空白格内显示"无数据"小标签，区别于"加载中"状态
2. **长期：** 检查模拟器运行稳定性，确保持续产生历史数据

---

## 四、Medium 级别问题

### M-1：预警阈值配置各风险级别显示相同数据

**位置：** `src/views/alert-management/config/index.vue` → 低危/中危/高危 标签页

**现象：** 切换标签时三个风险级别的阈值完全一致，不符合业务逻辑（高危岗位应有更严格的阈值）。

**排查：** 检查标签切换时是否带 `riskLevel` 参数重新请求 API，或是否所有标签共用同一份数据。

---

### M-2：预警记录"危急预警"筛选卡逻辑错误

**位置：** `src/views/alert-management/records/index.vue` → `filterByCard('critical')` 方法

**现象：** 点击"危急预警"统计卡，代码设置 `searchForm.warningLevel = 'critical'`，但下拉选项值为中文 `'高危'/'中危'/'低危'`，英文值无法匹配，过滤器实际无效。

**修复：**
```javascript
// 当前错误
filterByCard('critical') → searchForm.warningLevel = 'critical'

// 修复后（方案一：直接映射）
filterByCard('critical') → searchForm.warningLevel = '高危'

// 修复后（方案二：定义枚举映射，更易维护）
const LEVEL_MAP = { critical: '高危', warning: '中危', info: '低危' }
filterByCard(type) → searchForm.warningLevel = LEVEL_MAP[type]
```

---

### M-3：血压页 `filterDept` 未定义 Vue 警告

**位置：** `src/views/health-monitor/blood-pressure/index.vue`

**现象：** `chartPage.js` mixin 的 `filteredList` 计算属性引用 `this.filterDept`，但血压页 `data()` 中未定义此属性，导致 Vue 每次渲染时发出警告。

**修复方案（推荐）：** 将 `filterDept: ''` 移入 `chartPage.js` mixin 的 `data()` 中，彻底消除所有页面的此类警告，一次解决根本问题。

```javascript
// src/mixins/chartPage.js 的 data() 中添加
data() {
  return {
    filterDept: '',  // 添加此行
    // ... 其他已有数据
  }
}
```

---

### M-4：实时监控员工行大量指标显示 "--"

**位置：** `src/views/health-monitor/real-time/index.vue` → 实时数据表格

**现象：** 多行员工的所有体征指标均为 "--"，视觉上像数据异常。实际是设备类型只发送部分指标，行为正确但用户不知道。

**改进方案：**
- 为 "--" 单元格添加 `el-tooltip`："此员工设备暂未上报该项指标"
- 或在表格末列添加"最后上报时间"，帮助区分"设备无此功能"与"设备离线"

---

### M-5：健康画像 AI 诊断按钮无加载状态

**位置：** `src/views/health-monitor/employee-profile/index.vue` → AI 健康诊断报告按钮

**现象：** 点击 AI 诊断后，DeepSeek API 响应需要 3-8 秒，期间按钮无任何加载状态，用户可能重复点击，造成重复请求。

**修复：**
```javascript
// 点击后立即禁用按钮并显示加载状态
isAiLoading.value = true
try {
  await fetchAiReport()
} finally {
  isAiLoading.value = false
}
```

```html
<el-button :loading="isAiLoading" :disabled="isAiLoading">AI 健康诊断</el-button>
```

---

### M-6：滚动图表不随窗口 resize 重绘

**位置：** 各健康监测页面的 ECharts 图表

**现象：** 窗口缩放或侧边栏收起/展开时，ECharts 图表不自动 resize，出现空白区域或内容错位。

**修复（在 chartPage.js 或各页面 mounted 中添加）：**
```javascript
mounted() {
  window.addEventListener('resize', this.handleResize)
},
unmounted() {
  window.removeEventListener('resize', this.handleResize)
},
methods: {
  handleResize() {
    this.chartInstances.forEach(chart => chart?.resize())
  }
}
```

---

## 五、Low 级别问题

### L-1：登录页凭据明文显示

**位置：** `/login` 页面表单下方

**现象：** 明文显示 `username: admin  password: admin123`，存在安全隐患。

**修复：** 改为折叠的"开发环境提示"（可展开），或生产环境下彻底移除。

---

### L-2：操作按钮使用 Emoji 表情符号

**位置：** 实时监控页 → 操作列 → 💬（消息）、🔊（广播）按钮

**现象：** Emoji 在不同操作系统/浏览器渲染不一致，且对屏幕阅读器不可访问。

**修复：** 替换为 Element Plus 图标组件（`el-icon`），并添加 `aria-label` 属性。

---

### L-3：在线用户数显示 0

**位置：** 后台管理 → 用户列表 → KPI 卡

**现象：** 当前已登录 admin，但"在线用户"显示 0。

**修复：** 检查在线状态心跳逻辑（如 Redis session key 有效期），或调整更新频率。

---

### L-4：部门管理负责人/描述列为空

**位置：** 后台管理 → 部门管理 → 表格

**现象：** 20个部门的"负责人"和"部门描述"列全部为空，占据表格宽度但无用。

**修复：** 未填写时隐藏该列（使用 `v-if`），或在系统初始化时要求填写部门信息。

---

### L-5：阈值配置压力指数单位缺失

**位置：** `src/views/alert-management/config/index.vue` → 压力指数卡片

**现象：** 显示 `单位:` 后无内容，其他指标均有单位（bpm、%、°C、mmHg）。

**修复：** 补充压力指数单位字符串（如 `idx` 或 `分`）。

---

### L-6：全局缺少 loading 状态

**位置：** 所有数据页面

**现象：** 后端响应慢或重启时，图表和表格直接显示空态，无任何加载提示，用户无法区分"加载中"与"无数据"。

**修复：** 为所有 API 调用添加 `v-loading` 指令，为关键图表面板添加 skeleton 占位。

---

### L-7：Vue 组件实例存入响应式对象警告

**位置：** 后台管理 → 用户列表

**现象：** 3条 `Vue received a Component that was made a reactive object` 警告，可能导致内存泄漏或响应式追踪失效。

**修复：** 找到将 Vue 组件存入 `ref()`/`reactive()` 的地方，改用 `shallowRef()` 或 `markRaw()`。

---

### L-8：实时监控数据条目 label 过于模糊

**位置：** 统一管控 / 安全指挥中心 → 实时预警滚动列表

**现象：** 大量预警条目仅标注"体征异常"，未标明具体是哪项体征异常（心率？血压？血氧？），可操作性差。

**修复：** 预警生成时将具体指标名称和数值写入 `warningDesc` 字段，如"心率异常（49 bpm）"、"血压异常（163/98 mmHg）"。

---

## 六、整体架构与代码质量优化建议

### A-1：统一错误处理机制

当前各页面自行处理 API 错误，缺乏统一。建议在 `src/api/` 的 axios 拦截器中集中处理：

```javascript
// src/utils/request.js — response interceptor
instance.interceptors.response.use(
  response => response.data,
  error => {
    const status = error.response?.status
    if (status === 401) router.push('/login')
    else if (status === 500) ElMessage.error('服务器内部错误，请稍后重试')
    else if (!error.response) ElMessage.error('网络异常，请检查连接')
    return Promise.reject(error)
  }
)
```

---

### A-2：API 字段名规范化

审计中发现前后端字段名不一致的问题（如 `detectionRate` vs `detectRate`，`empCode` vs `name`）。

**建议：**
1. 后端统一使用 `camelCase` 命名 JSON 字段
2. 前端 API 层建立接口文档注释，标注每个字段的类型和含义
3. 可考虑引入 TypeScript 接口定义来静态校验字段匹配

---

### A-3：Mixin 提取不完整

`chartPage.js` mixin 提取了 `pagedList`/`totalPages`，但 `filterDept` 等关联属性未一并提取，导致部分页面漏定义（见 M-3）。

**建议：** 将 mixin 所依赖的所有 `data` 属性统一在 mixin 的 `data()` 中声明（带合理默认值），使 mixin 自包含、无副作用。

---

### A-4：路由与视图文件的孤立检查

`src/router/` 下已删除 `personnel-management.js` 和 `statistics.js` 路由文件，对应视图文件也已删除，需确认：
- 没有其他路由文件引用这些页面
- 没有导航菜单仍指向这些路径
- 直接访问旧 URL 时有友好的 404 提示而非白屏

---

### A-5：模拟器数据连续性保障

当前生产数据存在明显的时间空洞（工作台日历大量空白日期），建议：
1. 将模拟器（`watch_tcp_simulator_1000.py`）配置为系统服务，随机器启动自动运行
2. 或增加历史数据补全脚本，填补测试/演示环境的数据空洞

---

## 七、实施优先级时间表

| 时间 | 任务清单 |
|------|---------|
| **第1天** | C-1（处理接口500）、C-2（健康画像参数）、C-3（血氧检测率0%）、C-4（睡眠崩溃防御） |
| **第2天** | H-1（滚动刷屏）、H-3（预警记录KPI为0）、M-2（筛选卡映射错误）、M-3（filterDept迁入mixin） |
| **第3天** | H-4（ECharts废弃警告）、H-5（Element Plus prop警告）、M-1（阈值配置各级相同） |
| **下一迭代** | M-4~M-6、L-1~L-8、A-1~A-5 |

---

## 八、验证清单（修复后回归测试）

### Critical 验证
- [ ] 统一管控 → 点击预警行 → 弹窗出现 → 填写备注 → 确认处理 → 状态变为"已处理"，处理率数字上升
- [ ] 心率页 → 点击员工行 → 查看健康画像 → 画像页显示该员工完整数据（非空状态）
- [ ] 血氧页 → 检测率 > 0% 且与实际员工数量成比例
- [ ] 睡眠页 → 页面正常加载，控制台无 TypeError

### High 验证
- [ ] Dashboard → 预警列表滚动时控制台无刷屏日志
- [ ] 预警记录 → 今日预警 / 待处理 / 危急预警 数量与表格数据吻合
- [ ] 预警记录 → 点击"危急预警"卡 → 表格正确筛选出高危级别记录
- [ ] 安全指挥中心 → 处理率显示真实数值（非0%）

### 代码质量验证
- [ ] 血压页 → 控制台无 `filterDept` Vue 警告
- [ ] 所有图表页 → 控制台无 ECharts DEPRECATED 警告
- [ ] 所有表格页 → Element Plus Invalid prop 警告清零
- [ ] 窗口缩放时 → 所有 ECharts 图表正确 resize

---

> **文档版本：** 1.0
> **生成时间：** 2026-03-15
> **生成方式：** Playwright 全站自动化审计（96次操作）
> **下次更新：** 修复实施后更新各条目完成状态
