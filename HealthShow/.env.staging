NODE_ENV = production

# just a flag
ENV = 'staging'

# base api
VITE_BASE_API = '/health'

# 预发默认新库，保持与真实手表验收一致。
VITE_DEFAULT_DATA_SOURCE = 'new'
VITE_SAFETY_COMMAND_V2 = true
VITE_UNIFIED_CONTROL_V2 = true

