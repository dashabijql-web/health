# 单仓库迁移实施计划

## 执行结果（2026-05-20）

本计划已经完成从双仓到单仓的实际切换，当前真实状态如下：

- `/home/j/code/health` 根目录现在已经是唯一 git 仓库。
- 当前工作区分支：`main`
- 当前工作区远端：`https://github.com/dashabijql-web/health-monorepo.git`
- 当前工作区本地 `HEAD` 已与 `origin/main` 对齐。
- `HealthShow/.git` 与 `HealthData/.git` 已从当前工作区移除，不再作为日常提交入口。
- 旧双仓 `.git` 元数据与切换前状态已备份到：
  - `/home/j/code/health-cutover-backup-20260520-103236`
- 新 monorepo dry-run 工作区保留在：
  - `/home/j/code/health-monorepo-dryrun`
- 新 monorepo 候选 clone 保留在：
  - `/home/j/code/health-monorepo-candidate`

本次执行与原预案的一个差异：

- 预案初稿建议本地以 `master` 初始化以兼容旧仓默认分支；实际 GitHub 新仓默认分支为 `main`，最终正式 monorepo 工作区已统一落到 `main`，这是当前应以代码和仓库事实为准的状态。

## 1. 目标

把当前 `D:/Health` 工作区从“双仓库并列”迁移为“根目录单仓库（monorepo）”，同时满足以下约束：

- 保留 `HealthShow` 前端历史。
- 保留 `HealthData` 后端历史。
- 把根目录下现有 `AGENTS.md`、`docs/`、`tests/`、`tools/` 等协作资产纳入版本控制。
- 迁移过程中尽量不打断当前开发与回归链路。
- 切换失败时可快速回退到旧双仓库模式。

## 2. 当前事实（2026-05-20）

- `D:/Health` 根目录当前 **不是** git 仓库。
- `D:/Health/HealthShow` 是独立 git 仓库。
  - 远端：`https://github.com/dashabijql-web/health.git`
  - 默认分支：`origin/master`
  - 当前分支：`autonomous-evolution-20260510-night`
  - 当前工作树 **非干净**。
- `D:/Health/HealthData` 是独立 git 仓库。
  - 远端：`https://github.com/dashabijql-web/health-backend.git`
  - 默认分支：`origin/master`
  - 当前分支：`autonomous-evolution-20260510-night`
  - 当前工作树当前看起来是干净的。
- 根目录存在大量当前未被任何仓库管理的协作文件与脚本，例如：
  - `AGENTS.md`
  - `docs/`
  - `tests/`
  - `tools/`
  - 多份执行计划 / 评估文档

结论：

- 现在不能直接把根目录当成 git 仓库使用。
- 真正的“单仓库”不是改一条命令，而是一次仓库结构迁移。

## 3. 推荐方案

### 3.1 推荐路线：新建 monorepo 远端，再切换工作流

推荐新建一个新的 GitHub 仓库作为单仓库远端，例如：

- `dashabijql-web/health-monorepo`

原因：

- 不需要强改现有 `health.git` 和 `health-backend.git` 的历史结构。
- 可以先完整 dry-run，验证通过后再切换默认工作流。
- 出问题时，旧双仓库仍可继续工作，回退成本最低。
- 更适合保留双仓各自的完整 commit 历史。

### 3.2 不推荐路线：直接把现有 `health.git` 改造成单仓库

不推荐直接复用当前前端远端 `health.git` 作为 monorepo 根仓库，原因：

- 需要重写当前前端仓库的目录结构，让原本仓库根内容整体下沉到 `HealthShow/`。
- 容易让旧分支、旧 CI、旧 PR 链接与新目录结构产生混乱。
- 还要额外处理 `HealthData` 历史导入与后端远端归档。

如果后续坚持复用旧前端远端，也应在“新 monorepo 本地 dry-run 验证完成后”再做，不应作为第一步。

## 4. 目标目录结构

迁移完成后的目标结构：

```text
Health/
├── .git/
├── AGENTS.md
├── docs/
├── tests/
├── tools/
├── HealthShow/
├── HealthData/
└── 其他根级协作文档与脚本
```

规则：

- `HealthShow/` 和 `HealthData/` 不再保留各自内部 `.git/`。
- 日常 `git status`、`git diff`、`commit`、`push` 全部在根目录执行。
- 前后端仍保留当前目录结构，不在这次迁移里做路径级重构。

## 5. 迁移前置条件

执行迁移前，必须先满足以下前置条件：

### 5.1 工作树清理

- `HealthShow` 当前存在未提交改动，不能在脏工作树上开始单仓库迁移。
- 必须先把当前有效改动：
  - 提交到原双仓库；
  - 或明确丢弃；
  - 或暂存为可恢复 patch / 临时分支。

要求：

- `git -C HealthShow status --short` 为空。
- `git -C HealthData status --short` 为空。

### 5.2 根目录内容分层

需要先明确哪些根目录内容应该进 monorepo，哪些必须继续忽略。

建议纳入版本控制：

- `AGENTS.md`
- `CLAUDE.md`
- `CODEX*.md`
- `HEALTH*.md`
- `docs/`
- `tests/`
- `tools/`
- 各类执行计划和评估文档

建议忽略：

- `runtime-logs/`
- 各类测试运行产物、截图、缓存
- `node_modules/`
- `dist/`
- `target/`
- IDE/系统临时文件
- 凭证文件，例如 `C:\Users\j\Desktop\githubtoken.txt`

### 5.3 切换窗口

迁移期间建议冻结以下高风险操作：

- 双仓继续各自独立 push 新功能。
- 修改 CI/CD 默认仓库入口。
- 跨前后端同时做大重构。

建议开一个明确切换窗口，先冻结，再迁移，再切换。

## 6. 执行方案

## Phase 0：冻结与备份

目标：把双仓状态冻结到可迁移基线。

步骤：

1. 清理 `HealthShow` 和 `HealthData` 工作树，保证无未提交改动。
2. 在两个旧仓库分别打迁移起点 tag。
3. 记录两个旧仓库当前默认分支、活跃分支和最后有效提交。
4. 备份当前根目录文档与脚本清单。

建议命令：

```bash
git -C /home/j/code/health/HealthShow status --short
git -C /home/j/code/health/HealthData status --short

git -C /home/j/code/health/HealthShow tag monorepo-cutover-start-20260520
git -C /home/j/code/health/HealthData tag monorepo-cutover-start-20260520

git -C /home/j/code/health/HealthShow push origin monorepo-cutover-start-20260520
git -C /home/j/code/health/HealthData push origin monorepo-cutover-start-20260520
```

退出条件：

- 两个子仓库工作树都干净。
- 两个旧仓库都有可回退 tag。

## Phase 1：创建 monorepo dry-run 工作目录

目标：不污染现有工作区，先在旁路目录完成第一次完整导入。

建议新目录：

- `/home/j/code/health-monorepo-dryrun`

建议命令：

```bash
mkdir -p /home/j/code/health-monorepo-dryrun
cd /home/j/code/health-monorepo-dryrun
git init -b master
git commit --allow-empty -m "chore: initialize monorepo"
```

说明：

- monorepo 第一轮建议继续沿用 `master`，因为两个旧远端默认都是 `master`，能减少 CI 和脚本切换成本。
- 后续如需统一改为 `main`，应放到迁移完成后的第二阶段。

## Phase 2：导入双仓历史

目标：把两个旧仓库的完整历史保留到 monorepo 的子目录中。

推荐使用 `git subtree`，不要 `--squash`。

建议命令：

```bash
cd /home/j/code/health-monorepo-dryrun

git remote add healthshow https://github.com/dashabijql-web/health.git
git remote add healthdata https://github.com/dashabijql-web/health-backend.git

git fetch healthshow master
git fetch healthdata master

git subtree add --prefix=HealthShow healthshow master
git subtree add --prefix=HealthData healthdata master
```

说明：

- 这里导入的基线应优先使用两个旧仓库的 `master`。
- 当前夜间分支 `autonomous-evolution-20260510-night` 上若还有未合并内容，应在双仓内先决定：
  - 合并进 `master` 后再导入；
  - 或在 monorepo 导入完成后，再单独补迁这一条分支上的提交。

退出条件：

- `git log -- HealthShow` 能看到前端历史。
- `git log -- HealthData` 能看到后端历史。

## Phase 3：纳入根级协作资产

目标：把当前根目录下非 git 管理的文档、脚本、测试入口纳入 monorepo。

注意：

- 不要把现有 `HealthShow/`、`HealthData/` 物理目录直接复制进 dry-run；这两个目录已经通过 subtree 导入。
- 只复制根级资产和非子仓库目录。

推荐同步方式：

```bash
rsync -a \
  --exclude 'HealthShow' \
  --exclude 'HealthData' \
  --exclude 'runtime-logs' \
  --exclude '.claude' \
  /home/j/code/health/ \
  /home/j/code/health-monorepo-dryrun/
```

然后补齐根级 `.gitignore`，至少包含：

```gitignore
runtime-logs/
**/node_modules/
**/dist/
**/target/
**/.idea/
**/.vscode/
**/tests/**/artifacts/
**/tests/runs/
```

提交：

```bash
git add .
git commit -m "chore: add workspace root assets"
```

退出条件：

- 根级文档、测试、脚本都进入 monorepo。
- 无嵌套 `.git/` 被误纳入版本控制。

## Phase 4：验证 monorepo 可用性

目标：确认导入后能正常开发、构建、测试。

必做检查：

1. 历史检查
   - `git log -- HealthShow`
   - `git log -- HealthData`
2. 结构检查
   - `find . -name .git -type d`
   - 结果只能保留根目录 `.git`
3. 前端检查
   - `cd HealthShow && npm run build`
4. 后端检查
   - `cd HealthData && mvn -q test`
5. 根级 runner / 脚本检查
   - `bash tools/health-wsl-stack.sh status`
   - `python3 tools/start-health-runner.py --dry-run`

退出条件：

- 构建、测试、根级脚本入口均通过。
- 无脚本继续强依赖“子目录本身是独立仓库”的假设。

## Phase 5：创建 monorepo 远端并首推

目标：把 dry-run 结果推到新的单仓库远端。

建议：

- 新建 GitHub 仓库：`dashabijql-web/health-monorepo`

建议命令：

```bash
cd /home/j/code/health-monorepo-dryrun
git remote add origin https://github.com/dashabijql-web/health-monorepo.git
git push -u origin master
```

推送时继续沿用当前护栏：

- GitHub token 路径：
  - Windows：`C:\Users\j\Desktop\githubtoken.txt`
  - WSL：`/mnt/c/Users/j/Desktop/githubtoken.txt`

退出条件：

- monorepo 已在远端可见。
- 远端默认分支可设置为 `master`。

## Phase 6：切换日常工作流

目标：让团队和脚本全部切到新单仓库。

需要同步的内容：

1. 更新 `AGENTS.md`
   - 把“根目录不是 git 仓库”改为“根目录是唯一 git 仓库”。
   - 删除“双仓分别提交/推送”的护栏。
2. 更新根级文档
   - 启动入口仍保留现有路径，但 git 操作改为根目录执行。
3. 更新 CI / 自动化
   - 原来指向 `health.git` 和 `health-backend.git` 的流水线需要改到 monorepo。
   - 前后端可用 path filter 拆分触发。
4. 更新本地开发约定
   - 日常提交在 `/home/j/code/health` 根目录进行。
   - 子目录不再保留独立 `.git`。

退出条件：

- 新开发、提测、回归、夜间 runner 都以 monorepo 为唯一 git 来源。

## Phase 7：旧仓库收口

目标：把旧双仓降级为只读或归档。

建议：

- `health.git`、`health-backend.git` 至少保留只读一段时间。
- README 标明：
  - 仓库已迁移到 monorepo；
  - 新开发停止在旧仓进行；
  - 给出新仓地址。

不要立即删除旧仓库，至少观察一个稳定周期后再决定。

## 7. 回退方案

monorepo 迁移的安全前提是：**旧双仓在 cutover 前不被破坏**。

因此回退很直接：

1. 停止使用 monorepo。
2. 恢复双仓作为唯一提交入口。
3. 根目录继续作为普通工作区，不做 git 操作。

只要旧仓库未删、旧分支未重写，回退成本可控。

## 8. 风险与控制

### 风险 1：在脏工作树上开始迁移

后果：

- 把实验性改动、临时文件和正式迁移混在一起。

控制：

- Phase 0 必须强制清理双仓工作树。

### 风险 2：误把嵌套 `.git` 或产物目录纳入 monorepo

后果：

- 仓库结构污染，后续 `status` 和 CI 变得不可控。

控制：

- 先 subtree 导入，再单独同步根级资产。
- 明确 `.gitignore`。

### 风险 3：前端/后端历史丢失

后果：

- blame、追历史、回滚定位能力受损。

控制：

- 使用 `git subtree add` 且不 `--squash`。

### 风险 4：脚本仍默认“双仓分别 git 操作”

后果：

- 自动化链路与新仓结构冲突。

控制：

- Phase 4 和 Phase 6 分别做脚本核查和文档修正。

## 9. 建议决策

建议按下面顺序推进：

1. 先批准“新建 monorepo 远端”路线。
2. 先把 `HealthShow` 当前脏工作树清理干净。
3. 做一次完整 dry-run。
4. dry-run 验证通过后，再正式 cutover。

## 10. 执行入口

如果批准开工，建议从下面这个最小入口开始：

1. 清理 `HealthShow` 当前未提交改动。
2. 在两个旧仓分别打 tag。
3. 新建 `/home/j/code/health-monorepo-dryrun`。
4. 用 `git subtree` 导入两个 `master` 历史。
5. 同步根级文档与脚本。
6. 跑前后端构建与根级 runner 验证。

---

当前结论：

- 你要的“单仓库”是可做的。
- 推荐做法是“新 monorepo 远端 + subtree 保历史 + dry-run 先验证”。
- 在 `HealthShow` 当前仍有未清理工作树的前提下，不应直接开始正式迁移。
