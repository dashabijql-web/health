# Health 项目目标达成实施计划

制定日期：2026-05-08

适用目标：

- 把 Health 项目从“核心问题基本可控”推进到“可交付、可验收、可持续维护”。
- 让 Codex 以 goal-mode 连续执行时有明确停机条件，而不是无限重跑历史治理清单。

本计划不是替代所有旧文档。

旧文档的定位：

- `docs/archive/历史归档-治理清单.md`：历史问题总表和治理原则。
- `docs/archive/历史归档-CODEX_MULTI_DAY_TASKBOOK.md`：第一轮治理执行手册。
- `CODEX_TARGET_STATE_PLAN.md`：已经完成的目标态定义。
- 本文件：从 2026-05-08 当前状态出发，继续把代码质量、健壮度、功能完整性和页面美观度推到下一目标。

---

## 1. Goal-mode 总目标

给新 Codex 的目标应写成：

```text
以 D:/Health/目标达成实施计划.md 为主执行文档，结合 D:/Health/AGENTS.md 和 D:/Health/项目重新评估结果.md，持续推进 HealthShow 与 HealthData，直到满足本计划“最终完成定义”为止。

不要从头重复执行已完成的 P0/P1/G1-G6 历史任务；只处理本计划列出的剩余债、回归、功能闭环和页面产品化任务。

每完成一个阶段都要运行对应验证，更新相关 Markdown，并在发现旧文档与当前代码冲突时以当前代码和本计划为准。
```

---

## 2. 最终完成定义

只有下面条件全部满足，才允许 goal-mode 停止：

### 2.1 代码质量完成定义

- 后端核心 controller 不回退到 mapper 直连。
- 新增或修改的高频接口不再暴露裸 `Map<String,Object>` 作为外部契约。
- 关键 broad catch 不再把 `e.getMessage()` 直接拼给前端。
- AI、Dashboard、指标、预警、后台管理的剩余热点文件有明确边界或门禁说明。
- 前端 21 个已迁移页面继续通过 `audit:page-structure`。
- 指标页共享 runtime 不回退，后续新增指标页必须复用已有抽象。
- 图表生命周期、timer、listener 不回流到页面根 `index.vue`。

### 2.2 健壮度完成定义

必须通过：

```powershell
mvn -q test -f D:\Health\HealthData\pom.xml
```

必须通过：

```powershell
cd D:\Health\HealthShow
npm run build
npm run audit:structure
npm run audit:api
npm run audit:write
npm run audit:auth
npm run audit:e2e
npm run audit:pipeline
npm run audit:pipeline-warning
```

必须补齐或确认：

- 双库请求侧、手表侧、Redis flush、分表任务测试仍通过。
- AI SQL 越界、超时、修复、拒绝语义测试仍通过。
- 登录刷新、退出、后端重启、并发 401 仍通过。
- TCP 探针写入和预警探针仍通过。
- 关键 actuator metric family 启动后仍可见。

### 2.3 功能完整性完成定义

- 新库 `health_new` 的员工、部门、设备、设备绑定初始化流程有文档和脚本或明确操作步骤。
- 至少一台真实手表完成新库写入验收，且模拟器仍写老库。
- 告警链路完成从配置、触发、列表、通知、处理、画像跳转、已处理状态的验收。
- AI 健康助手和 AI 报告在 AI 服务不可用或超时时有可接受前端提示和后端日志。
- 角色、用户、部门、工种、设备等后台管理页的核心增删改查至少有 smoke 或人工验收记录。
- 报表/导出至少确认一组大数据量或典型数据量场景不会卡死页面。

### 2.4 页面美观度完成定义

- 关键页形成统一设计 token：
  - 色彩
  - 间距
  - 字号
  - 卡片
  - 表格
  - 图表
  - 空状态
  - 风险等级
- 至少完成这些页面的视觉复核和必要优化：
  - `safety-command`
  - `dashboard`
  - `workbench`
  - `real-time`
  - `heart-rate`
  - `blood-oxygen`
  - `pressure`
  - `blood-pressure`
  - `risk-warning`
  - `alert-management/notifications`
  - `alert-management/records`
  - `ai-chat`
  - `device-management`
  - `user-list`
  - `role-management`
- 桌面端不出现明显错位、裁切、遮挡、颜色冲突。
- 移动端至少覆盖：
  - `safety-command`
  - `dashboard`
  - `workbench`
  - `real-time`
  - `risk-warning`
  - `alert-notifications`
- 页面不再停留在 Element Plus 默认后台质感；管理页至少完成统一头部、筛选区、表格、按钮、弹窗、空状态的轻量美化。
- 建立截图验收产物，至少保存关键页桌面和移动端截图路径到最终 handoff。

### 2.5 文档完成定义

必须更新：

- `D:/Health/AGENTS.md`
- `D:/Health/docs/archive/历史归档-HEALTH_HANDOFF.md`
- `D:/Health/项目重新评估结果.md`
- `D:/Health/目标达成实施计划.md`
- 涉及双库时更新 `D:/Health/HealthData/DUAL_DB_CUTOVER.md`
- 涉及前端结构时更新 `D:/Health/HealthShow/TECHNICAL_DOCS.md`

最终文档必须写清：

- 已完成阶段
- 未完成但可接受的债
- 验证命令和结果
- 最新产物路径
- 新 Codex 接手入口

---

## 3. 执行总原则

### 3.1 不重复做

不要重复执行这些已完成任务：

- `P0-01` 到 `P0-08` 第一轮止血。
- `WatchDataHandler` 第一轮 dispatcher 拆分。
- `DataProcessService` 第一轮职责拆分。
- `DashboardMapper` 退出和 Dashboard 主题 mapper 第一轮拆分。
- 前端路由事实源 `app-routes.mjs` 与导航派生 `navigation.mjs` 第一轮收口。
- 21 个已迁移页面的基础目录门禁。

除非发现明确回归，否则不要重做。

### 3.2 每阶段只做一个主题

推荐主题拆分：

- 后端契约治理
- 后端健壮性补强
- 前端 page engine 和图表生命周期
- 页面视觉产品化
- 功能验收闭环
- 性能与观测
- 文档收口

不要把后端 mapper 重构、前端视觉重做、双库规则调整混在一个批次里。

### 3.3 工作树保护

- 两个子仓库都是独立 git 仓库。
- 每次开工先执行：

```powershell
git -C D:\Health\HealthShow status --short
git -C D:\Health\HealthData status --short
```

- 默认把现有未提交改动视为用户已有工作，不回滚。
- 修改前确认本阶段写入范围。

---

## 4. Phase A：基线冻结和问题再确认

### 目标

把当前状态固定为后续目标计划的起点，避免重复争论“旧任务是否完成”。

### 输入文档

按顺序读取：

1. `D:/Health/AGENTS.md`
2. `D:/Health/项目重新评估结果.md`
3. `D:/Health/目标达成实施计划.md`
4. `D:/Health/docs/archive/历史归档-HEALTH_HANDOFF.md`
5. `D:/Health/CODEX_TARGET_STATE_PLAN.md`
6. `D:/Health/docs/archive/历史归档-治理清单.md`
7. `D:/Health/docs/archive/历史归档-CODEX_MULTI_DAY_TASKBOOK.md`

### 必做

- 分别检查 `HealthShow` 和 `HealthData` 工作树。
- 重跑最小验证：

```powershell
mvn -q test -f D:\Health\HealthData\pom.xml
cd D:\Health\HealthShow
npm run build
npm run audit:structure
npm run audit:api
```

- 记录当前最大文件、`Map<String,Object>`、`catch (Exception)`、前端页面行数和副作用命中。
- 把验证产物路径写入本文件的执行记录。

### 验收

- 基线命令通过。
- 如果失败，先修失败，不进入后续 phase。

---

## 5. Phase B：后端契约和异常语义第二轮治理

### 目标

防止后端从“已收口核心域”回退到“Map + broad catch + controller 拼响应”。

### 优先范围

第一优先级：

- `AiChatService`
- `AiHealthReportService`
- `RiskWarningService`
- `DashboardServiceImpl`
- `DashboardDepartmentQueryService`
- `HeartRateMapper`
- `SleepMapper`
- `RealtimeService`

第二优先级：

- `UserListController`
- `DepartmentController`
- `PermissionController`
- `AlertConfigController`
- `AiHealthReportController`
- 设备、角色、工种等后台管理域

### 任务

1. 给新增或高频接口补 typed request/response DTO。
2. 保留 AI Text2SQL 的动态结果集例外，但必须继续封装在 `AiSqlResultSet`。
3. 清理 controller 中明显不必要的 `catch (Exception)`。
4. 禁止新增 `Result.error(e.getMessage())` 或“获取失败: + 原始异常”。
5. 把后台管理域的 Map 返回按页面优先级逐步替换为 DTO。
6. 对每个改动域补单测或 controller boundary test。

### 非目标

- 不追求一次性清零所有 `Map<String,Object>`。
- 不重写 AI Text2SQL 动态列能力。
- 不在同一阶段改前端视觉。

### 验收

```powershell
mvn -q test -f D:\Health\HealthData\pom.xml
```

并确认：

- 核心 controller boundary test 仍通过。
- `rg -n "Result.error\\(.*getMessage|获取失败.*getMessage" src/main/java` 不出现新增命中。
- 新增接口有 typed DTO 或明确动态结果例外说明。

---

## 6. Phase C：后端性能与观测第二轮治理

### 目标

把“能跑”推进到“现场数据量下能定位慢、堵、错、丢”。

### 优先范围

- Dashboard 查询
- 五类指标查询
- Realtime 查询
- Risk Warning 查询
- Statistics 查询
- AI 报告生成
- Redis buffer flush
- 手表写入链路

### 任务

1. 梳理仍依赖 `v_health_record` / `v_warning_record` 的高频查询。
2. 对慢接口建立阈值：
   - API smoke 中 500ms 以上接口进入观察清单。
   - 1000ms 以上接口必须解释原因或优化。
3. 对关键查询优先走月分表直查或缓存，不回退全视图扫描。
4. 补慢 SQL 指标和日志字段：
   - source
   - table
   - endpoint
   - elapsedMs
   - rowCount
5. 对 AI 报告生成增加超时、降级、缓存命中、生成失败的可观测信息。
6. 建立现场 dashboard / 告警面板方案文档，哪怕第一版只是 actuator metric 读取清单。

### 验收

- `npm run audit:api` 通过。
- API smoke 中超过 1000ms 的接口都有记录和处理结论。
- actuator metrics 启动后仍可见。
- 慢 SQL 日志能定位到接口和数据源。

---

## 7. Phase D：前端指标页 page engine 第二轮

### 目标

让五类指标页不再只是“部分共享 runtime”，而是形成稳定可复用 page engine。

### 范围

- `heart-rate`
- `blood-oxygen`
- `pressure`
- `blood-pressure`
- `sleep`
- `src/views/health-monitor/metric-page/*`

### 任务

1. 抽统一指标页配置：
   - 指标名
   - 单位
   - 风险颜色
   - API 方法
   - 趋势字段
   - Top5 字段
   - 导出列
2. 把四个核心指标页的重复查询、导出、Top5、趋势、分布、部门统计配置收敛到统一入口。
3. 评估 `sleep` 是否纳入同一 engine；如果差异过大，写清“等价抽象”边界。
4. 图表 option 按通用 chart factory 收口。
5. `index.vue` 只保留页面壳、配置和少量差异化展示。

### 非目标

- 不改变接口字段。
- 不改后端查询语义。
- 不做大规模视觉重做，只做结构收口和轻量一致性。

### 验收

```powershell
cd D:\Health\HealthShow
npm run audit:structure
npm run build
npm run audit:e2e
```

并确认：

- `audit:page-structure` 仍通过。
- 指标页 `index.vue` 不新增 timer/listener/chart init。
- 新增第六个指标页时可通过配置落地，而不是复制整页。

---

## 8. Phase E：前端页面美观度产品化

### 目标

把页面从“功能可用”推进到“视觉统一、现场演示稳定、管理页不再默认后台感”。

### 设计原则

- 保留当前行业健康监测和安全指挥中心的深色科技风方向。
- 管理页不强行做大屏风，但需要统一卡片、筛选、表格、按钮、弹窗、空状态。
- 不追求炫技动画，优先解决层级、密度、间距、颜色、响应式。

### 任务 1：建立设计 token

新增或收口统一样式入口，覆盖：

- 背景色
- 主色
- 风险色
- 文本层级
- 卡片圆角
- 卡片阴影
- 表格边框
- 图表面板
- 空状态
- 移动端断点

### 任务 2：关键大屏页视觉复核

必须复核：

- `safety-command`
- `dashboard`
- `workbench`
- `real-time`

重点检查：

- 1366 宽度是否裁切。
- 1920 宽度是否过空或过散。
- 投屏下字体是否可读。
- 告警色是否足够突出。
- 顶栏、侧栏、卡片、图表层级是否一致。

### 任务 3：指标和预警页视觉统一

必须复核：

- `heart-rate`
- `blood-oxygen`
- `pressure`
- `blood-pressure`
- `sleep`
- `risk-warning`
- `trend-warning`
- `alert-management/notifications`
- `alert-management/records`

重点检查：

- 头部筛选区一致。
- 概览卡一致。
- 图表主题一致。
- 表格状态标签一致。
- 空数据状态一致。

### 任务 4：管理页轻量美化

必须复核：

- `device-management`
- `user-list`
- `role-management`
- `department`
- `job-type`

重点检查：

- 不再是纯默认 Element Plus 堆叠。
- 操作按钮分主次。
- 弹窗字段分组清晰。
- 空状态、加载态、错误态统一。

### 任务 5：移动端补齐

至少覆盖：

- `safety-command`
- `dashboard`
- `workbench`
- `real-time`
- `risk-warning`
- `alert-notifications`

### 验收

- 保存关键桌面和移动端截图。
- `npm run audit:e2e` 通过。
- `npm run build` 通过。
- `docs/archive/历史归档-HEALTH_HANDOFF.md` 写入截图产物路径和视觉剩余债。

---

## 9. Phase F：功能闭环和现场验收准备

### 目标

把“脚本能跑”推进到“现场能验收”。

### 任务 1：新库初始化闭环

必须完成：

- 明确 `health_new` 保留和清空表的最终口径。
- 形成员工、部门、设备、设备绑定导入或重建流程。
- 确认 `admin / admin123` bootstrap 策略仍有效或有替代方案。
- 写入 `DUAL_DB_CUTOVER.md`。

验收：

- 新库登录成功。
- 新库员工/部门/设备从空库重建成功。
- 新库页面显示符合预期，不再被误判为老库缓存。

### 任务 2：真实手表验收

必须完成：

- 确认真实手表 IMEI 不命中模拟器正则。
- 真实手表连接 TCP `9000`。
- 数据写入 `health_new`。
- 前端切新库可见。
- 模拟器仍写老库。

验收：

- SQL 查询能证明真实手表进入新库。
- SQL 查询能证明模拟器仍在老库。
- API 响应头 `X-Health-Data-Source` 与预期一致。

### 任务 3：告警处理闭环

必须完成：

- 阈值配置变更。
- 触发高危数据。
- 生成预警记录。
- 通知页可见。
- 处理后状态变更。
- 画像或相关页面可跳转。
- 处理人和时间可追踪。

验收：

- `npm run audit:pipeline-warning` 通过。
- 人工验收步骤写入 handoff。

### 任务 4：AI 功能闭环

必须完成：

- AI 问答正常。
- AI SQL 被拒绝时前端提示清晰。
- AI 报告生成正常。
- AI 服务超时或不可用时提示清晰。
- 生成耗时和失败可观测。

验收：

- AI 成功、拒绝、失败三类路径均有记录。

### 任务 5：后台管理闭环

必须完成：

- 用户
- 角色
- 部门
- 工种
- 设备
- 阈值配置

至少每个域确认：

- 列表
- 查询
- 新增或编辑
- 删除或停用
- 权限/角色影响

验收：

- 形成一份后台管理验收记录，可写入 `docs/archive/历史归档-HEALTH_HANDOFF.md` 或单独 artifact。

---

## 10. Phase G：发布门禁和文档收口

### 目标

让项目从“当前可用”变成“后续新会话可持续执行和交接”。

### 必跑命令

后端：

```powershell
mvn -q test -f D:\Health\HealthData\pom.xml
```

前端：

```powershell
cd D:\Health\HealthShow
npm run build
npm run audit:structure
npm run audit:api
npm run audit:write
npm run audit:auth
npm run audit:e2e
npm run audit:pipeline
npm run audit:pipeline-warning
```

### 文档更新

必须更新：

- `D:/Health/AGENTS.md`
- `D:/Health/docs/archive/历史归档-HEALTH_HANDOFF.md`
- `D:/Health/项目重新评估结果.md`
- `D:/Health/目标达成实施计划.md`

按改动更新：

- 双库改动：`D:/Health/HealthData/DUAL_DB_CUTOVER.md`
- 前端结构改动：`D:/Health/HealthShow/TECHNICAL_DOCS.md`
- 回归脚本改动：`D:/Health/HealthShow/tests/README.md`

### 最终交付说明必须包含

- 当前两个仓库 `git status --short`。
- 已完成 phase。
- 验证命令和结果。
- 新增产物路径。
- 剩余可接受债。
- 明确是否达到目标。

---

## 11. 建议执行顺序

按顺序执行：

1. `Phase A`：基线冻结和问题再确认。
2. `Phase B`：后端契约和异常语义第二轮治理。
3. `Phase C`：后端性能与观测第二轮治理。
4. `Phase D`：前端指标页 page engine 第二轮。
5. `Phase E`：前端页面美观度产品化。
6. `Phase F`：功能闭环和现场验收准备。
7. `Phase G`：发布门禁和文档收口。

可以并行但要分支或明确文件范围：

- `Phase B` 和 `Phase D` 可以由不同 Codex 会话分别处理。
- `Phase C` 的观测补强可以和 `Phase F` 的现场验收准备配合。
- `Phase E` 不建议和大规模后端 SQL 改动同批次提交。

---

## 12. 每阶段交付模板

每完成一个阶段，在最终回复和文档中按这个模板记录：

```text
阶段：
目标：
完成项：
改动范围：
验证命令：
验证结果：
新增产物：
遗留风险：
下一阶段入口：
```

---

## 13. 停止条件

Codex 只能在以下情况停止：

1. `最终完成定义` 全部满足。
2. 遇到无法自动发现的现场信息阻塞，例如真实手表、真实账号、真实权限矩阵，需要用户提供。
3. 遇到会破坏当前业务语义的冲突，需要用户决策。
4. 本地服务或外部依赖不可用，并且已经明确记录重试和阻塞原因。

不能因为下面原因停止：

- 只完成了某一个小阶段。
- 只更新了文档，没有执行验证。
- 旧文档某个 phase 标了完成但新目标还没达成。
- 代码能编译但页面未复核。
- API 能通但功能闭环未验收。

---

## 14. 当前起点记录

2026-05-08 当前已重新验证：

- 后端 `mvn -q test -f D:\Health\HealthData\pom.xml` 通过。
- 前端 `npm run build` 通过。
- 前端 `npm run audit:structure` 通过。
- 前端 `npm run audit:api` 通过，产物：
  - `D:/Health/HealthShow/tests/api/artifacts/2026-05-07T22-29-45-860Z/summary.md`
- 前端 `npm run audit:nav` 通过。

近期已通过但本次未重复跑全套的产物：

- `D:/Health/HealthShow/tests/e2e/artifacts/2026-05-07T22-19-53-162Z/summary.md`
- `D:/Health/HealthShow/tests/pipeline/artifacts/2026-05-07T20-17-47-153Z/summary.md`
- `D:/Health/HealthShow/tests/pipeline/artifacts/2026-05-07T20-18-23-921Z/warning-summary.md`

当前仍然需要继续执行本计划的原因：

- 后端仍有 `Map<String,Object>`、broad catch、AI/指标/后台管理热点债。
- 前端指标页 page engine 未完全完成，图表生命周期和视觉门禁仍需第二轮。
- 功能闭环还缺真实手表、新库初始化、后台管理验收、AI 降级体验。
- 页面美观度还没有统一设计 token 和截图级验收。

---

## 15. 目标达成后的状态

执行完本计划后，项目应达到：

- 代码不是无债状态，但关键债务有边界、有门禁、有验收。
- 新功能不会默认堆回超级类和超级页。
- 真实手表、新库、告警、AI、报表、后台管理有现场验收路径。
- 页面从“能用”提升到“可演示、统一、移动端基本可靠”。
- 后续 Codex 新会话可以从 `AGENTS.md`、本评估、本计划和 handoff 接手，不需要重新猜项目状态。



