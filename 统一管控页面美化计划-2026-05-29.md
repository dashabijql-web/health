# 统一管控页面美化计划 (2026-05-29)

## 一、当前现状与痛点

目前统一管控页 (`HealthShow/src/views/health-monitor/dashboard/index.vue`) 处于“新旧混合”状态：
1. **头部已收口**：已经接入了 `PageHeroHeader` 与 `MetricStrip` 等最新的共享旗舰壳组件。
2. **主体遗留旧债**：页面主体区域仍大量依赖旧有的 `.dm-outer`, `.dm-panel` 样式体系。面板样式陈旧、包含硬编码的颜色、多余的内联样式以及旧版“霓虹化”表达。
3. **视觉焦点涣散**：现有的“左中右”三栏布局中，各面板视觉权重过于平均，导致作为核心业务所在的“值班决策面板（中栏）”不够突出。
4. **设计语言不一致**：近期安全指挥中心 (`safety-command`) 已向“电影级指挥中心 (Cinematic Command Center)” 风格演进，统一管控页亟需与之对齐，建立深色金属底、悬浮面板和统一的状态色系统。

## 二、核心目标

本次美化遵循与 `safety-command` 相同的可迁移视觉约束，不重构底层业务逻辑，纯做表现层升级：
- **视觉风格对齐**：全面引入“电影级指挥中心”语言（深色质感底、高信息密度、状态自发光等）。
- **重塑视觉动线**：首屏 3 秒内清晰传达“当前态势 -> 待处理高危 -> 下一步行动”，强化中栏的绝对主角地位。
- **净化冗余代码**：彻底废弃 `dm-` 样式体系，转为统一的 `hm-page-shell` 和页面专属缩写前缀（例如 `db-`）。

## 三、分期执行计划 (Phases)

### Phase 1：外壳骨架与命名空间迁移
**目标**：剥离旧有外壳，接入新版统一网格布局骨架。
- 移除 `<div class="dm-outer">` 和 `.dm-root`，替换为 `<div class="hm-page-shell db">`。
- 将头部 `header class="dm-hd"` 重构为符合新视觉的 `.db-hd`。
- 移除 `dashboard.scss` 中所有针对 `dm-` 前缀的全局紫粉色/霓虹风格代码，并搭建基本的深色金属底板（CSS 变量定义）。

### Phase 2：核心业务面板（中栏）升级
**目标**：将中栏真正打造为不可忽视的值班决策台。
- 升级 `DashboardDispatchPanel.vue` 和 `DashboardWarningStream.vue`。
- 将普通的列表滚动升级为具有层次感的“实时事件带”。
- 取消卡片边框，改用悬浮阴影和环境光底色。
- 面板标题轨道（Track）语法的统一（摒弃陈旧的 `.dm-ph`）。

### Phase 3：左右翼数据挂件精修
**目标**：降低左右两栏视觉噪声，转为辅助支撑区。
- **左栏（态势）**：重绘 `体征健康评估` 和 `部门综合看板`。利用统一的主色调（冷青蓝）替代花哨的多彩配色。
- **右栏（行动）**：收敛右侧 Sidebar。清理与管控不相关的轻提示（如旧版 HealthTips 残留），统一排行版、预警率面板的列表展现风格。

### Phase 4：色彩、图表与状态语义大一统
**目标**：杜绝页面内存在的第二套颜色表达。
- 统一核心状态色：`primary` (冷青蓝)、`danger` (红)、`warning` (橙黄)、`success` (绿)。
- 修改 `dashboard-chart-options.js`，将 ECharts 的底色、坐标轴、Tooltip 和图例与最新的悬浮面板质感对齐。

### Phase 5：滚动策略与移动端降噪
**目标**：实现桌面端独立面板滚动，移动端关键信息首屏化。
- 桌面端：主页面禁止全局长滚动，改为在 `DashboardWarningStream` 或其他长列表内部独立滚动（确保面板高度自适应）。
- 移动端：首屏仅保留 Hero、MetricStrip 和 值班决策面板，其他面板降级或折叠展示。

## 四、执行原则与测试验收

**执行原则**：
- 小步迭代，每次只改造 1-2 个组件。
- 不修改业务侧数据流接口与运行时文件 `dashboard-runtime.js`。
- 保证页面高度在特定分辨率（如 desktop-1440/1707/1920）不会出现破坏性溢出。

**验收命令**：
每次 Phase 提交前，需执行如下基线测试：
```bash
# 结构依赖与完整性验证
npm run audit:structure

# 测试特定组件与核心断言
node --test tests/dashboard-*.mjs

# 视觉对比快照 (Visual Regression)
node scripts/with-env.mjs VISUAL_ROUTES=dashboard -- npm run audit:visual
```
