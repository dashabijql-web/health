# Health 项目重新评估结果

评估日期：2026-05-08

评估范围：

- 前端仓库：`D:/Health/HealthShow`
- 后端仓库：`D:/Health/HealthData`
- 根目录治理文档：`AGENTS.md`、`docs/archive/历史归档-HEALTH_HANDOFF.md`、`docs/archive/历史归档-治理清单.md`、`docs/archive/历史归档-CODEX_MULTI_DAY_TASKBOOK.md`、`CODEX_TARGET_STATE_PLAN.md`

评估目标：

- 判断当前“屎山代码”是否已经基本可控。
- 判断代码健壮度是否已经能支撑继续演进。
- 判断功能完善性是否已经接近可交付。
- 判断页面美观度是否已经达到现场演示和长期产品化要求。
- 给后续执行计划提供证据，不把旧任务清单机械重跑一遍。

---

## 1. 总结论

当前项目已经不是“不可控屎山”。

更准确的状态是：

- 核心高风险链路已经完成第一轮止血，并且有自动化回归和观测入口。
- 后端最危险的超级类和查询栈已经被拆开，继续膨胀的趋势被明显遏制。
- 前端核心路由、菜单、大页目录和一批页面副作用已经纳入门禁。
- 功能主链路已经能跑通，API、页面路由、登录、写入链路、预警链路都有近期通过证据。
- 页面视觉已经从“纯后台表单”推进到“可演示的行业大屏 + 管理后台”混合状态，但还没有统一到成熟产品级设计系统。

因此当前可以说：

- “屎山代码和健壮度问题已经基本解决到可控状态。”
- 不能说“历史技术债已经清零”。
- 不能说“页面已经全面达到高质量产品设计水准”。
- 不能说“所有真实现场功能已经验收完成”。

当前后续工作的性质不应再是“重新执行所有旧治理任务”，而应切换为：

1. 消灭剩余 P1/P2 结构债和契约债。
2. 把功能闭环从“自动化可跑”推进到“现场可验收”。
3. 把页面从“能看、能用”推进到“统一、稳定、可演示、移动端可接受”。
4. 把门禁从“本地有脚本”推进到“新改动默认不会回退”。

---

## 2. 当前证据快照

### 2.1 工作树状态

`D:/Health` 不是 git 仓库，两个子目录是独立仓库：

- `D:/Health/HealthShow`
- `D:/Health/HealthData`

2026-05-08 评估时，两端工作树均存在大量未提交改动。

这些改动包含本轮治理成果：

- 前端路由从旧 `.js` 向 `.mjs` 事实源迁移。
- 前端大页拆分、样式模块、runtime、view-model、门禁脚本新增。
- 后端双库、AI SQL、Redis buffer、watch handler、dashboard mapper、DTO/typed row、测试和观测模块新增。

评估结论基于“当前工作树事实”，不是基于干净 git HEAD。

### 2.2 当前重新验证结果

本轮评估已重新执行并通过：

| 命令 | 结果 |
| --- | --- |
| `mvn -q test -f D:\Health\HealthData\pom.xml` | 通过 |
| `npm run build` | 通过 |
| `npm run audit:structure` | 通过，`21` 个迁移页，`0` 个 legacy tracked page |
| `npm run audit:api` | 通过，`53 checks / 0 failed / 0 warning` |
| `npm run audit:nav` | 通过，`6 navGroups / 23 visibleNavLeaves / 5 mobileNavItems` |

近期产物还显示：

- `audit:e2e`：`34 routes / 0 failed / 0 issues`
- `audit:pipeline`：手表 TCP 探针、SQL flush、API 可见、画像页可见均通过
- `audit:pipeline-warning`：体温高危预警、风险列表、通知处理、SQL handled 均通过
- `audit:auth`：登录预填、登录提交、刷新恢复、退出、后端重启 stale token、并发 401 收敛均通过

关键产物路径：

- `D:/Health/HealthShow/tests/api/artifacts/2026-05-07T22-29-45-860Z/summary.md`
- `D:/Health/HealthShow/tests/e2e/artifacts/2026-05-07T22-19-53-162Z/summary.md`
- `D:/Health/HealthShow/tests/pipeline/artifacts/2026-05-07T20-17-47-153Z/summary.md`
- `D:/Health/HealthShow/tests/pipeline/artifacts/2026-05-07T20-18-23-921Z/warning-summary.md`

---

## 3. 后端代码评估

### 3.1 规模与复杂度信号

当前后端代码指标：

| 指标 | 当前值 |
| --- | ---: |
| Java 主代码文件 | `325` |
| Java 测试文件 | `33` |
| Controller 命中 | `26` |
| Service / Service interface 命中 | `48` |
| Mapper 命中 | `33` |
| `@Autowired` 命中 | `100` |
| `Map<String,Object>` / `List<Map<String,Object>>` 命中 | `289` |
| `catch (Exception)` 命中 | `121` |
| `CompletableFuture.supplyAsync` 命中 | `1` |
| `@Async` 命中 | `18` |
| `${...}` 命中 | `79` |

说明：

- `${...}` 命中里包含大量 `@Value("${...}")` 配置占位，不等同于全部都是 SQL 注入风险。
- 真正需要持续盯住的动态 SQL 是 `SqlExecutorMapper` 中为 AI Text2SQL 保留的 `${sql}`，当前已经由 `AiSqlGuard` 收口。
- `CompletableFuture.supplyAsync` 只剩统一执行器持有，说明异步查询入口治理有效。

### 3.2 当前最大后端文件

| 文件 | 行数 | 评估 |
| --- | ---: | --- |
| `DashboardServiceImpl.java` | `429` | 已从超级 service 降下来，但仍是 Dashboard 编排核心，后续应继续防膨胀 |
| `HeartRateMapper.java` | `428` | 指标 SQL 仍偏集中，是性能与契约第二轮候选 |
| `AiChatService.java` | `402` | AI 对话、SQL 生成、修复、解释仍集中，属于中高风险热点 |
| `DataProcessService.java` | `343` | 已从 600+ 行降下，当前可接受，但协议字段标准化仍可下沉 |
| `RiskWarningService.java` | `332` | 告警域逻辑仍有继续拆分空间 |
| `DeviceManagerService.java` | `322` | 后台设备管理域仍是后续结构治理候选 |
| `AiHealthReportService.java` | `319` | AI 报告链路功能多，后续应加强失败语义和缓存策略 |
| `WatchProtocolDecoder.java` | `315` | 协议解析核心，应补更细协议样例测试 |
| `DeepSeekClient.java` | `311` | AI 客户端仍承担同步、流式、异常处理等职责 |
| `SleepMapper.java` | `299` | 指标 SQL 第二轮候选 |

### 3.3 已经明显好转的部分

后端当前已经完成这些关键脱泥：

- `WatchDataHandler` 从超级入口类收口为 dispatcher，协议处理迁移到 `handler/watch/*`。
- `DataProcessService` 不再承接全部设备上下文、绑定、缓冲、健康预警、行为报警职责。
- `DashboardMapper` 已退出，Dashboard SQL 拆到 `DashboardCalendarMapper`、`DashboardEntryMapper`、`DashboardOverviewMapper`、`DashboardDepartmentMapper`。
- `DashboardController` 不再直接调用历史 mapper，缓存和查询编排下沉到 service。
- `TrendWarning`、`HealthPortrait`、`Statistics`、`Pressure`、`BloodOxygen`、AI 报告统计链路已经开始或完成 typed row 治理。
- Redis buffer 已处理坏 JSON 毒化问题，并区分 `old/new` 双队列。
- 双库请求侧、手表侧、Redis flush、分表任务均有自动化测试覆盖。
- AI SQL 已有 guard、硬限制、审计、自动修复计数和失败收敛。
- 关键 actuator metric family 已启动可见。

### 3.4 仍然存在的后端问题

这些不是 P0 致命问题，但会影响长期维护：

| 问题 | 风险 | 处理优先级 |
| --- | --- | --- |
| `Map<String,Object>` 仍有 `289` 处命中 | 字段契约仍可能靠人工记忆，低级拼写错误不易编译期发现 | P1 |
| `catch (Exception)` 仍有 `121` 处命中 | 局部吞异常、错误语义漂移、日志噪声仍可能回流 | P1 |
| `@Autowired` 仍有 `100` 处命中 | 构造依赖不显式，测试替换和循环依赖风险仍在 | P2 |
| AI 对话和 AI 报告服务仍偏大 | AI 超时、降级、审计、提示词、SQL 修复职责仍耦合 | P1 |
| 指标 mapper 仍偏大 | SQL 性能第二轮和字段契约第二轮还没结束 | P1 |
| 后台管理 controller/service 仍有不少 Map 和 broad catch | 核心治理已覆盖健康主域，后台域仍偏旧 | P2 |
| Actuator 有指标，但没有现场 dashboard/告警面板 | 可观测但不够可运营 | P2 |

### 3.5 后端结论

后端已经从“高风险不可控”降到“可维护、可继续治理”。

当前最大风险不再是“系统随时跑不起来”，而是：

- 后续新增需求如果不受门禁约束，`Map`、broad catch、超长 mapper/service 会回流。
- AI、指标、后台管理域还有结构债。
- 性能第二轮仍需要针对大视图、分表直查、慢 SQL 做现场数据量验证。

建议后端目标状态定义为：

- 核心健康域和 Dashboard 域保持当前边界不回退。
- 新增 API 禁止裸 `Map` 出口。
- 剩余 `Map` 和 broad catch 按域逐步压缩，而不是追求一次性清零。
- 对 AI、指标、预警、后台管理补第二轮契约和负向测试。

---

## 4. 前端代码评估

### 4.1 规模与结构信号

当前前端代码指标：

| 指标 | 当前值 |
| --- | ---: |
| `.vue` 文件 | `61` |
| `.js/.mjs` 文件 | `130` |
| `src/views/**/index.vue` 页面 | `27` |
| 统一请求入口实际 axios 使用 | 主要集中在 `src/utils/request.js` |
| `setInterval` 命中 | `3` |
| `setTimeout` 命中 | `5` |
| `addEventListener` 命中 | `3` |
| `echarts.init` 命中 | `20` |
| `audit:page-structure` 跟踪迁移页 | `21` |
| `legacyTrackedPages` | `0` |

说明：

- 直接 axios 风险目前可控，真实 axios 实例集中在 `src/utils/request.js`；`ai-chat` 使用 fetch 是为了流式响应，属于合理例外。
- 页面级副作用已经明显减少，但图表初始化仍分布在多个 chart/runtime 模块中。
- `audit:page-structure` 已经能阻止大页重新把 timer、listener、`echarts.init` 塞回 `index.vue`。

### 4.2 当前最大前端文件

| 文件 | 行数 | 评估 |
| --- | ---: | --- |
| `dashboard-chart-options.js` | `728` | 图表配置过大，后续可按图表主题再拆 |
| `blood-oxygen/index.vue` | `490` 到 `516` | 指标页仍偏重，page engine 第二轮候选 |
| `heart-rate/index.vue` | `465` 到 `500` | 指标页仍偏重，但已有共享 runtime |
| `blood-pressure/index.vue` | `458` 到 `493` | 指标页仍偏重 |
| `PersonDetailDrawer.vue` | `458` | Safety Command 弹窗复杂度较高 |
| `risk-warning-runtime.js` | `454` | 预警页 runtime 可继续分层 |
| `alert-management/records/index.vue` | `416` 到 `445` | 记录页仍偏重 |
| `device-management/index.vue` | `401` 到 `415` | 已拆 use-page/runtime，但页面壳仍偏大 |
| `login/index.vue` | `394` | 登录页视觉和逻辑仍可分离 |
| `workbench/index.vue` | `382` 到 `420` | 已拆 chart/view-model，但仍有继续抽象空间 |

### 4.3 已经明显好转的部分

前端当前已经完成这些关键收口：

- `src/router/app-routes.mjs` 成为业务路由事实源。
- `src/layout/menu/navigation.mjs` 派生侧栏和移动底栏。
- `audit:nav` 固化导航结构。
- `audit:page-structure` 固化 21 个页面的目录结构和 `index.vue` 上限。
- `dashboard`、`real-time`、`safety-command`、`health-portrait`、`employee-profile`、`report-center`、`risk-warning`、`sleep` 等页面已拆出 runtime/view-model/scss 或等价模块。
- 四个核心指标页已共享滚动、导出、数据加载、生命周期 mixin 和 chart/scss 模块。
- `ai-chat` 已拆出图表、导出、查询结果、会话快捷问题、文本渲染和样式模块。
- `createEventBinding`、`useIntervalTask`、`useTimeoutTask`、`useScrollLoop`、`task-timer` 等副作用工具已经形成。

### 4.4 仍然存在的前端问题

| 问题 | 风险 | 处理优先级 |
| --- | --- | --- |
| 指标页还没有完整 page engine | 新增指标页仍可能复制较多页面代码 | P1 |
| `dashboard-chart-options.js` 过大 | Dashboard 图表调整 diff 粗，认知成本高 | P1 |
| 多处 `echarts.init` 分散在 chart/runtime 里 | 图表 resize/dispose 仍可能出现不一致 | P1 |
| 管理后台页视觉仍偏 Element Plus 默认 | 功能可用，但产品识别度和高级感不足 | P1 |
| 大屏页视觉强但密度高 | 小屏或非标准分辨率可能出现裁切、拥挤 | P1 |
| 构建产物中 `echarts`、`xlsx`、`jspdf`、`html2canvas` 体积较大 | 首屏和弱机性能仍可能受影响 | P2 |
| 视觉回归主要靠 e2e DOM 检查，缺少截图基准 | 页面美观回退难以自动发现 | P2 |

### 4.5 前端结论

前端结构已经从“页面堆代码”改进到“多数核心页有目录规范和门禁”。

当前主要不足不是“页面不能用”，而是：

- 产品级视觉统一性不足。
- 大屏页和后台页的设计语言割裂。
- 指标页抽象还没彻底完成。
- 图表生命周期和视觉回归还需要第二轮固化。
- 构建体积需要性能预算和按需加载治理。

---

## 5. 健壮度评估

### 5.1 已达成的健壮度

当前健壮度已经具备工程级基础：

- 后端完整单测通过。
- 前端构建通过。
- 前端 API smoke 覆盖 53 项且全部通过。
- 前端路由/e2e 覆盖 34 条路由且无 issue。
- 登录态回归覆盖关键场景。
- 写入链路通过 TCP 主动探针验证，不再依赖模拟器恰好产生数据。
- 高危预警链路覆盖从 TCP 上报到 SQL、API、页面处理、SQL handled。
- 双库上下文、Redis buffer、AI SQL、异步查询、分表任务均已有回归。
- actuator metrics 可作为现场排障入口。

### 5.2 仍需补强的健壮度

| 领域 | 当前状态 | 需要补强 |
| --- | --- | --- |
| CI/自动门禁 | 本地脚本齐全 | 需要明确哪些进入提交前、哪些进入夜间或发布前 |
| 视觉回归 | e2e 主要查页面可达和 DOM issue | 需要关键页截图基准和移动端截图基准 |
| 性能回归 | API smoke 有耗时记录 | 需要 P95/P99 阈值和慢 SQL 排查清单 |
| 真实设备 | TCP 探针可跑 | 仍需真实手表 IMEI、真实网络、真实写新库验收 |
| 新库业务数据 | `health_new` 为空业务库 | 需要员工、部门、设备、绑定初始化流程 |
| AI 降级 | guard 和审计已有 | 仍需 AI 服务不可用、超时、限流时的前端体验基线 |

### 5.3 健壮度结论

健壮度已经达到“可继续开发和可回归”的水平。

距离“现场长期稳定运行”还差：

- 发布前门禁分层。
- 视觉和性能预算。
- 新库初始化和真实设备验收。
- AI 失败降级体验。
- 现场指标 dashboard / 告警面板。

---

## 6. 功能完善性评估

### 6.1 当前已覆盖的功能面

当前可见模块较完整：

- 安全指挥中心
- Dashboard / 健康大盘
- 实时监控
- 心率、压力、血压、血氧、睡眠分析
- 趋势预警
- 风险预警
- 消息通知
- 预警记录
- 阈值配置
- SOS 紧急救援
- 职工健康档案
- 员工画像
- 入井准入
- 工作台日历
- 报表中心
- AI 健康助手
- 设备管理
- 用户、角色、部门、工种管理

近期自动化证据显示：

- API 主链路 `53 checks` 通过。
- 页面路由 `34 routes` 通过。
- 写入链路通过 TCP 探针、SQL、API、画像页显示。
- 预警链路通过 TCP 高危探针、SQL 预警、API 列表、页面处理。

### 6.2 功能层面的不足

| 功能域 | 现状 | 缺口 |
| --- | --- | --- |
| 双库真实切换 | 技术链路已完成 | 真实手表写新库、模拟器写老库仍需现场验收 |
| 新库业务启用 | 新库只保留系统基础配置 | 需要员工、部门、设备、绑定导入/重建流程 |
| 告警运营 | 告警生成和处理可跑 | 需要现场处理 SLA、升级、通知闭环定义 |
| AI 报告 | 员工/部门/矿级报告可用 | 需要不可用降级、报告可信度提示、生成耗时控制 |
| 权限模型 | 登录和基础角色可用 | 需要真实角色权限矩阵验收 |
| 数据质量 | 流水可写可查 | 需要异常值、重复上报、设备解绑/换绑、离线补传策略 |
| 导出/报表 | 多页已有导出 | 需要大数据量导出、字段口径、现场模板确认 |

### 6.3 功能完善性结论

当前功能完善性已经达到“主要模块具备、主链路可跑、可进入现场验收准备”的水平。

但它还不是“现场业务完全闭环”的状态。

后续重点不应再是堆新菜单，而是：

- 把真实设备、新库基础数据、人员组织、设备绑定、告警处理、AI 报告、导出报表这些链路做成可验收流程。
- 每条流程都有固定数据、固定步骤、固定期望结果。
- 失败时有前端提示、后端日志、指标入口和恢复方案。

---

## 7. 页面美观度评估

### 7.1 当前视觉状态

当前页面不是统一水平。

可以分成三类：

1. 指挥中心 / Dashboard / 部分健康监测页：
   - 有明显行业大屏风格。
   - 深色、霓虹、卡片、图表、状态块已经形成视觉方向。
   - 适合现场演示，但密度偏高。
2. 健康分析 / 预警 / 报表 / AI：
   - 结构和功能较完整。
   - 图表和卡片已经比普通后台页更有信息表达。
   - 仍有页面间视觉语言不完全一致的问题。
3. 系统管理 / 后台管理页：
   - 仍偏 Element Plus 默认后台。
   - 可用性尚可，但产品识别度弱。
   - 表格、筛选、弹窗、空状态、移动端细节还需要统一。

### 7.2 页面美观度主要问题

| 问题 | 表现 | 后果 |
| --- | --- | --- |
| 设计语言割裂 | 大屏页很重，后台页较默认 | 产品整体感不足 |
| 信息密度偏高 | 指挥中心和复杂图表页容易拥挤 | 小屏/投屏/低分辨率时观感下降 |
| 缺少统一设计 token | 色彩、间距、阴影、标题层级散落 | 后续页面容易继续发散 |
| 图表视觉未完全统一 | 多处 chart option 独立维护 | 图表风格和交互不稳定 |
| 缺少截图级视觉门禁 | DOM/e2e 通过不代表美观不回退 | 视觉退化难以及时发现 |
| 移动端覆盖有限 | e2e 覆盖 4 个移动路由 | 其他复杂页移动端仍需人工复核 |

### 7.3 页面美观度结论

当前页面可用于功能演示和内部验收，但还未达到“完整产品级视觉统一”。

建议定义为：

- 当前：`可演示 / 可用 / 部分页面有风格`
- 目标：`统一视觉系统 / 关键页高级感稳定 / 管理页不再默认后台感 / 移动端无明显破版`

---

## 8. 分项评级

这里的评分不是绝对质量分，而是“离当前目标的距离”。

| 维度 | 当前判断 | 当前等级 | 达标要求 |
| --- | --- | --- | --- |
| 屎山代码可控性 | 核心高风险已收口，剩余中等结构债 | `B+` | 禁止回退，继续压缩 Map、catch、指标页复制 |
| 后端健壮度 | 主链路和 P0 风险有回归 | `A-` | 增加性能阈值、真实设备、新库验收 |
| 前端健壮度 | 构建、结构、API、路由、e2e 已有门禁 | `B+` | 增加视觉截图、移动端、体积预算 |
| 功能完善性 | 主模块完整，链路可跑 | `B` | 现场流程和新库初始化要闭环 |
| 页面美观度 | 关键页有风格，后台页仍普通 | `B-` | 统一设计 token、关键页视觉重做、管理页轻量美化 |
| 交接可维护性 | 文档多且近期同步 | `B+` | 新增本评估和目标实施计划作为新入口 |

---

## 9. 是否还需要继续执行旧两份文档

旧文档仍有价值，但不能再被当作“从头逐项执行”的任务清单。

### 9.1 `docs/archive/历史归档-治理清单.md`

继续保留。

用途：

- 作为历史问题总表。
- 作为已完成项和治理原则的事实来源。
- 作为后续发现回归时的排查索引。

不建议：

- 不要让新 Codex 从 P0-01 开始重新执行。
- 不要重复做已经完成的 DashboardMapper 拆分、WatchDataHandler 拆分、DataProcessService 拆分。

### 9.2 `docs/archive/历史归档-CODEX_MULTI_DAY_TASKBOOK.md`

继续保留。

用途：

- 作为第一轮治理执行手册和上下文。
- 作为 phase 交接格式参考。

不建议：

- 不要把它当成当前唯一执行入口。
- 不要要求 Codex “执行完所有旧 phase 才停”，因为很多 phase 已经完成，再执行会制造重复改动。

### 9.3 新执行入口

后续 goal-mode Codex 应优先读取：

1. `D:/Health/AGENTS.md`
2. `D:/Health/项目重新评估结果.md`
3. `D:/Health/目标达成实施计划.md`
4. `D:/Health/docs/archive/历史归档-HEALTH_HANDOFF.md`
5. `D:/Health/CODEX_TARGET_STATE_PLAN.md`
6. `D:/Health/docs/archive/历史归档-治理清单.md`
7. `D:/Health/docs/archive/历史归档-CODEX_MULTI_DAY_TASKBOOK.md`
8. 双库相关任务再读 `D:/Health/HealthData/DUAL_DB_CUTOVER.md`
9. 前端结构任务再读 `D:/Health/HealthShow/TECHNICAL_DOCS.md`

---

## 10. 最终判断

如果你的目标是“项目别再是一坨没人敢改的屎山，核心健壮度基本过关”，当前已经接近达成，并且可以合理对外说“基本可控”。

如果你的目标是“上线现场长期稳定运行、功能闭环、页面也像成熟产品”，当前还需要继续执行第二轮目标计划。

下一份文档 `目标达成实施计划.md` 应作为新 Codex goal-mode 的主执行文档。



