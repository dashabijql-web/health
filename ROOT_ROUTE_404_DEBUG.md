# 根入口 `#/404` 排障记录

日期：`2026-05-04`  
范围：`HealthShow` 前端开发环境 `http://localhost:9528/`

## 问题现象

- 浏览器访问 `http://localhost:9528/` 后，没有进入登录页或首页，而是落到 `#/404`
- 用户看到的是项目自带的 404 页面
- 再由权限守卫重定向后，经常会变成 `#/login?redirect=/404`

这类现象容易被误判成：

- 前端没启动
- 后端没启动
- 浏览器缓存脏了
- 刚提交的 CI 改崩了项目

这次真正的根因不是上面这些。

## 先确认的事实

排查前先确认运行态，不要先猜代码。

- 前端 `9528` 在监听
- 后端 `8080` 在监听
- TCP `9000` 在监听
- Redis `6379` 在监听
- 无头浏览器直接访问根地址时，页面资源可以正常返回 `200`

说明：

- 这不是“服务没起来”的问题
- 这不是静态资源 404
- 问题在前端路由入口链

## 实际定位过程

### 1. 先查端口和进程

先确认前后端和 Redis/TCP 是否存活：

- `Get-NetTCPConnection`
- `Get-Process`
- `Invoke-WebRequest http://localhost:9528/`

目的不是修问题，而是先排除“服务没启动”这种低级误判。

### 2. 锁定可疑文件

本次重点检查了这些文件：

- `src/main.js`
- `src/router/index.js`
- `src/router/app-routes.js`
- `src/permission.js`
- `src/views/404.vue`

原因：

- 根入口 `http://localhost:9528/` 的问题，一般只会出在启动入口、根路由、通配符路由、权限守卫、404 返回逻辑这几类位置

### 3. 不靠前台浏览器，改用后台无头复现

前台浏览器容易被这些东西干扰：

- 旧 Cookie
- 旧标签页恢复
- 旧 PWA / Service Worker
- 手工点击路径不稳定

所以这次用后台无头 Chrome 复现，并记录：

- 最终 URL
- 导航顺序
- 控制台日志
- 页面文本

### 4. 临时加路由调试日志

为了看清根入口到底怎么跳的，临时在开发环境加了日志：

- `src/router/index.js`
- `src/permission.js`

目标是抓这条链：

- `http://localhost:9528/`
- `#/`
- 到底为什么变成 `/404`
- 守卫为什么又把它送去 `/login?redirect=/404`

### 5. 抓到的关键导航链

实际日志显示：

1. 浏览器进入 `http://localhost:9528/`
2. 跳到 `http://localhost:9528/#/`
3. 路由守卫收到的目标却已经是 `/404`
4. 未登录逻辑把它重定向成 `/login?redirect=/404`

这说明核心不是权限守卫先跳错，而是：

`#/` 本身没有被正确匹配到根路由。

## 根因

根因在 `src/router/index.js`：

- 根路由原来写成了“只有 `beforeEnter` 的空路由”
- 同时项目里又有通配符：
  - `/:pathMatch(.*)* -> /404`

结果是：

- `#/` 没有被稳定地当成一个真正可匹配的根入口
- Vue Router 直接把它落给了通配符
- 通配符再把它送去 `/404`

也就是说，这次不是缓存先把页面带错，而是路由定义本身就有问题。

## 修复方案

### 1. 根路由改成真正的 `redirect`

修复前：

- 根路由依赖 `beforeEnter`
- 没有显式 `redirect`

修复后：

- 根路由改成显式 `redirect: resolveRootEntry`
- `resolveRootEntry()` 只返回：
  - 未登录：`/login`
  - 已登录：`/health-monitor/dashboard`

这样 `#/` 会先被稳定匹配到根路由，再按登录态跳转。

### 2. 保留启动时预注入业务路由

`src/main.js` 里继续保留：

- 启动时直接 `await ensureAppRoutes()`

原因：

- 这能避免登录后首跳业务页时出现 “No match found”
- 也能降低恢复会话和书签直达时的路由竞态

### 3. 修正 404 页返回按钮

`src/views/404.vue` 原来是：

- `href=""`

这会导致用户在 `#/404` 页面点击 `Back to home` 时只是原地刷新，容易继续卡死。

修复后改成：

- `router-link to="/"` 

这样它会回到真实根入口，再由根路由统一分发。

### 4. 删除临时排障脚本

排障过程中一度试过在 `index.html` 里做本地缓存/Service Worker 清理脚本。  
这个做法只适合作为临时诊断，不适合长期留在主链。

最终处理：

- 临时脚本已移除
- 临时调试日志已移除

主链只保留真正必要的修复。

## 验证结果

本次已验证：

- 未登录访问 `http://localhost:9528/` -> `#/login`
- 登录后再访问 `http://localhost:9528/` -> `#/health-monitor/dashboard`
- 登录态进入 `#/404` 后点击 `Back to home` -> 正常回到 `#/health-monitor/dashboard`
- `npm run build` 通过

## 这次踩过的坑

### 坑 1：先怀疑缓存，容易把方向带偏

缓存和 PWA 确实可能放大症状，但这次不是主因。  
真正的问题是根路由定义不稳。

结论：

- 先查运行态
- 再查路由定义
- 最后才处理缓存

### 坑 2：Hash 模式下也不能把根入口写成“伪路由”

很多人会以为 Hash 模式天然不会出入口问题。  
实际上 `#/` 仍然必须被一个真正的路由记录接住。

不要依赖这种写法：

- 只有 `beforeEnter`
- 没有实际 `redirect/component`

否则根路径仍可能被通配符吃掉。

### 坑 3：404 页面按钮写空链接会放大排障成本

`href=""` 在 404 页面上看起来像“返回首页”，实际上只是刷新当前错误页。  
这会让用户误以为“项目完全坏了”。

### 坑 4：不要把临时清缓存脚本直接当正式修复

清缓存脚本只能辅助判断，不是根因修复。  
如果把它长期留在 `index.html`，后面会引入新的不可控行为。

### 坑 5：本机 E2E 环境可能漂移

当前仓库里的 `npm run audit:e2e` 依赖 `playwright`，但本机项目 `node_modules` 里并没有这包。  
这次能完成无头验证，是借用了机器上现成的 `playwright` 包和系统 Chrome。

结论：

- E2E 能力不应只靠“机器上刚好装过”
- 后续要么给前端仓库补标准 `playwright` 依赖
- 要么统一审计运行环境

## 以后遇到同类问题，推荐顺序

1. 查端口和进程，确认不是服务没起来
2. 用无头浏览器抓根地址的最终 URL 和导航链
3. 优先看：
   - 根路由
   - 通配符路由
   - 权限守卫
   - 404 返回逻辑
4. 临时加开发环境日志，只抓导航链，不做大改
5. 修主链后，把临时日志和临时脚本撤掉
6. 再做登录态 / 未登录态 / 404 返回的三条回归

## 本次最终结论

这次 `http://localhost:9528/ -> #/404` 的主因是：

- 根路由使用了不稳定的“仅 `beforeEnter` 入口”
- 被通配符路由抢走

正确修法不是继续清缓存，而是：

- 把根路由改成真正的 `redirect`
- 保证业务路由在启动阶段已经可匹配
- 让 404 返回按钮走真实路由跳转
