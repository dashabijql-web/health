# Health 项目重新评估结果（旧库 health 基线）

评估日期：2026-05-08  
评估数据源：旧库 `health`  
评估前端数据源：`Health-Data-Source=old` / `X-Health-Data-Source=old`  
评估目的：在旧库有完整模拟数据和历史流水的前提下，重新判断代码屎山程度、健壮性、功能完善性、页面美观度和“很多组件空数据”问题。

## 1. 与上一轮新库评估的边界差异

上一轮 `D:/Health/项目重新评估结果.md` 主要站在双库切换后的目标态看项目，其中新库 `health_new` 是真实手表承接库，业务数据为空或接近空属于预期。

本文件不再按新库空库口径判断，而是按旧库 `health` 判断：

| 维度 | 新库评估口径 | 旧库评估口径 |
| --- | --- | --- |
| 数据状态 | 业务基础数据清空，保留登录、角色、工种、阈值 | 部门、员工、设备、设备绑定、历史健康流水、历史预警均有大量数据 |
| 页面空数据 | 很多页面空是预期，需要初始化真实业务数据 | 旧库下核心组件不应为空，空组件应视为接口、映射、过滤或前端适配问题 |
| 主要风险 | 新库初始化、真实手表写入、空库体验 | 高数据量查询性能、旧库数据质量、分表一致性、演示大屏可读性 |
| 验收目标 | 能从空库重建真实业务闭环 | 能稳定支撑模拟器、演示、压力数据、全页面非空和高数据量回归 |
| 结论性质 | “目标态规划 + 新库上线准备” | “旧库演示/压测/高密度数据基线评估” |

因此，本文件结论不能简单替代上一轮新库文档。两个文档分别服务不同场景：

- 新库文档：真实手表、空库启用、上线切换。
- 旧库文档：模拟器、演示环境、历史数据压力、页面组件非空、性能治理。

## 2. 本轮验证证据

### 2.1 运行环境

- 前端：`http://localhost:9528/`
- 后端：`http://localhost:8080/health`
- SQL Server：`localhost,58135`
- 旧库：`health`
- 前端当前顶栏：`老库`
- 模拟器进程：`watch_tcp_simulator_1000.py` 正在运行
- 旧库用途：继续承接模拟器数据
- 新库用途：真实手表数据，非本文件主评估对象

### 2.2 自动化验证结果

| 验证项 | 命令 | 结果 | 产物 |
| --- | --- | --- | --- |
| 后端单测 | `mvn -q test -f D:/Health/HealthData/pom.xml` | 通过 | 无单独报告 |
| API smoke | `npm run audit:api` | `53 checks / 0 failed / 0 warning` | `D:/Health/HealthShow/tests/api/artifacts/2026-05-08T01-46-18-315Z/summary.md` |
| 旧库数据密度 | `npm run audit:data` | `21 checks / 0 failed / 0 warning`，`data_source=old` | `D:/Health/HealthShow/tests/api/artifacts/2026-05-08T01-46-18-336Z/data-density.md` |
| 导航结构 | `npm run audit:nav` | `6 navGroups / 23 visibleNavLeaves / 5 mobileNavItems` | 控制台输出 |
| 鉴权回归 | `npm run audit:auth` | `7 checks / 0 failed` | `D:/Health/HealthShow/tests/e2e/artifacts/2026-05-08T01-47-03-903Z/auth-summary.md` |
| 页面 e2e | `npm run audit:e2e` | `34 routes / 0 issue` | `D:/Health/HealthShow/tests/e2e/artifacts/2026-05-08T01-47-57-461Z/summary.md` |
| 写入回归 | `npm run audit:write` | `5 checks / 0 failed / 0 warning` | `D:/Health/HealthShow/tests/api/artifacts/2026-05-08T01-47-56-330Z/summary.md` |
| 健康流水 pipeline | `npm run audit:pipeline` | `11 stages / 0 failed / 0 warning` | `D:/Health/HealthShow/tests/pipeline/artifacts/2026-05-08T01-50-47-416Z/summary.md` |
| 预警流水 pipeline | `npm run audit:pipeline-warning` | `12 stages / 0 failed / 0 warning` | `D:/Health/HealthShow/tests/pipeline/artifacts/2026-05-08T01-51-51-320Z/warning-summary.md` |
| 前端构建 | `npm run build` | 通过 | `dist/` |

### 2.3 旧库核心表统计

本轮 SQL 取样结果：

| 表 | 行数 | 说明 |
| --- | ---: | --- |
| `department` | 20 | 旧库组织树完整 |
| `employee` | 1000 | 旧库员工档案完整 |
| `device` | 1000 | 模拟设备完整 |
| `device_user` | 1000 | 员工和设备绑定完整 |
| `realtime_data` | 0 | 实时状态主表为空，需要注意接口语义不是直接依赖该表 |
| `user_online_status` | 0 | 在线状态主表为空，需要继续核对“在线=1000”的来源 |
| `health_record` | 0 | 主表为空，实际数据在月分表 |
| `warning_record` | 0 | 主表为空，实际数据在月分表 |
| `device_data_buffer` | 0 | 当前旧库缓冲表为空 |
| `ai_health_report` | 5 | 有少量 AI 报告记录 |

月分表取样：

| 分表 | 行数 |
| --- | ---: |
| `health_record_202602` | 108000 |
| `health_record_202603` | 2429036 |
| `health_record_202604` | 5550312 |
| `health_record_202605` | 3557541 |
| `warning_record_202602` | 8010 |
| `warning_record_202603` | 15830 |
| `warning_record_202604` | 46001 |
| `warning_record_202605` | 30758 |

取样时 `health_record_202605` 最新记录时间已到 `2026-05-08 09:46:58`，说明模拟器仍在向旧库写入。

### 2.4 旧库数据密度关键数值

`audit:data` 的关键旧库结果：

| 检查项 | 值 | 说明 |
| --- | ---: | --- |
| `dashboard.overview.heartRate` | 1422571 | 大盘心率汇总非空 |
| `dashboard.overview.bloodOxygen` | 711793 | 大盘血氧汇总非空 |
| `dashboard.body.avgHeartRate` | 80 | 平均心率非空 |
| `dashboard.body.avgBloodOxygen` | 96 | 平均血氧非空 |
| `realtime.online.total` | 1000 | 实时在线接口非空 |
| `realtime.statistics.todayRecords` | 3349290 | 当日记录量极高 |
| `risk-warning.total` | 76755 | 旧库风险预警非空 |
| `heart-rate.totalCount` | 3643217 | 心率域历史记录非空 |
| `pressure.totalCount` | 3643217 | 压力域历史记录非空 |
| `blood-pressure.totalCount` | 3643227 | 血压域历史记录非空 |
| `blood-oxygen.totalCount` | 1000 | 血氧接口有人员级统计 |
| `statistics.monthly-summary` | 1000 | 月报员工维度非空 |
| `trend-warning.list` | 770 | 趋势预警非空 |
| `employee.list-detail` | 1000 | 员工档案非空 |
| `health-record.total` | 11644678 | 健康记录总量千万级 |
| `device.online.count` | 1000 | 设备在线统计非空 |

结论：旧库下“很多页面很多组件空”的主问题已经不是普遍存在。当前更准确的问题是：

- 新库空数据属于预期。
- 旧库下核心组件已能非空。
- 旧库仍需要继续扩大数据密度门禁覆盖，防止未覆盖组件回退为空。

## 3. 总体结论

旧库基线下，项目已经不是“页面数据大面积缺失”的状态，也不是不可控屎山。

当前真实状态是：

- 旧库数据密度足够高，能支撑演示和压力回归。
- 核心页面路由能打开，34 条 e2e 路由 0 issue。
- API、鉴权、写入、健康流水、预警流水均有自动化回归。
- 代码结构已从“完全不可控”下降到“中等技术债、可继续治理”。
- 主要剩余问题从“有没有数据”转为“高数据量下能不能快、稳、可解释、好看”。

可以对旧库场景下的项目状态给出以下判断：

| 维度 | 旧库评分 | 结论 |
| --- | --- | --- |
| 数据可用性 | `A-` | 旧库核心业务数据完整，组件非空问题已明显收敛 |
| 代码屎山可控性 | `B+` | 核心高风险已拆出边界，但仍有大文件、大 SCSS、重复指标页和复杂 SQL |
| 后端健壮性 | `A-` | 单测、API、pipeline、指标暴露都有基础，旧库高数据量仍暴露性能风险 |
| 前端健壮性 | `B+` | 构建、路由、鉴权、数据密度、e2e 可跑，仍缺截图级视觉门禁 |
| 功能完善性 | `B+` | 旧库演示链路较完整，真实业务闭环和部分管理动作还需验收 |
| 页面美观度 | `B` | 大屏有统一风格，旧库有数据后观感明显改善，但信息密度和后台页仍需产品化 |
| 交接可维护性 | `B+` | 文档和门禁丰富，但需要明确区分新库与旧库执行入口 |

## 4. 代码屎山情况重新判断

### 4.1 已经改善的部分

本轮旧库验证能通过，说明前后端核心边界不是靠偶然跑通：

- `DataProcessService` 已不再无限膨胀，主类约 `343` 行，设备上下文、落库、预警等职责已拆出。
- 大盘、实时、指标页、预警、人员、设备等主要接口都纳入 API smoke。
- 旧库高数据量下，核心接口仍能返回，不是只有空库或小样本能跑。
- 前端路由、导航和权限已经形成结构审计。
- 数据源切换已集中在 `src/utils/data-source.js` 和请求拦截器，不再散落到各页面。
- 本地开发历史 `new` Cookie 导致误查新库的问题已修复。

### 4.2 仍然属于屎山残留的部分

| 残留点 | 证据 | 风险 | 优先级 |
| --- | --- | --- | --- |
| 大盘前端样式和图表配置过大 | `dashboard.scss` 约 942 行，`dashboard-chart-options.js` 约 728 行 | 后续改版容易牵一发动全身 | P1 |
| 后端 Dashboard 查询仍偏重 | `DashboardServiceImpl` 约 429 行 | 旧库高数据量下容易形成慢查询和缓存错配 | P1 |
| AI 服务仍复杂 | `AiChatService` 约 402 行 | SQL 生成、解释、图表、降级混在一起，后续难测 | P1 |
| 指标页仍存在重复结构 | 心率、血压、血氧、压力页面都有相似 UI/接口模式 | bug 修复容易漏一个页面 | P2 |
| Mapper SQL 和 Service 语义仍不够统一 | 分表、视图、直查、统计口径并存 | 旧库和新库切换时更容易出现口径漂移 | P1 |
| 大量展示逻辑仍在页面侧 | 多个页面的 view-model/runtime/scss 切分刚完成，稳定性还需观察 | 维护者需要理解拆分边界 | P2 |

### 4.3 屎山结论

旧库下项目不是“数据空壳掩盖屎山”，因为高数据量验证已经跑过。

但项目也不能说“屎山彻底清完”。更准确结论是：

- 不可控屎山已经降级为可控技术债。
- 核心链路有回归保护。
- 下一阶段应重点压缩大盘查询栈、大盘前端配置、指标页重复和 AI 服务复杂度。

## 5. 健壮性重新评估

### 5.1 已达成的健壮性

旧库场景下已经具备以下健壮性基础：

- 后端单测通过。
- 前端构建通过。
- API smoke 覆盖 53 个关键接口。
- 数据密度门禁覆盖 21 个旧库关键组件。
- 页面 e2e 覆盖 34 条路由，含桌面和部分移动端。
- 鉴权回归覆盖预填登录、刷新恢复、退出、后端重启、并发 401。
- TCP pipeline 覆盖健康流水写入。
- 预警 pipeline 覆盖告警生成和回归清理。
- `/actuator/metrics` 已有业务指标入口。
- 双库 header/cookie 路由已经具备基本自检能力。

### 5.2 旧库暴露的健壮性风险

| 风险 | 旧库证据 | 影响 | 优先级 |
| --- | --- | --- | --- |
| 高数据量查询慢 | `health-record.total` 约 5.2s，`sleep.detailList` 约 2.1s，`trend-warning.list` 约 2.1s，`heart-rate.totalCount` 约 1.9s | 演示和现场使用可能卡顿，未来数据继续增长后风险扩大 | P1 |
| 分表和主表语义割裂 | `health_record` 主表 0，月分表千万级；`warning_record` 主表 0，月分表十万级 | 新接口若误查主表会显示空 | P1 |
| 实时状态来源需要解释 | `realtime_data=0`、`user_online_status=0`，但接口显示在线 1000 | 维护者容易误判在线状态或排障方向 | P1 |
| 原始数据存在局部空指标 | 最新健康记录样本里多个指标为 `NULL` | 页面需要明确部分包/部分指标缺失是正常还是异常 | P2 |
| 温度原始值疑似缩放存储 | SQL 样本中 `temperature=364.0/370.0/375.0`，页面展示为 `36.x` | 后续新增查询容易忘记缩放规则 | P2 |
| 旧库统计口径和 raw count 不一致 | 分表预警 raw count 与接口 `risk-warning.total` 不完全一致 | 报表、趋势、待处理、全部预警需要统一口径说明 | P1 |
| 可视化回归不够强 | e2e 是 DOM/可访问性检查，不是截图 diff | 页面美观退化无法自动发现 | P2 |

### 5.3 健壮性结论

旧库下健壮性已经能支撑持续开发和演示，但还没达到“高数据量长期运行无忧”的程度。

下一阶段健壮性重点不是补“能不能打开页面”，而是：

- 慢接口预算化。
- 分表查询封装化。
- 数据缺失和缩放规则文档化、DTO 化。
- 页面空态白名单化。
- 视觉截图门禁化。

## 6. 功能完善性重新评估

### 6.1 旧库下已较完整的功能

| 功能域 | 旧库表现 | 结论 |
| --- | --- | --- |
| 统一管控大盘 | 人员、设备、预警、趋势、部门、排行均能出数据 | 演示可用 |
| 实时监控 | 在线设备 1000，今日记录量千万级口径可展示 | 演示可用，但实时状态来源需澄清 |
| 心率/压力/血压/血氧 | 指标统计、趋势、分布、排行、部门统计均有返回 | 主体可用 |
| 风险预警 | 预警总量、趋势、部门、类型分布均有数据 | 主体可用 |
| 趋势预警 | 列表 770 行 | 可用，性能需关注 |
| 人员档案 | 1000 名员工、详情可打开 | 可用 |
| 健康画像 | 可从员工样本进入 | 可用 |
| 设备管理 | 1000 设备、在线统计可见 | 可用 |
| 报表中心 | 月度/日度统计可返回 | 可用，导出和口径仍需验收 |
| AI 健康助手 | 有少量报告数据，AI 入口可用 | 可演示，但依赖外部模型和降级策略 |
| 后台管理 | 用户、角色、部门、工种、设备列表可打开 | 基础可用，CRUD 需专项验收 |

### 6.2 旧库下仍未完全闭环的功能

| 功能缺口 | 说明 | 优先级 |
| --- | --- | --- |
| 预警处理闭环 | 旧库有大量预警，但处理、批量处理、通知、SOS、记录一致性需要专项验收 | P1 |
| 入井准入闭环 | 页面显示达标/禁入，但规则、复核、人员名单、处理动作需验收 | P1 |
| 报表导出闭环 | 数据可查，但导出字段、时间范围、统计口径、性能需验收 | P1 |
| AI 报告闭环 | AI 报告数少，模型不可用或慢响应时的用户体验需验收 | P2 |
| 后台 CRUD 闭环 | 列表可打开不等于新增、编辑、禁用、删除、权限刷新都完整 | P1 |
| 旧库/新库切换一致性 | 旧库有数据，新库空数据；所有页面都要明确显示当前数据源和空态说明 | P1 |

### 6.3 功能完善性结论

旧库下功能完善性高于新库下的观感。旧库已经可以用于“演示主要业务模块”和“回归模拟器链路”。

但如果目标是交付级产品，还需要补齐：

- 处理动作是否真的落库。
- 导出和报表是否符合现场口径。
- 预警、SOS、准入、AI 这些业务闭环是否有操作证据。
- 后台管理是否具备完整 CRUD 和权限刷新链路。

## 7. 页面美观度重新评估

### 7.1 旧库有数据后的页面观感

旧库切换后，大盘从大量空卡片变为：

- 顶部 KPI 有 1000 人、200 条新增/待处理、健康达标率、班前达标率。
- 左侧体征评估显示均值和状态。
- 部门综合看板有柱状图。
- 中间趋势图、预警时段、设备状态、环境关联有可见数据。
- 右侧异常人员排行、健康贴士、指标预警率、班前准入、AI 摘要均不再是大面积空。

这说明上一轮截图中的“空”主要是数据源误在新库，而不是页面组件都坏了。

### 7.2 美观度优点

- 大屏风格统一，颜色、边框、卡片、荧光线条一致。
- 关键页已经具备行业大屏感，不是 Element Plus 默认后台。
- 旧库有数据后，页面信息密度和演示效果明显提升。
- 桌面和移动基础路由都通过 e2e。
- 健康贴士、AI 摘要、排行、趋势等模块让页面不再只是表格堆叠。

### 7.3 美观度问题

| 问题 | 表现 | 优先级 |
| --- | --- | --- |
| 信息密度过高 | 大盘在 3440 宽屏可看，普通屏和移动端可能拥挤 | P1 |
| 视觉层级仍偏满 | KPI、图表、右侧栏、底部设备状态同时抢视觉焦点 | P2 |
| 后台管理页产品感弱 | 设备、用户、角色、部门、工种仍偏传统表格后台 | P2 |
| 图表截图级回归缺失 | e2e 不校验颜色、布局、遮挡、溢出 | P1 |
| 空态文案需区分数据源 | 新库空态和旧库异常空态需要不同文案 | P1 |
| SCSS 过大 | 大盘样式文件约 942 行 | P2 |

### 7.4 页面美观度结论

旧库下页面美观度比新库空数据截图更接近可演示状态，可以评为 `B`。

但还不能称为“产品级完成”，因为：

- 缺少截图级视觉门禁。
- 管理后台页仍普通。
- 大屏信息密度需要按 1920、1366、移动端分别调优。
- 设计 token 和组件化程度还不够。

## 8. 旧库特有风险清单

### 8.1 分表风险

旧库的业务流水主要在 `health_record_20*` 和 `warning_record_20*`，主表为 0。任何新接口或修复如果直接查主表，都可能再次导致页面空。

必须固化规则：

- 健康记录查询必须走当前月分表、跨月分表或已维护视图。
- 预警查询必须明确是查主表、分表、视图还是 union。
- 新增健康字段时必须同步月分表、视图、存储过程、Mapper、DTO、前端绑定。

### 8.2 旧库在线状态风险

当前 `realtime_data` 和 `user_online_status` 为 0，但 API 显示在线设备/在线人员为 1000。需要明确这不是 bug 还是统计口径：

- 如果在线来自最近健康记录，应把接口命名和文档说明补齐。
- 如果在线本应来自状态表，应修复模拟器或后端状态刷新。
- 如果两者都存在，应给出优先级和过期时间规则。

### 8.3 高数据量性能风险

旧库千万级数据暴露了真实压力：

- `health-record.total` 约 5.2 秒。
- `sleep.detailList` 约 2.1 秒。
- `trend-warning.list` 约 2.1 秒。
- `heart-rate.totalCount` 约 1.9 秒。

这些接口当前能跑通，但还不是长期可接受性能。

### 8.4 数据质量风险

最新样本存在局部指标 `NULL`，温度原始值以 `364.0`、`370.0` 形式存储。需要把缺失字段、缩放字段、异常值处理做成统一数据契约，避免各接口自行解释。

### 8.5 数据源误判风险

本轮已经发生过前台 Chrome 仍在 `新库` 导致误以为“没数据”的问题。已通过本地开发初始化修复缓解，但协作时仍要强制确认：

- 顶栏显示 `老库`。
- 请求头或响应头为 `X-Health-Data-Source: old`。
- `audit:data` 报告里 `data_source: old`。

## 9. 优先级结论

### P0：当前没有阻断演示的旧库 P0

理由：

- 页面可打开。
- 核心 API 非空。
- e2e 0 issue。
- pipeline 0 failed。
- 旧库数据已经恢复显示。

### P1：必须继续治理

| P1 项 | 目标 |
| --- | --- |
| 扩展旧库数据密度门禁 | 覆盖所有关键页面的关键组件，尤其管理页、AI、报表、准入、SOS |
| 慢接口预算化 | 将旧库高数据量接口纳入性能阈值，避免 5 秒级接口继续恶化 |
| 分表查询契约 | 防止新代码误查空主表 |
| 实时状态语义统一 | 解释或修复在线状态来源 |
| 业务闭环验收 | 预警处理、准入、导出、AI、后台 CRUD 必须有操作证据 |
| 视觉截图门禁 | 大屏、美观、移动端不能只靠 DOM e2e |

### P2：持续优化

| P2 项 | 目标 |
| --- | --- |
| 大盘样式和图表配置继续拆分 | 降低改版成本 |
| 指标页 engine 化 | 减少心率/血压/血氧/压力重复 |
| AI 服务拆分 | 降低 SQL、图表、解释、会话耦合 |
| 后台页轻量产品化 | 提升长期使用体验 |
| 文档自动更新 | 避免 `TECHNICAL_DOCS.md` 与代码继续漂移 |

## 10. 最终判断

按旧库 `health` 重新评估后，可以得出更清晰的判断：

1. “没数据”不是旧库真实状态，而是前端当时仍在新库。
2. 旧库下核心页面和组件已经大面积有数据，`audit:data` 证明 21 个关键检查全通过。
3. 项目屎山问题已经基本降到可控技术债，不再是没人敢改的状态。
4. 健壮性已经具备工程级回归基础，但高数据量性能和分表语义仍是下一阶段最大风险。
5. 功能完善性在旧库演示场景下较好，但真实业务动作闭环仍需专项验收。
6. 页面美观度在有数据后明显可演示，但离产品级视觉一致性仍有距离。

因此，旧库基线下的总体结论是：

> 项目已经具备旧库演示、模拟器压测和主要功能回归的基础；“页面很多组件空”的旧库问题已基本收敛。但如果目标是把代码健壮度、功能闭环和页面美观度推进到长期可交付状态，还必须继续执行旧库专项实施计划。

下一份文档 `D:/Health/旧库目标达成实施计划.md` 是旧库专项治理的执行入口。
