# Health 旧库评估过程方法与踩坑记录

记录日期：2026-05-08  
适用场景：按旧库 `health` 评估页面数据密度、代码健壮度、功能完善性、页面美观度，尤其排查“页面很多组件没数据”。

## 1. 这份文档解决什么问题

这次问题表面是“页面怎么没数据”，实际原因是前端仍选中了 `新库`，而新库 `health_new` 当前业务数据为空属于预期。

后续再做旧库评估时，必须先把“数据源是否真的是 old”确认清楚。否则会把新库空态误判为旧库页面、接口或代码问题。

本文件记录可复用过程、判断方法和本轮踩过的坑。

## 2. 标准处理流程

### 2.1 先确认当前目标

旧库目标：

- 使用数据库：`health`
- 前端数据源：`old`
- 模拟器：应运行
- 预期：核心页面大部分组件应有数据

新库目标：

- 使用数据库：`health_new`
- 前端数据源：`new`
- 模拟器：应停止
- 预期：员工、部门、设备等业务数据可为空

这两个目标不能混在一起判断。

### 2.2 确认服务和模拟器

```powershell
Get-Process | Where-Object { $_.ProcessName -match 'java|node|python' } |
  Select-Object Id,ProcessName,StartTime,Path |
  Format-Table -AutoSize
```

重点看：

- 后端 Java 是否运行。
- 前端 Vite node 是否运行。
- `watch_tcp_simulator_1000.py` 是否运行。

如果做旧库演示或旧库数据密度检查，模拟器应该运行。  
如果做新库真实手表或空库验收，模拟器应该停止。

### 2.3 先看页面右上角数据源

页面右上角：

- `老库` = `old` = `health`
- `新库` = `new` = `health_new`

截图、肉眼判断、接口判断必须一致。不要只看页面内容。

### 2.4 用接口或审计脚本确认 data_source

最稳妥方式：

```powershell
cd D:/Health/HealthShow
npm run audit:data
```

报告里必须出现：

```text
data_source: old
expect_non_empty: true
failed: 0
```

如果报告不是 `old`，不要继续分析页面空数据，先修数据源。

### 2.5 查询旧库核心表

```powershell
sqlcmd -S localhost,11433 -U sa -P [REDACTED] -d health -Q "SET NOCOUNT ON; SELECT 'department' AS table_name, COUNT(*) AS row_count FROM department UNION ALL SELECT 'employee', COUNT(*) FROM employee UNION ALL SELECT 'device', COUNT(*) FROM device UNION ALL SELECT 'device_user', COUNT(*) FROM device_user UNION ALL SELECT 'realtime_data', COUNT(*) FROM realtime_data UNION ALL SELECT 'user_online_status', COUNT(*) FROM user_online_status;"
```

旧库本轮基线：

- `department=20`
- `employee=1000`
- `device=1000`
- `device_user=1000`
- `realtime_data=0`
- `user_online_status=0`

注意：`realtime_data=0` 和 `user_online_status=0` 不等于页面实时接口一定没数据，当前接口在线统计还有其他来源或口径，需要单独确认。

### 2.6 查询分表，不要只查主表

旧库主表可能为空：

- `health_record=0`
- `warning_record=0`

真实历史数据在月分表：

- `health_record_202602`
- `health_record_202603`
- `health_record_202604`
- `health_record_202605`
- `warning_record_202602`
- `warning_record_202603`
- `warning_record_202604`
- `warning_record_202605`

查询分表行数：

```powershell
$q = @"
SET NOCOUNT ON;
DECLARE @sql nvarchar(max) = N'';
SELECT @sql = @sql + N'SELECT ''' + name + N''' AS table_name, COUNT_BIG(*) AS row_count FROM dbo.' + QUOTENAME(name) + N' UNION ALL '
FROM sys.tables
WHERE name LIKE 'health_record_20%' OR name LIKE 'warning_record_20%'
ORDER BY name;
IF LEN(@sql) > 0 BEGIN
  SET @sql = LEFT(@sql, LEN(@sql) - 10);
  EXEC sp_executesql @sql;
END
"@
sqlcmd -S localhost,11433 -U sa -P [REDACTED] -d health -Q $q
```

本轮旧库取样显示 `health_record_202605` 正在持续写入，最新记录到 `2026-05-08 09:46:58`。

### 2.7 自动化验证顺序

旧库完整验证建议顺序：

```powershell
cd D:/Health/HealthShow
npm run audit:api
npm run audit:data
npm run audit:nav
npm run audit:auth
npm run audit:e2e
npm run audit:write
npm run audit:pipeline
npm run audit:pipeline-warning
npm run build
```

后端：

```powershell
$env:JAVA_HOME='C:/Program Files/Java/jdk-17'
D:/apache-maven-3.8.1/bin/mvn -q test -f D:/Health/HealthData/pom.xml
```

本轮旧库验证结果：

- `audit:api`：`53 checks / 0 failed / 0 warning`
- `audit:data`：`21 checks / 0 failed / 0 warning`
- `audit:nav`：`6 navGroups / 23 visibleNavLeaves / 5 mobileNavItems`
- `audit:auth`：`7 checks / 0 failed`
- `audit:e2e`：`34 routes / 0 issue`
- `audit:write`：`5 checks / 0 failed / 0 warning`
- `audit:pipeline`：`11 stages / 0 failed / 0 warning`
- `audit:pipeline-warning`：`12 stages / 0 failed / 0 warning`
- `mvn -q test`：通过
- `npm run build`：通过

## 3. 判断“没数据”的方法

### 3.1 先判断是不是新库

如果页面很多组件是 0 或暂无数据，先看：

- 顶栏是否显示 `新库`。
- Cookie 是否有 `Health-Data-Source=new`。
- 请求头是否是 `X-Health-Data-Source: new`。
- 响应头是否回写 `X-Health-Data-Source: new`。
- `audit:data` 是否显示 `data_source: old`。

如果当前是 `new`，不能直接说旧库页面没数据。

### 3.2 再判断旧库是否有基础数据

旧库至少应有：

- 部门。
- 员工。
- 设备。
- 设备绑定。
- 健康记录月分表。
- 预警记录月分表。

如果这些为空，才是旧库数据源或模拟器问题。

### 3.3 再判断接口是否有数据

用 `audit:data` 判断比肉眼看页面更可靠，因为它会带 `old` 数据源并输出具体值。

关键结果示例：

- `realtime.online.total=1000`
- `risk-warning.total=76755`
- `health-record.total=11644678`
- `device.online.count=1000`
- `employee.list-detail=1000`

### 3.4 最后判断前端组件是否渲染

如果接口有数据但页面空，再查：

- 页面是否请求了正确接口。
- 字段名是否映射错误。
- 前端是否把 `0/null/undefined` 混用。
- 是否 loading 状态没有结束。
- 图表容器宽高是否为 0。
- 数据源切换后是否没刷新。

## 4. 本轮踩坑记录

### 坑 1：前端历史 Cookie 把页面锁在新库

现象：

- `.env.development` 已经设置 `VITE_DEFAULT_DATA_SOURCE=old`。
- 文档也要求切到旧库。
- 但截图右上角仍显示 `新库`。
- 页面大量组件为空。

原因：

- 浏览器历史 Cookie 里仍有 `Health-Data-Source=new`。
- 前端 `getDataSource()` 优先读 Cookie，导致默认 old 不生效。

处理：

- 已修复 `D:/Health/HealthShow/src/utils/data-source.js`。
- 本地开发默认老库时，如果浏览器历史遗留 `new` 且没有初始化标记，会自动重置为 `old`。
- 手动从顶栏切新库仍然允许。

后续判断：

- 看到页面空，先看右上角数据源，不要直接查后端。

### 坑 2：新库空数据不是 bug

新库 `health_new` 当前业务数据清空是设计目标：

- `department=0`
- `employee=0`
- `device` 基本为空或仅真实手表少量记录
- `realtime_data=0`
- `user_online_status=0`

新库用于真实手表重建业务数据，不用于模拟器演示。  
把新库空态当作旧库 bug，会把排查方向带偏。

### 坑 3：旧库主表为空但分表有千万级数据

现象：

- 查 `health_record` 得到 0。
- 查 `warning_record` 得到 0。
- 但页面和接口有大量历史数据。

原因：

- 旧库真实业务流水在 `health_record_20*` 和 `warning_record_20*` 月分表。

后续规则：

- 新接口不能只查主表。
- 排查旧库健康/预警数据时必须查分表或统一视图。
- 新增健康字段时必须同步分表、视图、存储过程、Mapper、DTO、前端绑定。

### 坑 4：`warning_record` 字段名不能凭记忆写

本轮第一次 SQL 明细查询用了：

- `warning_value`
- `handled`

旧库实际字段是：

- `indicator_value`
- `is_handled`

处理方法：

```powershell
sqlcmd -S localhost,11433 -U sa -P [REDACTED] -d health -Q "SELECT COLUMN_NAME, DATA_TYPE FROM INFORMATION_SCHEMA.COLUMNS WHERE TABLE_NAME='warning_record' ORDER BY ORDINAL_POSITION;"
```

后续规则：

- 不确定字段名时先查 `INFORMATION_SCHEMA.COLUMNS`。
- 不要靠实体类或旧文档猜数据库真实字段。

### 坑 5：实时状态表为空不等于实时页面一定错

本轮 SQL 显示：

- `realtime_data=0`
- `user_online_status=0`

但旧库接口显示：

- 在线设备/人员约 1000。
- 今日记录数约 300 万级。

说明当前实时页面统计可能来自最近健康记录、设备表、Redis、缓存或其它口径。  
这不是可以直接判定的 bug，但必须作为后续 Phase O1 的口径澄清任务。

### 坑 6：温度字段有缩放规则

SQL 样本中温度可能是：

- `364.0`
- `370.0`
- `375.0`

页面展示应是：

- `36.4`
- `37.0`
- `37.5`

后续新增查询或报表时，不能忘记缩放规则。

### 坑 7：部分健康指标为 NULL 是真实样本特征

最新记录样本中有些行只包含部分指标，例如只有温度、只有心率血压、血氧为空。

后续页面处理：

- 不能把单个指标 `NULL` 直接当整条数据无效。
- 也不能用 `0` 掩盖缺失。
- DTO 和前端空态要区分“未采集”和“异常值”。

### 坑 8：PowerShell `Select-Object -Index 128..145` 写法会报错

错误写法：

```powershell
Get-Content file.md | Select-Object -Index 128..145
```

正确写法：

```powershell
Get-Content file.md | Select-Object -Index (128..145)
```

### 坑 9：直接向 Chrome 地址栏粘贴 javascript 片段不可靠

本轮尝试用 `javascript:(()=>{...})()` 设置 Cookie，结果 Chrome 把内容当成 Google 搜索打开。

原因：

- Chrome 对地址栏粘贴 `javascript:` 有安全限制。
- 如果没有严格保留 `javascript:` 前缀或页面焦点不对，会变成搜索。

后续不要依赖这种方式。优先：

- 用页面右上角下拉切换。
- 用 Playwright 控制的页面 `document.cookie`。
- 修前端初始化逻辑。
- 刷新后用截图或 `audit:data` 确认。

### 坑 10：坐标点击下拉不稳定

本轮用 UIA 坐标点击顶栏下拉，某些点击没有展开选项。

原因：

- 浏览器缩放、窗口位置、Element Plus 弹层、DPI 都可能影响坐标。

后续优先：

- 使用 Playwright snapshot/click。
- 或直接修 `data-source.js` 初始化逻辑。
- 或让用户手动点下拉确认。

### 坑 11：`D:/Health` 不是 git 仓库

规则：

- `D:/Health` 本身不是 git 仓库。
- `D:/Health/HealthShow` 是 git 仓库。
- `D:/Health/HealthData` 是 git 仓库。

后续看状态必须分别执行：

```powershell
git -C D:/Health/HealthShow status --short
git -C D:/Health/HealthData status --short
```

不要在 `D:/Health` 根目录期待 `git diff` 管理所有文档。

### 坑 12：工作树非常脏，不要误回滚

当前 `HealthShow` 和 `HealthData` 都有大量未提交改动。  
这些默认视为用户现有工作或前序 Codex 工作，禁止无理由回滚。

后续改代码：

- 只改任务相关文件。
- 不用 `git reset --hard`。
- 不用 `git checkout --`。
- 不要清理未跟踪文件，除非用户明确要求。

## 5. 推荐排查决策树

### 页面空数据

1. 看顶栏是否 `老库`。
2. 跑 `npm run audit:data`。
3. 确认 `data_source: old`。
4. 查旧库基础表。
5. 查月分表。
6. 查接口返回。
7. 查前端绑定和空态。

### 接口为空

1. 看请求头 `X-Health-Data-Source`。
2. 看响应头 `X-Health-Data-Source`。
3. 查是否误查主表。
4. 查分表是否有当月数据。
5. 查 Mapper 字段名。
6. 查 Service 缓存是否返回旧空值。

### 页面有数据但慢

1. 看 `audit:data` 每项耗时。
2. 找超过 1500ms 的接口。
3. 看 SQL 是否全表或跨月扫描。
4. 看是否每次强制 count 全量。
5. 看是否已有 TTL 缓存。
6. 优先优化旧库千万级路径，不要只用新库空数据验证。

### 双库行为不符合预期

1. 确认环境变量。
2. 确认 Cookie。
3. 确认请求头。
4. 确认响应头。
5. 确认用户是否有切库权限。
6. 确认 `HealthDataSourceRequestFilter` 是否早于 Sa-Token MVC 拦截器执行。

## 6. 后续 Codex 执行建议

如果目标是旧库专项治理，新会话应先读：

1. `D:/Health/AGENTS.md`
2. `D:/Health/旧库项目重新评估结果.md`
3. `D:/Health/旧库目标达成实施计划.md`
4. `D:/Health/旧库评估过程方法与踩坑记录.md`

启动后第一件事：

```powershell
cd D:/Health/HealthShow
npm run audit:data
```

如果不是 `data_source: old`，先修数据源，不要继续做旧库评估。

## 7. 本轮产出的旧库专项文档

- `D:/Health/旧库项目重新评估结果.md`
- `D:/Health/旧库目标达成实施计划.md`
- `D:/Health/旧库评估过程方法与踩坑记录.md`

三者关系：

- 评估结果：回答旧库当前真实状态。
- 实施计划：回答旧库后续怎么做到目标态。
- 本文件：回答过程怎么复用、哪些坑不要再踩。
