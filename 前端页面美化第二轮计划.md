# 前端页面美化第二轮计划

## 文档定位

本文档用于承接 `前端页面美化计划.md` 的第一轮落地结果，定义下一轮执行范围、问题优先级和验收口径。

本轮计划不再讨论“要不要统一视觉底座”，而是基于已完成的第一轮样式底座和 `dashboard` 样板页，继续收口：

- 移动端顶部拥挤与文本重叠
- Cockpit 线页面之间的头部、KPI、面板语言不一致
- 预警工作页与报表/AI 页仍然存在各写一套的问题
- 视觉审计对关键页面的移动端仍未通过

## 当前基线

截至 `2026-05-19`，已完成的第一轮事实：

- 已统一第一批全局 token、字体、阴影、圆角和滚动条规则
- 已重做桌面侧栏、顶栏、移动端底栏的基础视觉壳层
- 已移除移动端底栏 emoji，切换为正式图标
- 已沉淀第一批共享壳组件：
  - `PageHeroHeader`
  - `MetricStrip`
  - `PanelShell`
  - `PageEmptyState`
- `dashboard` 已接入新 hero / metric strip / empty state，并完成第一轮色系统一

当前代码落点主要集中在：

- `HealthShow/src/styles/index.scss`
- `HealthShow/src/styles/health-monitor.scss`
- `HealthShow/src/styles/sidebar.scss`
- `HealthShow/src/layout/*`
- `HealthShow/src/components/health-shell/*`
- `HealthShow/src/views/health-monitor/dashboard/*`

## 当前阻塞与证据

### 1. 全量视觉审计仍失败

参考：

- `HealthShow/tests/visual/artifacts/2026-05-19T08-02-49-843Z/layout-summary.md`

主要结论：

- `failed_routes = 8`
- 失败主体几乎都集中在 `mobile-390 / mobile-414`
- `text_overlap = 54`
- 受影响页面：
  - `safety-command`
  - `dashboard`
  - `real-time`
  - `risk-warning`
  - `alert-notifications`
  - `alert-records`
  - `report-center`
  - `ai-chat`

### 2. 第一轮定向复核后仍有残留

参考：

- `HealthShow/tests/visual/artifacts/2026-05-19T08-06-34-830Z/layout-summary.md`

主要结论：

- `dashboard`：
  - 移动端 hero 标题 / 页面标题仍会触发 `text_overlap`
  - 桌面端仍有一个 `chart_container_too_small`
- `risk-warning`：
  - `mobile-414` 顶部标题与副标题仍触发 `text_overlap`

说明：

- 这说明第一轮改造方向是对的，但“新壳层 + 老页面局部头部”还没有完全解耦
- 第二轮必须从“移动端头部模板”和“Cockpit 线统一页头”两个层面一起解决，而不是继续逐个页面打补丁

## 第二轮目标

### 总目标

把第一轮“底座统一 + dashboard 样板页”推进为“可复用的页面体系”，让核心页面真正进入稳定可扩展状态。

### 具体目标

- 让核心 Cockpit 页的页头、KPI、面板标题和移动端折叠策略统一
- 让预警工作页和报表/AI 页进入同一条 Workbench 视觉线
- 显著减少视觉审计中的 `text_overlap`
- 不为了美化继续引入新的页面风格分叉

## 第二轮范围

### A. Cockpit 线继续推进

优先页面：

- `dashboard`
- `real-time`
- `safety-command`
- `risk-warning`

目标：

- 统一头部 hero 口径
- 统一 KPI 条表达
- 统一面板标题区和状态 badge
- 统一移动端压缩策略

### B. Workbench 线开始模板化

优先页面：

- `alert-notifications`
- `alert-records`
- `report-center`
- `ai-chat`

目标：

- 统一列表页顶部结构
- 统一筛选区、导出区、空态、禁用态
- 统一“业务说明 + 操作区 + 主内容区”的组织方式

## 第二轮执行计划

## Phase R2-1：移动端头部模板收口

目标：先解决最明显的视觉审计失败源。

涉及文件优先级：

- `HealthShow/src/styles/health-monitor.scss`
- `HealthShow/src/components/health-shell/PageHeroHeader.vue`
- `HealthShow/src/components/WarningCenterNav.vue`
- `HealthShow/src/layout/components/MobileBottomNav.vue`

执行内容：

- 为 `PageHeroHeader` 增加明确的移动端模式：
  - `eyebrow` 在移动端默认可折叠或隐藏
  - `meta` 与 `actions` 允许独立换行
  - 标题、副标题、说明文案在窄屏下有固定优先级
- 为 `WarningCenterNav` 增加移动端水平滚动标签模式
- 明确页面顶部和移动端底栏之间的安全区关系
- 建立统一规则：
  - 移动端优先保标题、关键状态、一级操作
  - 副标题、说明文案、次级 tabs 可以折叠或横滑

验收目标：

- 至少解决 `dashboard / risk-warning / alert-notifications / alert-records` 的顶部重叠类问题

## Phase R2-2：Cockpit 页头和 KPI 体系统一

目标：把 `dashboard` 的做法沉淀成 Cockpit 线公共模式。

涉及页面：

- `dashboard`
- `real-time`
- `safety-command`
- `risk-warning`

建议抽象：

- `CockpitHeroHeader`
- `CockpitMetricStrip`
- `CockpitPanelHeader`

执行内容：

- 把四个页面顶部拆成一致的结构层级：
  - 页面身份
  - 当前状态
  - 时间范围切换
  - 首屏关键 KPI
- 限制首屏 KPI 数量和副文案长度
- 避免“页标题 + 模块标题 + 副标题”重复表达同一语义
- 统一高危、待处理、异常、在线、达标等主状态颜色

特别注意：

- `dashboard` 当前桌面端 `chart_container_too_small` 需要一并处理
- `real-time` 当前更需要“工具条 + 列表”主次优化，而不是继续堆大标题
- `safety-command` 当前首屏存在标题与 `SOS` 区域移动端撞位，说明头部与操作位没有分层

## Phase R2-3：预警工作页模板化

目标：把预警工作页从“独立风格页面”收成一个系列。

涉及页面：

- `alert-notifications`
- `alert-records`
- `risk-warning`

建议抽象：

- `WarningPageShell`
- `WarningFilterToolbar`
- `WarningStatusSummary`

执行内容：

- 统一 `WarningCenterNav` 下方的页面标题区
- 统一列表页顶部：
  - 标题
  - 描述
  - 筛选
  - 导出
  - 批量动作
- 统一 SLA / 待处理 / 已处理 / 高危语义块
- 移动端优先保留：
  - 页签
  - 主标题
  - 筛选入口
  - 列表卡片

## Phase R2-4：Workbench 线样板页落地

目标：确定工作页的稳定视觉模板。

优先页面：

- `report-center`
- `ai-chat`

执行内容：

- `report-center`：
  - 顶部改成“报表说明 + 当前筛选摘要 + 导出动作”
  - 导出按钮区改成真正的工具栏，而不是独立漂浮块
  - 图表和表格区的主次更清楚
- `ai-chat`：
  - 移动端先解决输入区、快捷问题区与底栏撞位
  - 结果区按“回答 / 结构化数据 / SQL 调试 / 导出”分区
  - 避免标题和快捷问题区在小屏首屏互相压住

## Phase R2-5：视觉审计专项清零

目标：把“移动端文本重叠”从偶发问题变成明确门禁项。

执行方式：

- 先按页面簇跑定向视觉审计，不全量反复跑：
  - `dashboard,risk-warning`
  - `real-time,safety-command`
  - `alert-notifications,alert-records`
  - `report-center,ai-chat`
- 每修一簇就补一轮定向验证
- 全量通过前，不继续扩散到 `workbench`、`employee-*`、`admin` 页

## 第二轮建议顺序

1. 先修 `PageHeroHeader`、`WarningCenterNav`、移动端头部模板
2. 再收 `dashboard + risk-warning`
3. 再收 `real-time + safety-command`
4. 再收 `alert-notifications + alert-records`
5. 最后做 `report-center + ai-chat`

原因：

- 当前最大问题不是桌面风格，而是移动端顶部信息组织
- `dashboard` 已经是新样板页，必须先把它打磨稳定
- 预警工作页和 AI / 报表页不适合同时推进，否则工作线会再次分叉

## 第二轮不做的事情

- 不扩散到全部后台页
- 不继续新增大量动画
- 不重新改全站配色方向
- 不在还没收口前就给每个页面单独设计一套 hero

## 第二轮验收标准

### 工程门禁

必须通过：

- `npm run audit:structure`
- `npm run build`

尽量通过：

- `npm run audit:visual`

如果全量视觉审计仍未全部通过，至少要满足：

- `dashboard,risk-warning` 定向审计无移动端标题重叠
- `alert-notifications,alert-records` 定向审计无页签与标题撞位

### 体验验收

- 移动端首屏不再出现标题压标题、标题压 tabs、标题压底栏的情况
- Cockpit 线页面能一眼看出是同一产品的一组页面
- Workbench 线页面能一眼看出是同一套操作型页面

## 本轮完成后的下一入口

如果第二轮完成，第三轮才进入：

- `workbench`
- `employee-archive`
- `employee-profile`
- `mine-entry`
- `admin` 系列页

也就是先把“核心展示页 + 核心工作页”的模板打稳，再推广到长表格和档案页。
