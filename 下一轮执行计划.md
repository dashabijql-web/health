# 下一轮执行计划

更新时间：2026-05-19

## 执行顺序

### 1. 跑一轮 WSL full-stack 验收

```bash
python3 /home/j/code/health/HealthShow/tests/run-full-stack-local.py --data-source both
```

目标：

- 确认 WSL 主入口在 `old + new` 双数据源下可完整执行。
- 确认后端、前端、模拟器、双库切换、summary 和 hermes archive 都按新链路产出。

### 2. 如果 full-stack 失败

规则：

- 只处理本轮首个阻塞问题。
- 不顺手扩改无关问题。
- 修复后只回归对应最小验证，再决定是否重跑 full-stack。

### 3. 如果 full-stack 通过

补跑根级 loop runner：

```bash
python3 /home/j/code/health/tests/run-health-loop.py --profile full --data-source both
```

目标：

- 确认根级 WSL runner 与 HealthShow full-stack runner 的衔接也已跑通。
- 确认 summary、issues、latest-run 指针写入正常。

### 4. 可选收尾

统一文档中的路径表述：

- 把仍残留的 `D:/Health` 入口表述继续收口到 `/home/j/code/health`
- 仅改当前主文档和高频入口文档，不重写无价值历史描述

### 5. 可选降噪

处理终端中的 `MODULE_TYPELESS_PACKAGE_JSON` 警告：

- 先确认是否适合给前端包显式声明模块类型
- 若风险过大，则仅记录为低优先级噪音，不在主链路上消耗时间

## 结束条件

- `run-full-stack-local.py --data-source both` 通过
- `run-health-loop.py --profile full --data-source both` 通过
- 或者明确记录当前首个阻塞问题、最小复现方式和下一步修复入口
