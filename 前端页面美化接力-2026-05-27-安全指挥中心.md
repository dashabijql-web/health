# 前端页面美化接力 - 2026-05-27 - 安全指挥中心

这份文档给下一位会话直接接力，不要再从头还原上下文。

## 1. 当前任务定义

- 工作区：`/home/j/code/health`
- 前端目录：`/home/j/code/health/HealthShow`
- 当前主任务：继续迭代 `safety-command` 页面美化
- 视觉方向：`电影级指挥中心 / cinematic command center`
- 用户偏好：
  - 用中文
  - 不要反复确认，直接小步迭代
  - 不要从头重做
  - 页面必须保持正确滚动

## 2. 已完成且已验证的状态

`safety-command` 已经不是救火态，结构和滚动基本稳定，当前是精修态。

已完成的关键点：

- 页面已切到共享旗舰壳：
  - `PageHeroHeader`
  - `MetricStrip`
- 已形成电影级指挥中心主视觉：
  - 深色金属底
  - 体积光
  - 悬浮面板
  - 强信息密度
- 整体滚动规则已经收稳：
  - 桌面端保留整页纵向滚动
  - 长列表只在各自面板内部滚动
  - 不要再把页面做成“完全锁死不能滚”
- 下半区已拆出聚焦组件：
  - `HandleProgressPanel.vue`
  - `VitalsTrendPanel.vue`
  - `WarningDistributionPanel.vue`
  - `WarningTrendPanel.vue`
- 右侧事件栏、右下 recap、低高度桌面适配都已经做过收口
- `safety-command/index.vue` 当前约 `404` 行，已经压到结构门禁可接受范围

## 3. 最新可依赖验证结果

最新一轮完整通过的验证来自 `2026-05-26`，不要忽略这组基线：

```bash
cd /home/j/code/health/HealthShow
node --test tests/frontend-beautification.mjs
npm run audit:structure
node scripts/with-env.mjs VISUAL_ROUTES=safety-command BASE_URL=http://127.0.0.1:9528 -- npm run audit:visual
npm run build
```

对应最新视觉产物目录：

- `/home/j/code/health/HealthShow/tests/visual/artifacts/2026-05-26T13-47-48-267Z`

重点截图：

- `/home/j/code/health/HealthShow/tests/visual/artifacts/2026-05-26T13-47-48-267Z/safety-command-desktop-1707.png`

视觉审计结论：

- `safety-command` 五档视口全通过
- `failed_routes = 0`
- `failed_checks = 0`

补充现况：

- `http://127.0.0.1:9528` 在 `2026-05-27` 复核仍返回 `HTTP/1.1 200 OK`

## 4. 当前真实停点

这轮没有再落新的补丁。原因不是方案没定，而是会话被多次打断。

因此，新会话要基于下面这个事实继续：

- 最新可信代码状态，就是上面那轮已验证通过的状态
- 最新可信视觉参考，就是 `2026-05-26T13-47-48-267Z` 那组截图
- 我已经把下一刀范围收敛好了，但还没正式写进代码

## 5. 下一刀只做什么

下一刀不要大改，只做一个很小的“对比度与标题轨道统一”精修。

只动这两个面板：

1. `HealthShow/src/views/safety-command/components/DeptRankTable.vue`
2. `HealthShow/src/views/safety-command/components/AreaMapGrid.vue`

目标非常具体：

- 统一标题轨道的视觉语法
- 提高辅助文案和说明 note 的可读性
- 统一胶囊控件亮度和层级
- 不动页面骨架
- 不碰滚动边界

建议落点：

- `DeptRankTable.vue`
  - 提升 `rank-head-note` 可读性
  - 微调 `legend-chip` 与 `scroll-btn` 的对比度
  - 提升 `rank-stat-note` 的说明层级
- `AreaMapGrid.vue`
  - 把标题说明从工具栏语义里拿出来，改成和其他面板一致的标题轨道说明
  - 提升 `area-stat-note` 可读性
  - 统一 `tb` / `tb-refresh` 的胶囊控件视觉

如果要补测试，优先只补一条很轻的结构回归：

- 在 `HealthShow/tests/frontend-beautification.mjs` 里给 `AreaMapGrid.vue` 的标题说明轨道加一个存在性断言

## 6. 不要做的事

- 不要从头重做 `safety-command`
- 不要重新碰整页滚动策略
- 不要把任务扩散到后端
- 不要大面积重排 `index.vue`
- 不要顺手改 `dashboard`、`real-time`、`risk-warning`
- 不要删除当前未提交的新组件文件

## 7. 当前工作树相关文件

这些文件已经在本轮会话里发生过前序改动，新会话默认要兼容，不要回滚：

- `HealthShow/src/views/safety-command/index.vue`
- `HealthShow/src/views/safety-command/safety-command.scss`
- `HealthShow/src/views/safety-command/components/EventPanel.vue`
- `HealthShow/src/views/safety-command/components/DeptRankTable.vue`
- `HealthShow/src/views/safety-command/components/AreaMapGrid.vue`
- `HealthShow/src/views/safety-command/components/RiskPersonPanel.vue`
- `HealthShow/src/views/safety-command/components/HandleProgressPanel.vue`
- `HealthShow/src/views/safety-command/components/VitalsTrendPanel.vue`
- `HealthShow/src/views/safety-command/components/WarningDistributionPanel.vue`
- `HealthShow/src/views/safety-command/components/WarningTrendPanel.vue`
- `HealthShow/tests/frontend-beautification.mjs`

## 8. 新会话起手步骤

直接按这个顺序继续，不要再做背景考古：

1. 打开这份文档
2. 查看截图：
   - `/home/j/code/health/HealthShow/tests/visual/artifacts/2026-05-26T13-47-48-267Z/safety-command-desktop-1707.png`
3. 只编辑：
   - `DeptRankTable.vue`
   - `AreaMapGrid.vue`
   - 如有必要再补 `frontend-beautification.mjs`
4. 完成后跑：

```bash
cd /home/j/code/health/HealthShow
node --test tests/frontend-beautification.mjs
npm run audit:structure
node scripts/with-env.mjs VISUAL_ROUTES=safety-command BASE_URL=http://127.0.0.1:9528 -- npm run audit:visual
npm run build
```

5. 再看最新 `layout-summary.md` 和新截图，继续下一轮小步精修

## 9. 一句话交接

`safety-command` 已经过了结构修复期，现在处于“左中两块面板的标题轨道和辅助说明层级统一”这一刀，下一会话直接从这两个组件下手，不要重开大手术。
